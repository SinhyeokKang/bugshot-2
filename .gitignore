node_modules
dist
dist-*
.DS_Store
*.local
.env
.env.*
!.env.example
# CI e2e 빌드용 더미 값 — 실 크레덴셜 아님(워크플로가 .env.local로 복사해 쓴다).
!.env.ci
.vite
*.tsbuildinfo
.claude/settings.local.json
.claude/projects/
e2e/.last-green
test-results
playwright-report
.wrangler
*.zip
# 커버리지 리포트 본체는 무시하되, git-tracked 트렌드 베이스라인은 커밋한다.
coverage/*
!coverage/baseline.json

# git worktree 체크아웃 (다른 브랜치 사본 — 커밋·테스트 수집 대상 아님)
.worktrees/
