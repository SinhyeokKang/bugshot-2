# e2e 커버리지 맵 · 수동 잔여

e2e 스위트가 **무엇을 커버하고 무엇이 빠졌는지**의 단일 출처. 개요·실행법·헬퍼는 [README.md](./README.md), 함정은 [GOTCHAS.md](./GOTCHAS.md). 시나리오가 늘면 이 표를 함께 갱신한다.

## 커버리지 맵

| spec | 시나리오 | 형태 |
|---|---|---|
| `activetab-broad-permission.spec.ts` | 광역 host 권한 보유 시 cross-origin 커버 URL 이동 → 패널 유지(세션만 정리·activated 보존) / 비커버 URL(chrome://) → deactivate / cross-origin 이동 후 `captureVisibleTab` 성공(<all_urls> 캡처 능력). bg storage(activated set·세션 키)·캡처 data URL을 SW로 판정 | 3 tests |
| `style-edit-flow.spec.ts` | 요소 선택 → 스타일 수정 → [다음] → drafting 진입 (next-step 경로 유일 커버) | 1 test |
| `style-code-view.spec.ts` | 편집(폼)/CSS(코드) 2탭 + **CodeMirror CSS 뷰**(`style-css-view`/`.cm-content`, 아이콘 Paintbrush/Code2) — 탭 스왑(CSS 탭에서 `class-editor`·`text-editor`·폼 섹션 hidden, 에디터 노출) / **specified prefill + shorthand 병합**(4면 동일 padding→`padding: 8px` 표시·`padding-top` longhand 미노출)·무편집 시 `next-step` aria-disabled(오버라이드 0, phantom diff 없음) / 선언 append(`padding-top: 32px;` — `}` 직전 삽입) 라이브 적용 + 변경 다이얼로그 `[data-prop=padding-top]` / 폼↔CSS **양방향 동기화**(폼 color(#00ff00) 편집이 CSS 재진입 시 에디터에 재파생) / 폼 미지원 임의 속성(`cursor: pointer;`) 폼↔CSS 왕복 유지 / **버퍼 다중요소 복원**(#title 편집→#card repick(자기 specified prefill=`border-radius`·cursor 없음)→#title 재선택 시 편집 복원) / **삭제=initial 원복**(전체 삭제→specified color/padding `initial`→page color rgb(0,0,0)·padding-top 0px + 다이얼로그 `[data-prop=color]` initial 행) / CSS 탭 **영속**(패널 재열기 후 `data-state=active`로 code 시작). CodeMirror 구동은 GOTCHAS 참조(fill 불가·append·삭제=initial). styleEditorView 영속이라 afterAll에서 storage로 form 복원 | serial 8 |
| `style-changes-dialog.spec.ts` | 변경사항 다이얼로그 — 트리거 badge·요소별 카드·행/전체 초기화·세션 폐기 (16 체크 serial) | serial 16 |
| `style-changes-stacked.spec.ts` | 전 에디터 타입 스타일 누적 + shorthand collapse + class 편집 후 as-is baseline 보존 + class diff 변경 토큰 볼드 강조(`changes-tobe` 안 `<strong>`=추가 토큰, 공통 토큰 평문, as-is 강조 0) | serial 6 |
| `style-field-fixes.spec.ts` | style-panel-field-fixes — (D) length 라이브 입력 px 정규화 즉시 적용 / (C) SelectProp 빈 옵션 `(원본값)` 선택이 prop 리셋(`__empty__` 미기록, changes-row 0건) / (E) 4면 동일(`#el1`)→상이(`#quad`) repick 후 한 면 입력이 4면 안 덮음 + 링크 토글 어포던스 유지(눌러 통일) | serial 2+2 |
| `linked-quad-merge.spec.ts` | linked-quad-merge — padding 4면 동일(`#el1`) → linked 단일 병합 필드(`merged-side-field`) 1개·per-side(`quad-sides`) 부재 / LinkToggle 끄면 4필드(`quad-sides` 안 트리거 4개) / 단일 필드 16px 입력 → 4면 동시 반영(`setAllProps`) + 단일 필드 유지 / 변경 다이얼로그 `padding` 단일 행 collapse(`padding-top` 부재) / border-width·style·color 셋 다 linked 변경(2px dashed red) → **2차 통합** `border` 한 행(`2px dashed red`, `border-width`/`-style` 개별 행 부재) | serial 5 |
| `border-per-side.spec.ts` | per-side-border-editing — 전용 **Border 섹션**(Container>Border>Size)에서 변별 편집. border-width 한 변(bottom) 입력 라이브 반영(나머지 변 baseline 유지) / 링크 토글 네 변 동일 일괄 / 네 변 동일 → 변경사항 다이얼로그 `border-width` 단일 행 collapse(`border-top-width` 행 부재) / border-style **4변 QuadStyleProp** 링크 토글 네 변 일괄(dashed) / border-style 한 변(top)만 dotted 변별 반영 / border-color 변별 한 변(top) 라이브 반영(캡처 버그 픽스 경로). `#el1`(.swatch, `border:1px solid #ddd` baseline) 사용 | serial 6 |
| `style-token-hint.spec.ts` | length 토큰 변경 시 필드 우측 미리보기(`token-value-hint`) 갱신 — width에 `--space-sm`(8px) 선택→hint "8px", `--space-lg`(32px)로 변경→hint "32px". 회귀: hint가 stale computed(편집 전 baseline)에 묶여 토큰 변경에 안 따라오던 버그(`rightHintText` 토큰 정의값 우선 수정) | 1 test |
| `style-cross-origin-section.spec.ts` | cross-origin sheet만 적용된 `#target` 선택 시 specified 빔 → computed fallback으로 섹션 펼침. **cross-origin-styles 보강 추가 후에도 green** — fixture sheet 호스트가 localhost(loopback)라 SSRF 가드가 background fetch를 차단해 specified가 여전히 비고 fallback 분기 유지(보강 inert). 실 보강은 수동 잔여 | 1 test |
| `style-zindex.spec.ts` | element-zindex — **Position 섹션** z-index(full-width TextProp) 입력 라이브 적용(`#title` `toHaveCSS` 9999) + 변경 다이얼로그 `[data-prop="z-index"]` 행 노출 / 입력 비우기(`fill("")`) → inlineStyle 키 delete로 원본(auto) 복원 + `changes-trigger` 재비활성. **#title은 position류 전부 기본값이라 Position 섹션 접힘 → `ensureSectionOpen`으로 펼친 뒤 편집** | serial 2 |
| `style-position-section.spec.ts` | position-section — Layout에서 분리된 독립 **Position 섹션**(position SelectProp + z-index + inset QuadProp `top/right/bottom/left`). `#title`(position류 기본값→접힘) `ensureSectionOpen("section-position-toggle","position")` 펼침 → position `relative` 선택 라이브 반영(`toHaveCSS` position relative) → inset top 12px(`setQuadSideValue("inset",0,...)`, 4면 동일 auto라 linked→unlink 후 입력) 라이브 적용(`toHaveCSS` top 12px) / 변경 다이얼로그 `[data-prop="position"]`·`[data-prop="top"]` 행 노출 / inset top 비우면(`""`) top 행만 제거(relative라 computed 0px)·position·z-index 행 유지·trigger 활성 | serial 3 |
| `style-code-value-hints.spec.ts` | code-view 값 자동완성 property-aware — `#tbl` CSS 뷰에서 `border-collapse: c` 타이핑 시 콤보박스에 `collapse` 노출(과거 generic 덤프 `[center,checked,cue,currentColor]`엔 없던 값), `overflow: s`→`scroll`. `propValues.PROP_VALUES` 단일 출처 + boost 회귀 가드. 속성 고유 값이 lang-css generic과 중복 노출될 수 있어 `.first()`로 스코프(내 boosted 소스가 top·aria-selected). afterAll에서 form 복원 | serial 1 |
| `style-table-section.spec.ts` | table-section — 테이블 속성 전용 **Table 섹션**(table-layout·border-collapse SelectProp / border-spacing TextProp / caption-side·empty-cells·vertical-align). `#tbl`(테이블 속성 전부 기본값→접힘) `ensureSectionOpen("section-table-toggle","table-layout")` 펼침 → table-layout `fixed` 선택 라이브 적용(`toHaveCSS` table-layout fixed) / 변경 다이얼로그 `[data-prop="table-layout"]` 행 노출. 원 이슈("table-layout 안 먹힘")를 폼 경로로 가드 | serial 2 |
| `style-bugfix-regression.spec.ts` | style-panel-bug-hunt 픽스 회귀 — #4 baseline 동일값(width 200px) 입력 시 phantom diff 없음(`changes-trigger` disabled) / #1 line-height unitless `2` 입력이 `2px` 강제 안 됨(computed=2×font-size) / #3 named-color 토큰(`--brand:tomato`) color 드롭다운 노출 / #5 단축 hex `abc` 라이브 미확장(페이지 색 미적용)·blur 시 `#aabbcc` 확장 / #6 multiplier 토큰(`calc(var(--space-sm)*2)`) hint 곱셈 표시(16px) / #7 unitless `0` 토큰(`--space-0`) length 드롭다운 노출 | serial 6 |
| `buffered-reselect-edit.spec.ts` | 버퍼 요소 재선택 → 편집 복원 → 추가 편집 후 재버퍼에 이전 편집 유지 | serial 2 |
| `dom-tree-nav.spec.ts` | DOM 트리 다이얼로그로 이동 시 현재 요소 편집이 버퍼에 유지 (회귀 — DomTree도 useBufferThenSwitch 경유) | 1 test |
| `capture.spec.ts` | screenshot 영역 드래그 캡처 / element-shot → drafting / element-shot previewing env DOM 행 (회귀) | 3 tests |
| `capture-methods.spec.ts` | capturing 하단 캡처 방식 툴바 — **뷰포트 캡처**(드래그 없이 `capture-method-viewport` 클릭 → drafting) / **스크롤 캡처**(`scroll-capture.html` fixture = 뷰포트 1.5배 높이 + fixed 헤더 → 타일 2장 스티칭 → drafting 이미지가 뷰포트보다 세로로 길고, 캡처 후 페이지 scrollY 원복). 스크롤 캡처는 타일마다 `tab.active`를 확인하므로 fixture를 앞으로 보낸 뒤 백그라운드 패널 버튼을 DOM 클릭으로 누른다(GOTCHAS) | 2 tests |
| `annotation-overlay.spec.ts` | konva 주석 오버레이 — screenshot 캡처 → drafting 미디어 연필(`annotation-edit`) → 오버레이(`annotation-overlay`) 열림 + 도구 active(`annotation-tool-rect` `data-active`) + 빈 상태 `annotation-done` disabled + 취소(`annotation-cancel`) → img(`media-preview-img`) src 불변 / Stage 캔버스 드래그로 rect 커밋 → done 활성 → 클릭 시 오버레이 닫힘 + img src가 raw→`data:image/webp` 전이 + 제거(`annotation-remove`, RotateCcw) → raw 복귀·버튼 소실. 도형 시각 정합·6도구·undo/redo·transform은 수동(canvas 픽셀 판정 불가) | serial 2 |
| `annotation-zoom.spec.ts` | 어노테이션 캔버스 줌·팬 — **스크롤(페이지 전체) 캡처**로 진입해야 fit<1·팬 가능(영역 캡처는 fit=1이라 줌 시나리오를 못 태운다). 진입 시 맞춤 상태(`annotation-zoom-fit` 부재 + `annotation-zoom-out` 활성=전체 스톱 존재) / `[+]`(`annotation-zoom-in`) → 라벨(`annotation-zoom-level`) 배율 증가 + 맞춤 버튼 등장 → 맞춤 클릭 → 진입 라벨 복귀·버튼 소실 / 콤보박스 `100%` 선택 → 라벨 100%, `[+]` 상한 → `400%` + `aria-disabled` / 선택 도구 빈 곳 드래그 → 뷰포트(`annotation-canvas-viewport`) `scrollTop` 증가 / **rect 그린 뒤 도형 클릭 → 선택(`annotation-delete` enabled), 빈 곳 클릭 → 해제**(팬 도입 회귀 가드 — 그리기 직후엔 자동 선택 안 됨) / 도형 드래그 → 이동하고 `scrollTop` 불변(팬 아님) / 100% 확대 상태로 그려 done → 결과 img `naturalWidth` == 원본(export가 배율에 안 묶임) / 전체(fitAll)까지 축소하면 `[-]`가 `aria-disabled`. 배율은 **상대 비교·버튼 상태로만 판정**(e2e 뷰포트 480px에선 fit이 90%대라 절대 배율 단언은 깨진다) | serial 8 |
| `recording-annotation.spec.ts` | 녹화 중 그리기 오버레이 — SW `chrome.tabs.sendMessage`로 `annotation.show/setTool/hide` 직구동(picker 엔트리 all_frames 자동 주입, 실 녹화 없이). setTool은 pen/highlight+color/strokeWidth/opacity, off는 `{tool:null}`. show → open shadow host(`__bugshot_annotation_host`) 마운트 + svg·blocker 존재 + host `pointer-events:none`·blocker 펜 OFF(기본 pass-through) / pen setTool + fixture 드래그 → svg `<g>` 획 증가 / highlight setTool + 드래그 → 획 path 스타일(stroke=color·stroke-width=두께배율 16·stroke-opacity=0.4) + 그룹당 path 1개(흰 아웃라인 없음) / off(`tool:null`) → 드래그해도 획 0 + 페이지 클릭 통과(blocker pass-through) / hide → host 제거. 획 3초 페이드·시각 정합·실 녹화 합성은 수동(canvas 아님·타이머 판정 밖) | serial 5 |
| `capture-modes-layout.spec.ts` | revert-idle-capture-layout 1x2x2 idle 레이아웃(Row1 `mode-element` primary 단독=ButtonGroup 밖 / Row2 ButtonGroup `[mode-element-shot][mode-screenshot]`·`mode-element` 미포함 / Row3 2-segment ButtonGroup `[mode-record][replay-button]`) + 구 `mode-video`/`mode-screen-record`/`mode-record-settings`(⚙) 부재 / 녹화·리플레이 균등 너비(±2px) / 기본 모드=탭(`svg.lucide-app-window`) / **비활성 replay-button(force click) → 설정 탭 이동**(`tab-settings`+`settings-sub-issue` active, `getByRole("dialog")` 0 — 다이얼로그 제거됨) / 설정 `recording-mode-screen` 선택 → debug 복귀 시 녹화 버튼 아이콘 라이브 전환(`svg.lucide-monitor-play`). serial, afterAll로 설정 탭 `recording-mode-tab` 복원. 실제 getDisplayMedia/tabCapture 녹화는 자동화 불가(수동 잔여) | serial 7 |
| `download-buttons.spec.ts` | 미디어·로그 섹션 다운로드 버튼 — screenshot 캡처 → drafting·preview 미디어 download=`screenshot.webp` / console·network 로그 적재 후 drafting 로그 섹션 download=`logs.html`. blob URL `<a download>` → Playwright download 이벤트로 파일명 판정 | 2 tests |
| `draft-resume.spec.ts` | to-preview(confirmDraft) 초안 저장 → 패널 재열기 → 이슈 목록 → DraftDetailDialog 내용 복원 | 1 test |
| `draft-field-edit.spec.ts` | 저장된 draft 필드 편집 — freeform 초안 저장 → 이슈 목록 → 상세(`draft-detail-dialog`) 위에서: 섹션 [수정](`edit-field-description`)→편집 다이얼로그(`draft-edit-dialog`) Tiptap `fill`→[저장](`draft-edit-save`)→상세에 새 텍스트 반영·원본 소멸 / 섹션 편집 후 [취소](`draft-edit-cancel`)→상세 원본 유지·미저장 텍스트 부재 / 제목 [수정](`edit-title`)→Input `fill`→저장→상세 제목 + 뒤 `issue-row` 제목 동시 갱신(최상위 title). draft-resume 진입 재사용, serial(상세 다이얼로그 열어 둔 채 3연속) | serial 3 |
| `freeform-draft.spec.ts` | freeform 진입 → 제목·섹션 입력 → preview 렌더 → 마크다운 복사 페이로드(clipboard stub) | 1 test |
| `console-linkify.spec.ts` | 콘솔 로그 linkify — uncaught error(정적 인라인 스크립트 throw, `console-error.html`) 1건으로 ① 접힌 헤더 URL 링크 클릭이 행을 안 펼침(stopPropagation, capture-phase preventDefault로 네비 억제) ② 헤더 URL이 포커스 가능 native anchor(target=_blank) ③ 펼친 행 `console-stack` 안 `a[href]`≥1·href에 `:line:col` 꼬리 없음(line:col 제거 검증, 행 단위 카운트는 항상 있는 pageUrl 링크로 회귀 못 잡아 stack 컨테이너로 스코프). (시나리오 4 action-nav-link 유지는 30s Replay 캡처 spec 제거로 미커버 — 수동 잔여) | serial 3 |
| `log-capture.spec.ts` | console·network 로그 수집 → 항목 표시 → network 상세 status → clear(양쪽) | serial 2 |
| `logs-cross-page.spec.ts` | same-origin 이동 후 로그 보존·누적(webNavigation 꼬리 sync) / reload 시 클리어 | serial 2 |
| `logs-iframe.spec.ts` | same-origin iframe 내부 console 로그 캡처 (all_frames + sentinel 경로) | 1 test |
| `logs-error-warn.spec.ts` | arm 중 page `console.error`/`console.warn` → error·warn 레벨 캡처 (`data-level` 단언) / iframe error 캡처 | serial 2 |
| `logs-origin-filter.spec.ts` | 127.0.0.1 top + localhost iframe cross-origin 로그 → OriginFilterBar 노출·필터링·해제 | 1 test |
| `websocket-log.spec.ts` | WebSocket 프레임 캡처 — fixture ws echo 연결 → 네트워크 행(`data-ws`) 1개 + 클릭 시 Messages 탭(`detail-tab-messages`) 기본 활성 + send/receive 프레임(`data-frame-direction`) / 연결 close 후 Headers(`detail-tab-headers`) status 101 / Send 방향 필터(`ws-dir-filter[data-dir=send]`) → receive 데이터 프레임 0·open 유지 / 무간섭(`__wsCheck`: `WebSocket.OPEN===1`+`instanceof`) / 동시 2연결 → 행 2개. capturing 확인(warm-up fetch)→clear 후 `__openWs`로 정확히 1회 연결 | serial 4 |
| `network-body-search.spec.ts` | 네트워크 로그 검색이 URL뿐 아니라 **응답 본문**까지 매칭 — `/e2e-json*`(본문에만 마커 `zqxbodyneedle` 담은 JSON) + 노이즈 요청 적재 → 마커로 검색 시 URL엔 없어도 본문 매칭 행만 잔존(1건), 해제 시 재확장. 검색 200ms 디바운스는 `toHaveCount`/`expect.poll` 재시도로 흡수 | 1 test |
| `network-search-highlight.spec.ts` | 네트워크 로그 검색어 **하이라이트** — ① `/e2e-json`(본문 마커) 검색→행 클릭→`detail-tab-response`에서 응답 JSON 트리의 매칭 값이 `mark[data-testid=log-highlight]`로 강조(텍스트=마커), 검색 해제 시 mark 0. ② WS 격리: `__openWs(host)`로 프레임 `{"ping":"<host>"}` 적재→host 검색(ws url에 host 있어 행 유지)→`detail-tab-headers`엔 url host 하이라이트(쿼리 활성 증명)·`detail-tab-messages` FrameBody엔 보이는 mark 0(highlightQuery 미전달 격리). mark 스코프는 `:visible`(숨은 headers 탭 mark 제외) | 2 tests |
| `action-log-scope.spec.ts` | 액션 로그 스코프 확장(v1.5.8 — video 전용 → console/network와 동일, element만 제외) — ① 스크린샷 drafting에 `action-log-card` 노출 + 첨부 토글 기본 ON(`startCapturing`의 3종 일괄 true) + 카드를 열면 idle에서 발생시킨 클릭이 `[data-entry-id]`로 기록됨 ② 액션 토글만 OFF → preview에서 `action-log-card` 제외되고 `console-log-card`는 유지(대조 판정) ③ element drafting은 액션·콘솔 카드 둘 다 부재(로그 전무 회귀 가드). 액션은 idle 서브탭이 없어 콘솔 항목을 sync 프록시로 polling | serial 3 |
| `logs-prearm.spec.ts` | active origin reload 시 로드 초반(head/body) ① network marker fetch ② console.error/warn(조기 후킹 installEwWrap)·log가 pre-arm 버퍼로 잡혀 logClear 경계를 우회 보존(preArm 마커) → 로그에 나타남. ③ recorders-entry가 TDZ 에러 없이 평가됨(action-recorder throttle hoist 회귀 — `pageerror` 부재 단언). recorders-entry 동기 IIFE 전제 회귀 가드 | serial 3 |
| `onboarding.spec.ts` | 연동 0개 온보딩(자동 전환 폐지 → CTA 배너 유도) — ① 클릭 없이 `tab-debug` 착지 + `tab-integrations` 비활성 + `integrations-cta` 노출 + `mode-element` 활성(배너가 캡처를 막지 않음) ② 배너 클릭 → `tab-integrations` active ③ `close()`→`openPanel()` 재오픈해도 `tab-debug` 유지(반복 리다이렉트 부재) ④ freeform→`to-preview`로 previewing 도달 시 배너 노출 + `issue-submit-open` disabled 공존(안내 vs 차단은 목적이 다름) + 배너 클릭 전환 ⑤ github account storage seed(`bugshot-settings` v10) → 배너 `toHaveCount(0)`, `finally`로 seed 제거 | 5 tests |
| `clickup-submit-gating.spec.ts` | ClickUp 제출 게이팅 — account를 storage seed로 우회 연결 → freeform drafting → preview → 제출 다이얼로그. List 미선택(no last) → `submit-issue-confirm` 비활성 / `lastSubmitFields.clickup`로 workspace+list prefill → 활성. `fieldsReady`(`workspaceId && listId`) 게이트 회귀 가드. SW fetch 없이(콤보박스 미오픈) 판정 | serial 2 |
| `slack-issue-promotion.spec.ts` | Slack 이슈 승격 — slack+jira+github account & slackPreserved 이슈를 storage seed(`bugshot-settings`+`bugshot-issues`) 우회. 보존 이슈 fixture는 `url`=permalink(`/archives/C123/p…`)·`key`=메시지 ts(`1700000000.123456`). 트래커 연결 시 카드 우측 [자세히](`view-detail-issue`)·[승격](`promote-issue`) 노출 / 본문 클릭은 `chrome.tabs.create` 스파이로 permalink URL 확인 + `draft-detail-dialog` 부재(permalink 이동, 부정 판정) / [자세히]→`draft-detail-dialog` 열림(`submit-issue-confirm` 부재=제출 자동오픈 안 함, **Slack 보존 상세에도 `edit-title`·`edit-field-description` [수정] 버튼 노출**=`canEditDraftFields`=draft‖slackPreserved → 섹션 편집→저장→상세 새 텍스트 반영·원본 소멸, 승격 전 문구 다듬기) / **편집 문구가 승격 트래커 본문에 실림**(view-detail 편집→Esc 닫기→promote→`jira.submitIssue` payload 스파이 기록 `__jiraSubmits`: `description` ADF에 편집 문구 포함·seed 원문 부재 = buildCtxForSubmit이 patch된 draft 소비) / [승격]→`submit-issue-confirm` + 플랫폼 탭 `platform-tab-jira`/`-github` present·`platform-tab-slack` 부재(Slack 제외) / submitted 필터 1건·draft 필터 0건 / slack-only seed → 두 버튼 부재 + `slack-submitted-badge` 유지 / **승격 백링크**(`chrome.runtime.sendMessage` 스파이로 `jira.submitIssue`를 fake 성공으로 가로채 SW fetch 없이 제출 완료 + `slack.postMessage`를 `window.__slackPosts`로 기록): Jira 승격 시 `slack.postMessage` 1회(`channelId`=C123·`threadTs`=key·`text`에 트래커 URL) + 성공 화면(`BUG-123` 링크) / slack 미연결 seed → 호출 0회·성공 유지 / `slack.postMessage` reject해도 성공 화면 유지(best-effort) | serial 10 |
| `slack-submit-gating.spec.ts` | Slack 제출 게이팅(8번째 플랫폼, 메시지 앱) — slack account(OAuth) storage seed 우회 연결. (1) `tab-integrations` 진입 시 SlackConnectedBody가 seed한 워크스페이스 이름(i18n 무관 값) 노출 / (2) freeform→preview→제출 다이얼로그에서 채널 미선택 → `submit-issue-confirm` 비활성 / (3) `lastSubmitFields.slack`로 channel prefill → 활성. `fieldsReady`(`channelId`) 게이트 회귀 가드 / (4) **채널/멘션 직전값 우선 복원** — `slackAccount.defaults`(기본 채널)와 `lastSubmitFields.slack`(직전 채널·멘션)이 다를 때, `initialSlackFields`가 last 우선이라 채널 콤보박스 트리거가 직전 채널(`#last-channel`)을 표시(`#default-channel` 아님)하고 멘션도 복원(sameChannel 게이트 통과). `submit-fields-dialog`로 스코프(기본 채널명은 숨겨진 연동 탭 SlackConnectForm에도 떠 전역 단언 부정확). 회귀: defaults가 last를 덮어 직전 채널·멘션이 사라지던 버그. clickup-submit-gating과 동일 패턴(version:10, listChannels SW fetch 미오픈) | serial 4 |
| `slack-promote-media-guard.spec.ts` | Slack 보존 이슈 GitHub 승격 시 **미디어 업로드 실패(href:null) → 이슈 생성 전 중단 + 원본 보존**. slack+github account seed(`lastSubmitFields.github`로 owner/repo prefill — 없으면 requiredMissing 조기 throw) + screenshot 모드 slackPreserved 이슈 + IndexedDB before 이미지 blob seed(가드는 미디어가 captureFiles에 있어야 발동). `chrome.runtime.sendMessage` 스파이로 `github.uploadFiles`를 요청 전 파일 href:null로, `github.submitIssue`는 호출 횟수만 기록. [승격]→github confirm 후: `__ghUpload≥1`(업로드 경로 진입=early bail 아님)·`__ghSubmit===0`(이슈 생성 차단)·`bugshot-issues` storage에 `slackPreserved/platform:slack/status:submitted` 불변(markSubmitted 미실행). 회귀: github.com 쿠키 세션 soft-fail 시 깨진 이슈 생성+원본 blob 전멸하던 데이터 소실 | 1 test |
| `picker-guard.spec.ts` | 미지원 iframe 거부 — 중첩(2-depth: 등록된 #outer 핸드오프 통과 후 1-depth picker가 미등록 #inner 거부)·미주입(srcdoc — `<all_urls>` 미매치) iframe 클릭 → `iframe-unsupported-dialog` + idle 복귀. 다이얼로그 노출까지 클릭 재시도(`pickUntilUnsupported`)로 announce 등록 레이스 흡수 | serial 2 |
| `picker-iframe.spec.ts` | picker all_frames — **same-origin**(`iframe.html`): picking 중 iframe ESC → 전 프레임 overlay 정리+idle(teardown) / iframe 내부 요소 선택 → 스타일 에디터 / color 편집 → iframe DOM `toHaveCSS` / top 재선택 → 두 편집 버퍼 분리 유지 + same-origin 배지 0 / 버퍼(iframe) 행 초기화 → iframe DOM 원복(frameId 라우팅). **cross-origin**(`cross-origin.html`, 127.0.0.1 top + localhost iframe): 내부 선택·편집 → 프레임 DOM 반영 / top의 **동일 selector**(#title) 재선택 → 복합키 분리(카드 2장) + iframe 카드만 `origin-badge`(localhost) / 버퍼 행 초기화 → cross-origin DOM 원복 | serial 5 + serial 3 |
| `session.spec.ts` | 패널 닫기/재열기 세션 폐기 · 두 탭 독립 세션 (dialog spec test14/16 비중복분) | 2 tests |
| `settings-sections.spec.ts` | 이슈 섹션 토글(notes ON·expectedResult OFF) → drafting·preview 본문 반영 (finally로 기본값 복원) | 1 test |
| `attachments.spec.ts` | 파일 첨부 — 설정 토글(기본 OFF) 게이팅 → drafting 첨부 섹션 노출 / `setInputFiles` 다중 추가 → 카운터 갱신 / 개별 삭제 / 상한 10 도달 시 `10/10`+버튼 비활성 / 패널 재오픈 후 메타 복원 유지 / 토글 OFF 재숨김. settings 영속이라 afterAll OFF 복원 | serial 6 |
| `unsupported-url.spec.ts` | chrome:// 탭에서 mode 진입 → pickerUnavailable 다이얼로그 → idle 복귀 | 1 test |
| `ai-draft.spec.ts` | AI 초안(BYOK mock) — 선입력 텍스트가 요청 payload에 포함 + 이미지 없는 섹션 전체 교체 / inline 이미지 보존(상단 유지)+텍스트 교체 + payload에 `image_url` 포함 / 재생성 시 최신 선입력 반영. `panel.route("**/chat/completions")` mock + llm seed | serial 3 |
| `ai-styling.spec.ts` | AI 스타일링(BYOK mock) — element 선택 → AI 응답 `inlineStyle`이 페이지 DOM(`toHaveCSS`)·변경사항 다이얼로그(`changes-row`)에 적용 + 요청 발생 / payload에 선택 요소 스타일 컨텍스트 포함 / **CSS 탭 유지 상태에서 2차 AI 턴 → 코드 뷰 본문 선언 보존**(`selectorLock` protected range가 uiw의 전체 doc 교체를 드롭해 선택자 1행으로 붕괴하던 회귀 — 탭 왕복 remount가 증상을 가리므로 CSS 탭에 머문 채 판정) / **응답 값 필터**(`isDeniedStyleValue`) — `background-image: url(https://…)`는 드롭돼 페이지 computed `none`·`changes-row` 0건, 같은 응답의 `color`는 정상 적용(키 필터만으론 값으로 나가는 외부 요청을 못 막는다). mock 응답은 `mockStyling` 변수라 턴마다 교체(같은 값이면 doc 미변경 → 재동기화 경로 미진입). styleEditorView 영속이라 afterAll에서 storage로 form 복원. ai-draft와 동일 mock 패턴 | serial 4 |
| `ai-local-provider.spec.ts` | AI 초안 — **로컬 BYOK(compact 좌표)**. `baseUrl`만 loopback(`http://localhost:11434/v1`)이라 `byokCapabilities`가 `LOCAL_BYOK_CAPABILITIES`(compact·이미지 불가·8k 예산)로 판정하는 경로. ① inline 이미지가 본문에 있어도 payload에 `image_url` **부재**(이미지를 못 받는 프로바이더에 이미지를 실으면 조용히 버려지는데 프롬프트만 "분석하라"고 지시해 환각) — 이미지 자체는 본문에 보존 ② **예산 캡 원문 보존**: compact `existingDraftChars=400`을 넘는 제목·description은 프롬프트에서 통째로 빠지고(payload에 마커 부재), 모델이 그 자리에 지어낸 텍스트를 반환해도 사용자 원문이 유지된다(제목 `toHaveValue` + 본문 마커 잔존·MOCK 부재). 대조군으로 캡 안에 드는 짧은 원문(expectedResult)은 정상 교체돼 보존이 과잉이 아님을 락. ai-draft와 동일 mock 패턴(baseUrl만 상이) | serial 2 |
| `logview/log-viewer.spec.ts` (**logview**) | 내보낸 로그 뷰어(standalone HTML, viewport 1280×800) — 확장 없이 `dist-log-viewer/index.html`에 합성 로그 주입(`setContent`). **i18n 회귀**: 액션 필터 칩이 정확 라벨(`키`/`Keys` 등)·raw i18n 키(`actionLog.filter.keypress`) 미노출 / 네트워크 검색 placeholder 본문 검색 안내(`URL·본문 검색…`/`Search URL & body…`) — ko/en 양쪽(`navigator.language` locale). **동작**(en): 탭 전환+badge / 액션 필터링(keypress) / 네트워크 본문 검색(URL엔 없는 마커가 응답 본문 매칭) / 콘솔 레벨 필터+검색 / origin 필터 2종 / 빈 로그 탭 disabled / screenshot 분할 모드 공존 / **Report 탭**(제목·env 2행·paragraph+orderedList 섹션 + markdown copy=`__copiedText` payload) / **다운로드 버튼**(탭별 `Console-log.json`·`Network-log.har`·`Action-log.json` 파일명) | 15 tests (ko/en label 6 + behavior 9) |

## 수동 잔여 (자동화 못 한 것 — 이유 포함)

스크립트로 판정 불가하거나 e2e 환경 제약으로 빠진 항목. 새로 자동화하면 위 맵으로 옮긴다.

- **picker unsupported URL 중 webstore 차단 호스트** — 실제 webstore 접근이 필요해 fixture 서버로 재현 불가 (미지원 *스킴* 가드는 `unsupported-url.spec.ts`가 chrome://로 커버).
- **iframe 요소 캡처 크롭 정확도·top overlay 미포함** — before/after·요소 스크린샷의 크롭 좌표(offset 변환·topViewport 스케일)와 "top overlay가 캡처에 안 찍힘"은 `captureVisibleTab` 픽셀 판정이라 e2e 불가(+캡처 진입 spec은 quota flaky 정책상 금지 — GOTCHAS). 좌표 순수부는 `frame-geometry.test.ts`(composeTopRect·arm 게이트·스푸핑 방어) 단위로, 크롭 시각 정합은 수동(tasks.md).
- **iframe hover 하이라이트 시각 정합·blocker 핸드오프 경계 깜빡임·다수 iframe(광고) 성능** — overlay는 shadow DOM 시각 요소라 셀렉터 판정 불가(선택 성공이 핸드오프+클릭 라우팅을 함의 — `picker-iframe.spec`). 인접 링크 오네비게이션·경계 깜빡임·프레임 수십 개 성능 체감은 수동(tasks.md).
- **수동 캡처 video 모드**(탭 녹화 — `recordingMode="tab"`일 때의 `mode-record`) — `getUserMedia`+`chrome.tabCapture` 의존이라 headed 자동화에서 fake media 없이는 불안정.
- **`integrations-cta` 배너의 시각 접합·트렁케이션** — 배너 *존재/클릭*은 `onboarding.spec`이 커버하나, 시각 판정은 픽셀 문제라 수동: ① `DraftDetailDialog`에서 `-mx-6 -mt-5 -mb-5`가 `DialogContent`의 `gap-5`/`p-6`을 상쇄해 스크롤 영역·`DialogFooter` 양쪽에 갭 없이 접합되는지 ② `EmptyState`의 배너 위 빈 여백(스크롤 경계가 아니라 중앙정렬 잔여 공간)이 어색하지 않은지 ③ 320px 최소 폭에서 ko 문구가 truncate돼도 우측 `Blocks`+"플랫폼 추가"가 살아남는지 ④ 다크모드 대비(WCAG AA) ⑤ Tab 키로 세 배너(amber CTA·purple AI·teal AI)에 focus 링이 동일하게 뜨는지. (`DraftDetailDialog` 배너 경로 자체도 미커버 — 배너를 띄우려면 slack-preserved 이슈 또는 연동 0개 상세 진입 seed가 필요.)
- **konva 주석 — 도형 시각 정합·6도구 그리기/이동/리사이즈/회전·undo/redo·색상·두께·텍스트 인라인 입력** — Konva Stage가 단일 `<canvas>`라 도형이 DOM 노드가 아니어서 "무엇이 어떻게 그려졌나"를 셀렉터로 판정 불가(픽셀 비교 영역). `annotation-overlay.spec.ts`가 캔버스 *외부* 신호(오버레이 open/close·도구 active·img src 전이)만 커버하고, rect 1개 드래그로 done 활성화까지만 탄다. 순수 도형 로직(좌표 변환·undo/redo·empty 판정·transform 흡수)은 `shapes.test.ts`/`history.test.ts`/`presets.test.ts` 단위로 커버. 큰 스크린샷 자연 해상도 출력·좁은 폭 3단 툴바 리플로우·세션 복원(`screenshotAnnotated`)·IndexedDB before 첨부 반영은 수동(tasks.md).
- **어노테이션 줌·팬 — pointer capture 상실 회귀·중앙 앵커·커서·키보드 스크롤** — (a) **"두 번째 도형부터 커서를 따라다니는" 회귀(POSTMORTEM 2026-07-14)는 Playwright로 재현되지 않는다**: CDP 합성 입력은 실제 Chrome의 암묵적 pointer-capture 해제를 유발하지 않아, 영역/전체 캡처 × 클릭/드래그 조건에서 프로브가 전부 통과했는데 실 브라우저는 먹통이었다. **실 Chrome에서 도형을 2개 연속(두 번째를 첫 도형 위에서 시작) 그리는 것만이 유일한 감지 수단**이다. (b) 확대·축소 시 중앙 앵커가 유지되는지, (c) 커서(`grab`/`grabbing`/`crosshair`), (d) Tab 포커스 후 화살표키 스크롤, (e) 400% 확대 시 hit 영역·Transformer 앵커가 화면 기준으로 일정한지는 전부 시각·입력 판정이라 수동. 순수분(`viewport.ts` — fit/스톱/중앙앵커/팬 델타/줌 정규화/커서)은 `viewport.test.ts`가 커버.
- **녹화 중 그리기(펜/형광펜) — 실 녹화 합성·3초 페이드·그리기 툴바 노출·Esc 동기화·내비게이션 재표시** — `recording-annotation.spec`이 `annotation.show/setTool/hide` 메시지 직구동으로 오버레이 마운트·획 추가/미추가·pass-through·제거를 커버하지만, (a) 그린 획이 tabCapture/getDisplayMedia 스트림에 실제로 담기는지는 실 녹화(headed 자동화 불안정)라 수동, (b) 획이 ~3초 후 그린 순서대로 페이드아웃되는지는 타이머 시각 판정이라 수동, (c) RecordingState 그리기 툴바(색/펜·형광펜/두께) 노출·클릭→store→setTool 전송·crosshair는 실 녹화 진입(phase=recording)이 전제라 수동(오버레이가 받은 스타일을 획 path에 반영하는 부분은 `recording-annotation.spec`이 setTool 직구동으로 커버), (d) 페이지 Esc→툴 OFF 업스트림 동기화·녹화 중 내비게이션 재표시는 실 녹화+패널 조합이라 수동. 순수분(`pointsToPath`·`smoothPoint`·`overlayStrokeStyle`·`matchesOwnHost`·`annotationTool` 리셋)은 단위 테스트로 커버.
- **화면 전체 녹화**(화면 녹화 — `recordingMode="screen"`일 때의 `mode-record`) — `getDisplayMedia` 네이티브 picker가 자동화에서 안 떠(fake display 없이는 스트림 미획득) 클릭→녹화→drafting을 e2e로 못 탄다. **버튼 노출·1×2×2 DOM 구조·모드별 아이콘 전환은 `capture-modes-layout.spec.ts`가 커버**, 실제 녹화·source 라벨(탭/화면)·공유 중지·60초·picker 취소는 수동(tasks.md). getDisplayMedia가 side panel에서 동작하는지의 PoC도 수동 선행. idle 시각 정합(primary 강조·3행 정렬)·좁은 폭 라벨 truncate·리플레이 활성+인코딩 중 Row3 레이아웃도 수동.
- **단축키 4종**(`_execute_action`·capture-*) — Playwright는 확장 manifest commands를 발화할 수 없다(CDP 미지원).
- **permission-expired 다이얼로그** — activeTab 권한 만료 상태를 자동화로 재현하기 어렵다 (unsupported 분기만 커버).
- **광역 권한 보유 시 drafting cross-origin → deferred 만료 다이얼로그 부재** — 보존 세션 cross-origin은 keep(메시지 미발신)이라 "다이얼로그가 안 뜸"이라는 *부재* 단언이고, 가짜 패널 + deferred 메시지 broadcast는 SW로 관찰 불가. 분기 자체는 `resolveNavigationAction` 표 테스트(보존+커버→keep)와 `activetab-broad-permission.spec`의 비보존 keep 경로로 커버, 다이얼로그 부재는 tasks.md 수동 테스트로 검증.
- **광역 *미보유* 경로(닫힘·deferred)** — `<all_urls>`가 required host_permission으로 승격돼 광역 권한은 항상 보유 → 미보유 분기는 **프로덕션에 도달 불가**(과거 optional 시절의 경로). 순수함수 분기 자체는 `tab-bindings.test.ts`의 `legacyCases`(broadGranted=false)가 락인한다.
- **afterImage·캡처 썸네일 시각 정합** — 픽셀 비교라 스크립트 판정 불가. 행 수·색상 등으로 간접 단언만.
- **30s Replay 트리밍 전체** — 트림 오버레이는 `captureVisibleTab` 캡처로 진입해서 e2e(`replay-trim`·`replay-trim-logs`)가 환경 flaky로 **제거됨**. 로직은 단위 테스트로 커버한다: 초↔프레임 매핑·트림 경계(`trim-math` `secondsToFrameRange`/`frameOffsetsMs`/`previewTrimBounds`/`isTrimmedOut`), 가드밴드(`apply-trim`), 마커 환산·variant 필터(`buildErrorMarkers` positionPct·console error|warn / network 4xx·5xx·error·pending / action navigate). 오버레이 UI(아이콘 4탭·핸들 드래그/툴팁·muted=실제잘림 parity·재인코딩 영상 길이·에러 마커 위치·마커 클릭→탭 전환·로그 0건 empty·재생 게이팅)는 전부 **수동**(tasks.md).
- **action 레코더 pre-arm** — `DebugTab` subtab은 issue/console/network뿐이라 **idle에서** action 적재를 직접 관측할 창이 없다(로드 초반 action의 pre-arm 보존을 콘솔·네트워크처럼 서브탭에서 못 본다). 단, action 로그 **UI 자체는 더 이상 미커버가 아니다** — v1.5.8에서 스코프가 screenshot까지 넓어져 `action-log-scope.spec`이 스크린샷 drafting 경로로 카드·토글·기록 내용을 검증한다(과거 "video drafting 후 뷰에만 있어 검증 불가"는 stale). pre-arm `capturing` 게이트는 network/console과 **동일 코드 경로**라 `logs-prearm`이 메커니즘을 입증하고, action 고유분(input dedup의 `capturing` 게이트)은 단위/코드리뷰로 커버. 실 video 경로 pre-arm은 수동(tasks.md).
- **pre-arm 음성 케이스(미사용 origin은 안 잡힘)** — 패널이 열리면 `useBackgroundRecorder`가 자동 arm해 sessionStorage 플래그를 세팅하므로, "한 번도 armed 안 된 origin"을 e2e에서 깨끗이 재현하기 어렵다(비결정적 "부재" 단언). `isPreArmFlag`/`readPreArmFlag` 단위 테스트가 게이트 분기를, `logs-prearm` 양성 케이스가 효과를 커버.
- **action 로그 필터 탭 376px 가로 오버플로** — `ActionLogContent`에 `min-w-0 overflow-x-auto` 래퍼로 처리(6종 동시 present 시 스크롤). e2e는 `--lang=ko`라 필터 라벨이 2자(클릭·이동·입력·키·토글·선택)라 376px에도 그냥 들어가 스크롤 경로를 의미 있게 타지 않음(라벨이 긴 en에서만 발생). 구조 픽스는 리뷰로 검증, 시각 확인은 수동(tasks.md).
- **CC 멘션 필드** — CC 콤보박스는 제출 다이얼로그(`SubmitFieldsDialog`/`IssueCreateModal`)의 플랫폼 `IssueFields` 안에만 존재하고, 그 다이얼로그는 플랫폼 OAuth 연결 account 전제라 e2e에서 진입 불가(플랫폼 필드 UI 전체가 동일 사유로 미커버). 멤버 목록도 실제 플랫폼 API 의존이라 fixture 서버로 재현 불가. 순수 로직(`cc @이름` 조립·이스케이프·Asana sentinel 치환)은 `ccMention.test.ts` 단위 테스트로 커버. (단, **제출 버튼 활성/비활성 게이트만은 account를 storage seed해 우회**할 수 있다 — `clickup-submit-gating.spec` 참고. 콤보박스 자체는 여전히 미진입.)
- **ClickUp(및 전 플랫폼) 연결·제출 플로우** — `clickup.testPat`(연결 검증)·`getLists`/`getMembers`(콤보박스)·`submitIssue`(제출)는 전부 **background SW fetch**라 `panel.route`로 못 막는다(GOTCHAS AI mock은 panel-page fetch만 가로챔). 그래서 (a) PAT 실연결 → 연결 상태 표시, (b) List 콤보박스 채워 선택, (c) 실제 제출 성공/task URL은 **자동화 불가**. (b)·(c)의 순수 로직은 `submitToClickup.test.ts`(업로드 순서·logsDropped, sendBg mock)·`initialClickupFields.test.ts`(prefill 우선순위)로, OAuth env 가드(미설정 시 OAuth 옵션 숨김)는 공용 `connectMethods`/`isClickupOAuthConfigured` + typecheck로 커버. e2e는 **제출 게이트(`fieldsReady`)만** seed 우회로 커버.
- **Slack 연결·전송 플로우** — 위 ClickUp과 동일 사유. 실제 OAuth(`launchWebAuthFlow`), `listChannels`/`listMembers`(채널·멘션 콤보박스 SW fetch), `postMessage`/`uploadFiles`/`getPermalink`(전송·2-step 파일 업로드·permalink)는 e2e 불가. 순수 로직은 `submitToSlack.test.ts`(부모→스레드→첨부→permalink 순서·멘션 주입·logsDropped, sendBg mock)·`markdownToMrkdwn.test.ts`(mrkdwn 변환·escape)·`buildSlackBody.test.ts`·`slack-api.test.ts`(normalizeChannel·messageForSlackError·ok:false→SlackError)로 커버. e2e는 **제출 게이트(`channelId`)·connected 노출만** `slack-submit-gating.spec`이 seed 우회로 커버.
- **class diff 볼드 강조 — 이슈 본문 출력(6 빌더)** — 마크다운/GitHub/GitLab/Linear/Asana/Jira ADF/Notion 본문의 class 토큰 볼드 직렬화는 제출 다이얼로그(OAuth 연결 account 전제)에 가려 e2e 미진입(위 CC·첨부와 동일 사유). 순수 로직은 `classDiff.test.ts`(diffClassTokens/segmentsToMarkdown) + `buildIssueMarkdown`/`buildIssueAdf`/`buildNotionIssueBody` 통합 테스트로 커버. **UI 볼드(변경사항 다이얼로그)만 `style-changes-stacked`가 e2e 커버**.
- **CSS 뷰(CodeMirror) 시각·`!important` 실적용·타이핑 커서 점프 없음** — CodeMirror의 **신택스 하이라이팅·줄번호·자동완성(prop명 lang-css + 값 커스텀 completionSource) 시각·동작**, **lazy 로드 fallback(pulse 스켈레톤) 표시**, **Tab 키 포커스 이탈(`indentWithTab={false}`)**은 시각/상호작용이라 수동. `color: red !important`가 페이지 `!important` 규칙을 이기는지는 fixture에 그런 규칙이 없고 computed value가 일반 값과 구분 안 돼(priority는 CSSOM 내부 상태) e2e 판정 불가 — 적용 헬퍼 `applyInlineStyle` 접미사 분리는 단위로. 빠른 연속 타이핑 중 커서 점프 없음·cross-origin 늦은 specified 보강 시 포커스 가드도 시각/타이밍이라 수동(`lastCommittedRef` 재구성 문자열 재동기화는 `style-code-view` 폼↔CSS 왕복·버퍼 복원으로 간접 커버). serialize/parse round-trip·`computeOverrides`(삭제=initial·무편집 {})는 `cssBlock.test.ts`/`inlineCssText.test.ts` 단위 커버.
- **calc/clamp 길이 토큰(#9) 분류 e2e** — `categorizeToken`이 `clamp(1rem,2vw,3rem)` 등을 length로 분류하는 픽스는 단위 테스트(`css-resolve.test.ts > categorizeToken`)로 커버. e2e는 토큰 value가 `getComputedStyle`에서 clamp 원형으로 보존될지(브라우저가 resolve할 수도) 비결정적이라 생략. unitless 0/named-color는 결정적이라 `style-bugfix-regression`이 e2e 커버.
- **box-shadow 멀티레이어 편집(#2/#8) 회귀** — `setLayer`의 `filter(Boolean)` collapse·붙여넣기 잘림은 인덱스 기반 레이어 모델의 구조적 한계로 **버그픽스에서 스킵**(레이어 id 모델 재설계는 별도 작업). 미수정 항목이라 e2e 대상 아님. 단일 레이어 box-shadow는 정상.
- **파일 첨부 — 실제 플랫폼 업로드·본문/네이티브 노출** — 6개 플랫폼 어댑터로의 첨부 업로드(GitHub/GitLab 본문 링크·Linear createAttachment·Notion file block·Jira/Asana 네이티브)는 OAuth 연결 account + 제출 다이얼로그 전제라 e2e 미진입(위 CC 항목과 동일 사유). 합류 로직은 `buildCaptureFiles.test.ts`(filename 고유화/displayName)·`attachmentLimits.test.ts` 단위 테스트로, IndexedDB rekey/정리는 수동(tasks.md). `attachments.spec.ts`는 drafting까지의 UI·세션 영속만 커버. **플랫폼 단건 한도 경고 배지(Notion 5MiB/GitLab 10MB 초과 시각)**도 한도 초과 파일 준비가 필요해 수동.
- **cross-origin 스타일 보강(양성 경로) — specified author 값 채움·source 셀렉터 라벨·`var()` 해석·부분 보강 fallback·cross-origin-only 토큰 swatch hint** — background가 외부 cross-origin CSS를 fetch해 빈 specified를 채우는 양성 경로는 e2e로 재현 불가. e2e fixture 서버는 loopback 전용(127.0.0.1 top + localhost sheet로 origin 분리)인데, **SSRF 가드(`isFetchableSheetUrl`)가 localhost·127.x를 loopback으로 차단**(보안상 정상)해 보강 fetch가 안 일어난다. 가드를 통과하는 cross-origin 호스트(공개 도메인/IP)가 loopback 서버엔 없음. 가드 로직은 `ssrf-guard.test.ts`(35 케이스: 공개 통과 / loopback·link-local·RFC1918·IPv6 ULA·IPv4-mapped·FQDN 후행점 차단), 머지·인덱싱·var 참여는 `css-resolve.test.ts > mergeCrossOriginDecls`·`css-source-cache.test.ts > indexCrossOriginRules`, fallback 분기는 `sectionDefaultOpen.test.ts`(부분 보강 collapse·실패 시 computed 유지), stale 가드는 `editor-store.test.ts`로 커버. **cross-origin :root에만 정의된 custom prop이 토큰 목록에 들어가 swatch/hex hint가 뜨는 경로(`collectTokens`가 `getCrossOriginCustomProps()`를 same-origin 우선 merge)도 같은 SSRF 차단으로 e2e inert** — `getCrossOriginCustomProps()`가 `{}`라 `mergeCrossOriginTokens`가 no-op. 머지 헬퍼는 `css-resolve.test.ts > mergeCrossOriginTokens`(5 케이스: 추가·same-origin 우선·부분 충돌·빈 입력·비-- 방어)로 직접 커버. 실 보강(공개 CDN·naver)은 수동(tasks.md). **graceful fallback(가드 차단 → computed 유지, 에러 없음) 회귀는 `style-cross-origin-section.spec`이 새 코드에서 green으로 커버.**
