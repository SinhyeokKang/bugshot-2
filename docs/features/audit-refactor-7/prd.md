# audit-refactor-7 — 전체 감사 28건 배치

## 배경

`/audit` 전체 감사(4개 전문 에이전트 · 고유 약 480파일 정독)가 **71건**을 냈다: 🔴 1 · 🟡 33 · ⚪ 37. 이 배치는 그중 **27건** + `docs/POSTMORTEM.md` 이월 1건, 합계 **28건**을 다룬다.

전 배치(`audit-refactor-4/5/6`)와 같은 성격이다 — 개별로는 작지만 흩어져 있어 각각을 따로 손대면 컨텍스트 로딩 비용이 수정 비용을 넘는다. 묶어서 **논리 단위(G그룹)**로 재편하고, 그룹 안에서 같은 파일·같은 그물을 공유한다. (P0/P1/P2 + G그룹 라벨링을 쓴 건 `audit-refactor-6`뿐이다 — ar-4는 flat `Task 1~14`, ar-5는 소주제 A~F 9태스크였다.)

이번 감사가 직전 배치들과 다른 점은 **에러 경로에 몰려 있다**는 것이다. 🟡 33건 중 11건이 "예외가 나면 응답이 영영 안 온다 / 실패가 성공으로 판정된다 / 마스킹 게이트가 우회된다" 계열이다. 이건 정상 경로에서 안 보이고 사용자에게는 "가끔 멈춘다"로만 나타난다. 그 11건 중 응답 채널이 실제로 매달리는 3건(#5·#7·#11)과 Privacy 게이트 우회 1건(#12)을 P0에 두고, 계약 위반 3건(#2·#3·#4)은 사용자 손실이 "잘못된 안내"에 그치므로 P1이다.

### 스코프 결정 근거 (71건 → 28건)

| 컷 | 건수 | 사유 |
|---|---|---|
| ⚪ 37건 제외 | −37 | 다음 배치로 미룬다. 배정은 아래 "⚪ 배치 지도" 참조. |
| DROPPED 재소환 2건 제외 | −2 | #24 `dataUrlToBlob`·#31 `created` 항상-true. 아래 "드랍 소환" 참조. |
| 클론군 2건 제외 | −2 | #26 배지 셸 7벌·#27 PatDialog 6벌. 아래 "위험 요소" 참조. |
| **검수에서 드랍 3건** | **−3** | #21 `DomTreeDialog`·#28·#29 콤보박스 수렴. 아래 "검수 드랍" 참조. |
| **회고 이월 1건 편입** | **+1** | `ActionLogContent` 톤 드리프트(POSTMORTEM 2026-08-14 이월). 목표 5에 정확히 해당하는데 감사가 ⚪로 분류해 스코프 밖에 있었다. |
| **잔여** | **28** | 🔴 1 + 🟡 26 + 이월 1 |

### ⚪ 배치 지도 (다음 배치 사전 배정)

**이 표가 없으면 항목이 사라진다.** 배치 문서는 완료와 함께 삭제되고(ar-4는 `7c5e1cf`, ar-5·6은 `f456445`) 감사 리포트는 저장소 파일이 아니다 — `docs/POSTMORTEM.md:167`이 "배치 문서는 수명이 짧아 교차 참조가 남으면 끊긴다"를 기록한 그 함정이다. 실패 실증도 있다: `editor-store`의 `annotation/presets` value import는 ar-5가 이월로 남겼고 ar-6이 안 걷었고 이번에 **#8로 세 번째** 등장했다.

| 배치 | 대상 | 건수 |
|---|---|---|
| `audit-refactor-8` | ⚪ 중 **문서·주석·미사용 export** 계열 (검증이 "grep 0"으로 끝나는 것들) | 약 18 |
| `audit-refactor-8` | 이번 검수가 새로 드랍한 3건(#21·#28·#29) + 아래 "검수 신설" 3건 | 6 |
| `audit-refactor-9` | ⚪ 중 **코드 수정이 필요한** 계열 (i18n 사각·접근성 잔여·클론군) | 약 19 |
| 조건부 | #24·#31 (DROPPED 조건 충족 시) · #26·#27 (추출 대상 재설계 시) | 4 |

### 검수 드랍 (`/feature-review` 결과 — 3건)

착수 전 검수에서 **"수렴"이 아니라 "동작 변경"임이 드러난 3건**을 뺀다. 사유는 design.md "대안 검토" F·G에 있고, 배치 종료 시 Task 13-1이 `DROPPED.md`에 옮긴다.

| 항목 | 드랍 사유 | 다시 볼 조건 |
|---|---|---|
| #21 `DomTreeDialog` raw button → shadcn | 근거로 든 "DESIGN §10 등재 예외 4계열"이 **DESIGN.md에 없다**(실제 2계열). 교체하면 행 높이 +4px(`h-9` vs 헤더 최대 32px)·텍스트 폭 −32px(`px-4`)·**`truncate` 파손**(`inline-flex`로 익명 flex item이 되어 하드 클립)·hover가 `opacity-70`→면색 블록. 이건 버튼이 아니라 sticky 헤더의 **클릭 가능한 제목**(h1 자리)이고, 이행하려면 override 4종이 원본 className보다 길어진다. e2e는 testid만 잡아 이 회귀를 **아무 테스트도 못 잡는다** | DESIGN.md에 "클릭 가능한 제목"을 raw button 예외 5번째 계열로 등재하면 항목 자체가 소멸한다 → **Task 11-5가 그 등재를 수행한다** |
| #28 `SingleLazyCombobox` 미채택 6파일 | `DESIGN.md:270`이 이 파일들을 **"로드 모델이 달라 의도적으로 밖에 있다"**고 이미 등재해 뒀다. 게다가 이행은 동작 변경 다발이다 — ① `shouldFilter={false}`+수동 `includes()`로 매치 집합·순서 변경 ② `tabs/ProjectCombobox`는 `JiraAccount`에 `projectName`이 없어 트리거가 `"Name (KEY)"`→**`"KEY"`로 퇴화** ③ linear `LabelCombobox`는 로드 목록 우선 라벨(`items.find(...)?.name ?? valueName`)이 저장 이름 고정으로 퇴화 ④ `useLazyListOnOpen`은 `load` identity로 리셋해 부모가 `useCallback`을 안 쓰면 무한 refetch. 대상 6파일 테스트 0·e2e 0 | 아래 "검수 신설" ①이 먼저 고쳐진 뒤 |
| #29 원격 검색 3벌 → `useDebouncedSearch` | 동치 통합이 아니다. 실측 디바운스는 **250/250/300ms**(문서는 "300ms 3벌")이고, 3파일은 `setLoading(true)`를 타이머 **안**에서 부르는데 훅은 타이머 **앞**(leading edge)에서 부른다 → 이행 후 키 입력마다 목록이 스피너로 교체돼 400px 패널에서 깜빡인다. 훅엔 `if (!open) return` 게이트도 없고 `items` reset도 노출하지 않는다(`JiraIssueFields.tsx:215`가 `key` remount로 우회 중). tasks 원안의 "호출부를 훅에 맞춘다"는 그 자체로 동작 보존 포기 선언이었다 | 훅이 `open` 게이트·`items` reset·loading 타이밍 옵션을 갖게 되면 |

### 검수 신설 (다음 배치 후보 — 3건)

검수 과정에서 **감사가 못 잡은 결함**이 셋 나왔다. 이번 배치에 넣지 않는다(각각 별 사정거리).

| 항목 | 내용 |
|---|---|
| ① `SingleLazyCombobox`의 `query` 리셋 부재 | `:57`이 `query`를 컴포넌트 state로 들고 `:95 shouldFilter={false}` + 수동 필터(`:60-63`)를 돌리는데 `setQuery("")` 리셋이 **어디에도 없다**(`:69 onOpenChange`에도 없음). 컴포넌트는 팝오버와 수명이 달라 살아남으므로 **재오픈 시 입력창은 비어 보이는데 목록은 stale query로 필터된 채**다. POSTMORTEM 2026-07-14("검색어 state 수명 불일치")를 리셋이 *안 오는* 방향으로 재현한 것이고 **이미 11파일이 이 컴포넌트를 쓴다**. #28보다 이득이 크다 |
| ② `scroll-capture` 응답 shape 무검증 계열 | 이번 배치는 `scrollCaptureTo` 한 경로만 shape 가드를 넣는다(Task 1-1). `sendAll`/`send`가 `catch { return undefined }`로 `lastError`를 지워 **포트 닫힘과 의도적 `undefined`가 구별 불가**한 구조 자체는 남는다 |
| ③ 3벌째 seq-guard 사본 | `useLazyListOnOpen.ts:18`·`NotionConnectForm.tsx:74`가 같은 `reqIdRef` 패턴의 4번째·5번째 사본인데 #29 대상에 없었다 |

### 드랍 소환 (`docs/features/DROPPED.md`)

착수 전 grep에서 **3건이 이미 드랍된 항목**으로 걸렸고, 세 건 다 "다시 볼 조건"이 미충족이라 이번 배치에서 제외한다.

| 감사 항목 | 드랍 기록 | 다시 볼 조건 | 현재 |
|---|---|---|---|
| #24 `dataUrlToBlob` 통합 | audit-refactor-6 ④ "동치가 아니다" | notion 첨부 입력이 base64로 좁혀졌음을 **실측**할 수 있을 때 | 미충족 — 실측 수단 없음. (감사 리포트는 "3벌"이라 했지만 실제 정의는 **2벌**이다: `store/blob-db.ts:732`·`background/notion-api.ts:292`. `DROPPED.md:27`도 2벌로 적었다 — POSTMORTEM 2026-07-16의 "이름이 아니라 본문으로 세라"를 그 표에서 밟은 것이라 여기서 정정한다.) |
| #31 `ensureGithubTab`의 항상-true `created` | audit-refactor-6 ⑥ | 같은 파일을 다른 이유로 손대며 **실탭 회귀를 어차피 돌릴 때** | 미충족 — G5가 `github-upload.ts:139`를 손대지만 그건 **함수 반환 타입**이고 `executeScript({func: pageBatchUploadFn})` 주입부(`:158`)는 안 건드린다. 조건이 요구하는 "실탭 회귀"는 업로드 성공/실패 판정 분기 확인(design.md 위험 ⑤)뿐이라 주입 함수 회귀는 돌지 않는다. |
| #59 `useT()`의 렌더 중 전역 write | audit-refactor-6 ① 인접 | log-viewer가 `@/i18n` alias를 안 쓰게 되거나 alias가 정확 매칭이 될 때 | 미충족 (애초에 ⚪라 스코프 밖). 조건에 붙은 필수 검증 조항 — **"그때도 `pnpm build:log-viewer`를 반드시 넣는다"**(`DROPPED.md:16`) — 을 함께 소환해 둔다. |

> 항목번호는 **감사 회차별 로컬 번호**다. ar-6의 🟡59는 `ContentMessage`, 이번 #59는 `useT`로 서로 다르다. #24·#31은 ar-6에서 ⚪94/⚪95·96이었으나 이번 감사가 🟡로 재판정했다(그래서 30 = 🔴1 + 🟡29 산식에 들어 있었다).

**이 세 건이 재감사에서 또 나왔다는 사실 자체**를 `DROPPED.md` 해당 항목에 한 줄씩 추기한다(Task 13-1 — 세 건 전부, #59/① 포함). 안 적으면 다음 감사가 같은 계산을 네 번째로 한다.

## 목표

각 목표에 담당 항목번호를 병기한다. **합이 28이어야 한다** — 성공 기준 1은 "항목↔태스크"만 대조하므로, 목표가 항목을 못 덮는 불일치는 이 병기가 유일한 그물이다.

1. **응답 채널이 예외로 매달리는 경로를 없앤다** — #5 · #7 · #11. `picker.scrollCaptureTo`·css 캐시 로드·picker 보강 IIFE 3계열에서, 예외가 나도 사이드패널의 `await`가 유한 시간에 풀린다.
2. **실패를 성공으로 읽는 응답 계약을 고친다** — #2 · #3 · #4 · **#5의 shape 축**. OAuth 에러 레인 오분류(2 갭), 토큰 응답 무검증(2 갭), 업로드 결과 판별자 4종, 그리고 `{ok:false}`가 truthy라 성공으로 읽히는 경로.
3. **마스킹 게이트 우회를 막는다** — #12. `aria-labelledby`가 공백 구분 다중 ID일 때 라벨을 못 구해 민감 필드가 액션 로그에 원문으로 실리는 경로.
4. **user gesture가 첫 await에 소실되는 경로를 없앤다** — #6.
5. **`store`가 사이드패널 컴포넌트 트리를 참조하지 않게 한다** — #8 · #9. value import 2건 제거 — typecheck도 테스트도 안 잡는 축이라 지금 끊는다.
6. **드리프트가 이미 발생한 단일 출처를 합친다** — #22 · #23 · #25 · #30 · **M1**(회고 이월: `ActionLogContent` 톤). `escapeHtml`·`imageCell`·`MarkdownIt` 설정·frozen phase 집합·로그 톤 색상.
7. **경합으로 화면이 되살아나는 경로를 직렬화한다** — #10.
8. **컨벤션·접근성 이탈 국소 수정** — 🔴#1 · #13 · #14 · #15 · #16 · #17 · #18 · #19 · #20 · #32 · #33 · #34 (12건).
9. **회귀를 재발 방지 그물로 고정한다** — 위 전부에 걸린 횡단 목표. 새 순수 함수·헬퍼는 테스트 선행, 브라우저 실동작에 걸린 것만 수동으로 남긴다.

> 합계: 3 + 4 + 1 + 1 + 2 + 5 + 1 + 12 = **28**. (#5는 목표 1·2에 걸치므로 목표 1에서만 센다.)

## 비목표 (Non-goals)

- **⚪ 37건** — 이번에 안 한다. 위 "⚪ 배치 지도"가 배정을 갖고 있다.
- **콤보박스 수렴(#28·#29)·`DomTreeDialog` 이행(#21)** — 위 "검수 드랍". #21은 항목을 고치는 대신 **DESIGN.md에 예외로 등재**해 항목 자체를 소멸시킨다(Task 11-5).
- **배지 셸 7벌 추출(#26)·PatDialog 6벌 추출(#27)** — 아래 "위험 요소" 참조.
- **`dataUrlToBlob` 통합(#24)·`created` 정리(#31)** — DROPPED 조건 미충족.
- **`src/sidepanel/**` 전역 import 표기 통일** — #34는 **이탈 2건(background 1파일 1줄 · types 1파일 3줄) + 신규 유입 차단 그물**까지만 한다. sidepanel 전역 일괄 치환은 이 배치의 국소 수정 성격과 규모가 다르다(design.md 대안 C).
- **`SingleLazyCombobox`의 `query` 리셋 픽스** — 위 "검수 신설" ①. #28을 드랍했으므로 이번 배치가 그 컴포넌트를 건드리지 않는다.
- **i18n 네임스페이스 재편·토큰 값 변경·`t()` 확장** — audit-refactor-5가 이미 기각했고 이번 감사가 그 판정을 뒤집을 새 근거를 내지 않았다.
- **`BgResponseMap` 도입** — #4를 근본적으로 고치려면 `BgRequest["type"]` → 응답 타입 map으로 `sendBg`가 추론하게 해야 한다. 이번엔 양단 명시 + 목 갱신으로 국소 처리하고, map은 별 배치로 남긴다(design.md 대안 G).

## 사용자에게 보이는 변화

**"동작 변경 없음"으로 뭉뚱그리지 않는다** — `audit-refactor-5/prd.md:5`가 그 이유를 못박았다. 이 배치의 노출 변화는 **8건**이고 축이 셋이다.

| 항목 | 축 | 지금 | 변경 후 |
|---|---|---|---|
| 🔴#1 | 시각 · 접근성 | 비활성 제출 버튼에 이유가 안 뜬다(`disabled:pointer-events-none`이 hover를 죽여 `title`이 절대 안 뜸) | 툴팁으로 "연동된 플랫폼이 없습니다"를 읽는다. 키보드 사용자는 `aria-disabled`라 포커스를 받고 스크린리더가 상태와 이유를 함께 읽는다 |
| #17 | 시각 | Linear 상태 팝오버가 **빈 채로** 뜬다(로딩 스피너가 사라진 뒤 아무것도 없음) | Jira(`issueList.jira.noTransitions`)·Notion과 같은 형태의 안내 문구 |
| #5 | 시각 | 페이지 전체 캡처 중 content가 throw하면 **영원히 대기**하거나(settle 경로) **빈 밴드가 있는 이미지가 성공으로 스티치**된다(`{ok:false}` truthy 경로) | 둘 다 "캡처 실패"로 정상 종료 |
| #12 | **본문 내용** | 다중 `aria-labelledby` 폼에서 민감 필드 값이 **원문으로** 액션 로그에 실려 이슈 본문으로 나간다 | `***`로 마스킹 |
| #6 | 시각 | 인라인 이미지가 많으면 마크다운 복사가 **무음 실패**한다(포커스가 IDB 왕복 중에 빠지면 `write`가 죽고 폴백도 같이 죽음) | 복사가 성공한다 |
| #2 | 시각 | 연동한 적 없는 사용자에게 **"세션 만료" 배너**가 뜬다(최초 연결 실패가 401 레인으로 오분류) | 일반 연결 실패로 처리. **역방향 위험**: 진짜 refresh 실패가 400으로 내려가면 배너가 *안* 뜬다 → design.md 위험 ⑥ |
| #19 | 접근성 | `JsonTreeViewer`의 "n개 더"·"모두 보기"가 `<div>`/`<span>` onClick이라 **키보드로 도달 불가** | `<button>`이라 Tab·Enter·Space로 동작 |
| M1 | 시각 | 액션 로그 DOM 칩의 태그명 `sky-600/400`, 속성값 `red-700/400` (로컬 리터럴) | `TONE_TEXT`로 수렴해 태그명 `blue-600/400`, 속성값 `red-600/400`. **색이 실제로 바뀐다** — 톤 표와 값이 갈려 있었기 때문이다. 대비는 태그명이 개선(`bg-muted` 위 3.72:1 → 4.72:1, 원래 AA 미달이었다), 속성값은 하락(5.95:1 → 4.41:1, AA 4.5 미달). 후자는 앱 전역 `TONE_TEXT.red`와 같은 값이라 **이 자리만 좋게 두는 대신 전역과 일치**시키는 선택이다 — 대비를 올리려면 `TONE_TEXT.red` 자체를 손대야 하고 그건 로그 표면 전역 변경이라 별건이다 |

**부수 접근성 변화 2건** — #18(`PropertySelectCombobox`에 접근 이름이 생겨 스크린리더가 속성명을 읽는다)·#13/#14(문서 언어 선언이 실제 로케일과 일치해 스크린리더 발음·폰트 선택이 맞아진다). 화면은 안 바뀐다.

**나머지 21건은 "같은 화면·같은 결과, 다른 경로"다.**

## 성공 기준

1. 28개 항목이 **전부** 태스크에 배정돼 있고, `tasks.md`의 **항목↔태스크 대조표**에서 미배정 항목이 0이다.
   - 이 조항은 장식이 아니다. `docs/POSTMORTEM.md` 2026-08-16 항목이 "design.md가 편입했다고 명시한 항목 셋이 tasks.md 어느 태스크에도 없어, 태스크만 보고 구현했으면 PRD 약속과 결과가 조용히 어긋난 채 배치가 닫혔을 것"이라고 기록한 바로 그 함정이다.
2. `pnpm typecheck` · `pnpm test` green.
3. `pnpm test:e2e` green — **기존 스위트 무회귀 + 신규 2건**(G3의 `text/html` flavor 검증 확장, G4의 툴팁 노출). "페이지 전체 캡처 실패" 시나리오는 **유도 수단이 없어 e2e에서 뺀다**(design.md 위험 ④).
4. **드리프트 제거는 grep으로 증명한다** — "단일 출처로 올렸다"는 주석이 거짓인 채 머지된 전례(POSTMORTEM 2026-07-16)가 있으므로, 각 통합 태스크의 검증 항목에 잔존 사본 수를 세는 grep을 박는다. **단 그 grep은 정당한 비-대상을 세지 않아야 한다** — 원안의 3개(`replace(/&/g)`·`reqIdRef`·`gid: null`)가 성립 불가였다.
5. 로직 스코프 커버리지가 이번 배치 후 하락하지 않는다(`pnpm coverage:report` 기준).
   - **베이스라인은 82.34%**(`coverage/baseline.json`의 `logic.pct` — 원안의 82.7%는 존재하지 않는 수치다). 단 그 파일은 2026-08-14 생성이고 이후 추가된 로직 스코프 파일 몇 개가 미등재라 **오늘의 실측 %는 착수 시점에 다시 잰다**(Task 0의 첫 단계).
   - 신규 파일 중 `sidepanel/picker-clear.ts`는 **`BROWSER_BOUND_EXACT`에 등록해야** 한다 — 안 하면 ~0%로 분모에 들어와 이 기준을 자기 손으로 깬다(Task 6-2).
6. 신규 순수 함수·헬퍼는 **테스트 선행**으로 들어간다(CLAUDE.md 테스트 우선).
7. **컴포넌트 테스트 3벌은 파일 신설이다** — `IssueCreateModal.test.tsx`·`LinearStatusBadge.test.tsx`·`useThemeEffect.test.tsx`가 존재하지 않는다. "케이스 추가"로 비용을 과소평가하지 않는다. 특히 🔴#1의 **유일한** 그물이 `IssueCreateModal`(531줄·28 import)이라 `vi.mock` 하네스가 필요하다(선례 `IssueTab.test.tsx:5-16`).

## 위험 요소 (PRD 수준)

**클론군 2건을 제외한 근거 — 판정 기준 "사정거리가 이름값보다 좁은가".**
`DROPPED.md` audit-refactor-6 ③이 `ConnectButton` 추출을 기각한 논리가 그대로 적용된다.

- **#27 PatDialog 6벌**: `{Asana,Github,Gitlab,Clickup}ConnectForm`의 `PatDialog`와 `{Linear,Jira}ConnectForm`의 `ApiKeyDialog`는 골격이 같지만, Jira는 이미 `PlatformConnectFlow`를 안 쓰고 레거시 메시지명(`oauth.start`)을 유지하는 별종이고(⚪43), Slack은 술어가 다르다. **6벌 중 4만 걷고 패턴 중복은 남긴다** — 추출 자체가 새 비대칭을 만든다.
- **#26 배지 셸 7벌**: audit-refactor-6 G5가 이미 `BadgeFallback`(공통 폴백 배지)을 뽑아갔고, 남은 63~106줄은 파일마다 메시지 타입·patch 필드가 다르다. 추출 대상을 새로 설계해야 하는 작업이지 "이미 있는 공용으로 수렴"이 아니다.
- **검수 결과 #28·#29도 같은 판정으로 넘어갔다.** 원안은 "`SingleLazyCombobox`가 이미 있고 이미 쓰는 파일이 있으니 수렴"이라 봤지만, `DESIGN.md:270`이 대상 파일들을 의도적 제외로 등재해 뒀고 이행이 동작 변경 4종을 끌고 온다. **"지배 패턴이 저장소에 있다"만으로는 부족하고, "대상이 그 패턴의 전제(로드 모델)를 공유하는가"까지 봐야 한다** — 이번 배치가 얻은 판별 기준이다.

**#34의 사정거리도 같은 축에서 좁혔다.** sidepanel은 `@/sidepanel/`와 상대경로가 혼용이고 혼용 파일이 수십 개다. 어느 쪽으로 통일해도 수백 개 import 문을 건드리고, 그 diff 안에서 진짜 회귀를 못 본다. 이탈이 명백한 2건(`background/lib/createRefreshRunner.ts` 1줄, `types/picker.ts` 3줄 — 각각 해당 패키지에서 **유일한** 이탈)만 고치고, 나머지는 신규 유입만 소스 스캔 테스트로 막는다. **원안이 적었던 측정치(441/502/혼용 70)는 재현되지 않는다**(재측 457/806/79) — 방법론이 기록되지 않아 검증 불가한 숫자였으므로 수치를 문서에서 뺀다.

**나머지 위험은 design.md "위험 요소"로 내린다** (번들 경계 검증 수단 부재, 골든 스냅샷 게이트의 공허함, `withLocale` 구간 store write, 브라우저 실동작 미검증 3건, `refreshFailed` 역방향 회귀, 계획-태스크 불일치).

## 가이드 영향

**없음.** 사용자 노출 변화 7건은 전부 "원래 그래야 했던 것"이라 가이드에 새로 설명할 기능이 아니다. 🔴#1의 툴팁 문구는 기존 i18n 키(`platform.empty.title`)를 그대로 쓴다(현재 `title` 속성이 이미 그 키를 쓰고 있어 i18n diff가 0이다). #17만 신규 키 1개(`issueList.linear.noStates`)가 필요하다.

## Privacy 영향

**문서 변경 불필요 — 단, 이 배치가 유일하게 "문서가 코드보다 앞선" 항목을 담고 있다.**
`docs/privacy.ko.md:76`이 `aria-labelledby`로 연결된 라벨을 마스킹 근거로 **이미 공언**하고 있다. 즉 #12는 방침이 실제보다 넓은 보호를 주장하는 상태를 코드가 따라가는 수정이고, 고친 뒤에야 그 문장이 사실이 된다. 새 캡처·수집·저장·전송 동작은 없다.
