# 어노테이션 캔버스 줌

## 배경

v1.5.6에서 페이지 전체 캡처(scroll capture)가 들어오면서, 어노테이션 오버레이가 감당해야 하는 이미지의 종횡비가 크게 달라졌다.

현재 `AnnotationOverlay`는 이미지를 **폭·높이 모두 가용 영역에 맞추는 배율**(`fitScale`, 최대 1 — 확대 불가) 하나로만 표시한다. 캡처 파이프라인은 `MAX_OUTPUT_PIXELS = 4_000_000` 캡을 걸어 최종 이미지를 다운스케일하므로, 세로로 긴 페이지 전체 캡처는 예컨대 1074×3730 같은 이미지가 된다. 사이드패널 폭(400px 기준)에서 이 이미지의 표시 배율은:

- 현행 fit-all: `min(368/1074, 0.7×800/3730, 1)` = **약 15%**
- fit-width: `min(368/1074, 1)` = **약 34%**

15%에서는 본문 텍스트도, 버그가 난 UI 요소도 식별되지 않는다. 어노테이션(화살표·박스·텍스트)을 **정확한 위치에 찍는 것 자체가 불가능**하다. 확대 수단이 없으면 페이지 전체 캡처는 사실상 어노테이션할 수 없는 이미지다.

## 목표

- 어노테이션 오버레이의 캔버스를 사용자가 확대·축소할 수 있다. 배율 범위는 맞춤(fit-width) ~ 400%.
- 확대 상태에서도 그리기·선택·변형·텍스트 입력이 원본 좌표 기준으로 정확히 동작한다.
- 진입 시 기본 배율을 fit-width로 바꿔, 페이지 전체 캡처가 처음부터 두 배 크게 보이게 한다.
- 배율을 조작한 상태임을 사용자가 인지하고 한 번에 되돌릴 수 있다.
- 확대·축소해도 **완료(Done) 결과물의 해상도는 변하지 않는다** — export는 항상 원본 natural 해상도.

## 비목표 (Non-goals)

- Ctrl/Cmd + 휠 줌, 핀치 줌, 키보드 단축키(+/-/0). 이번엔 버튼·콤보박스만.
- 스페이스+드래그 팬. 확대 시 이동은 캔버스 컨테이너의 기본 스크롤로만 한다.
- 커서 위치 기준 확대(zoom-to-cursor). 앵커는 뷰포트 중앙 고정.
- 배율의 세션 영속화. 오버레이를 닫으면 배율 상태는 사라진다.
- 녹화 중 그리기 footer(`IssueTab`의 `recording-pen` 경로)와 element before/after 비교 뷰. 줌 대상 아님.
- 전체 맞춤(fit-all) 프리셋. 페이지 전체 캡처를 15%로 보는 조망은 아무것도 식별되지 않아 가치가 낮다.

## 사용자 시나리오

### 기본 플로우 — 페이지 전체 캡처 어노테이션

1. 사용자가 페이지 전체 캡처로 긴 페이지를 찍고, 미리보기에서 **주석 편집**을 누른다.
2. 어노테이션 오버레이가 열린다. 이미지는 **fit-width 배율**(예: 34%)로 표시되고, 캔버스 우상단 줌 컨트롤 라벨에 `34%`가 보인다. 세로는 넘치므로 캔버스 영역을 스크롤해 아래쪽을 볼 수 있다. 좌상단 맞춤 버튼은 **보이지 않는다**(맞춤 상태이므로).
3. 버그가 난 영역이 작아서 잘 안 보인다. 사용자가 `[+]`를 눌러 50% → 75% → 100%로 확대한다. 확대할 때마다 **화면 중앙에 보이던 지점이 그대로 중앙에 남는다.**
4. 배율이 맞춤에서 벗어났으므로 **좌상단에 맞춤 버튼이 나타난다.**
5. 100%에서 화살표 도구로 버그 요소를 가리키고 텍스트를 단다. 도형은 원본 좌표에 정확히 찍힌다.
6. 전체를 다시 보려면 좌상단 맞춤 버튼을 누른다 → 배율이 34%로 돌아가고 맞춤 버튼이 다시 사라진다.
7. **완료**를 누르면 원본 해상도(1074×3730) webp가 나온다. 배율은 결과물에 영향을 주지 않는다.

### 콤보박스로 배율 점프

- 줌 컨트롤 가운데 `34% ▾`를 클릭하면 프리셋 목록이 열린다: `맞춤(34%)` · `50%` · `75%` · `100%` · `150%` · `200%` · `300%` · `400%`.
- **fit 배율보다 작은 프리셋은 목록에 나타나지 않는다** — fit보다 축소하면 이미지가 가용 폭보다 작아져 의미가 없다. 위 예에서는 fit이 34%라 25% 항목이 빠진다.
- `맞춤`을 고르면 좌상단 맞춤 버튼을 누른 것과 같다.

### 엣지 케이스

- **작은 이미지(요소 캡처 등)**: natural 폭이 가용 폭보다 작으면 fit-width는 1(=100%)로 클램프된다. 프리셋 목록은 `맞춤(100%)` · `150%` · `200%` · `300%` · `400%`가 되고, 25~75%는 fit보다 작으므로 빠진다. 진입 화면은 현행과 동일하다.
- **패널 폭 변경**: 사용자가 배율을 조작한 상태(맞춤 아님)에서 사이드패널 폭을 바꿔도 **배율은 유지된다.** 맞춤 상태라면 새 폭에 맞춰 자동 refit되고, 라벨 숫자만 바뀐다.
- **텍스트 입력 중 줌 조작**: 텍스트 박스 입력 중 줌 버튼을 누르면 textarea가 blur되어 입력이 먼저 커밋된 뒤 배율이 바뀐다. 좌표가 어긋난 textarea가 남지 않는다.
- **배율 경계**: 맞춤 상태에서 `[-]`, 400%에서 `[+]`는 비활성(`disabled`).
- **확대 후 좌우 스크롤**: 이미지가 가용 폭보다 넓어지면 좌우로도 스크롤된다. 콘텐츠가 컨테이너보다 클 때 왼쪽·위쪽이 잘려 도달 불가능해지지 않아야 한다.

## 성공 기준

- 페이지 전체 캡처 이미지에서 100% 이상으로 확대해 본문 텍스트를 읽을 수 있고, 그 상태로 찍은 화살표·박스가 완료 후 원본 이미지의 같은 위치에 남는다.
- 진입 시 페이지 전체 캡처의 표시 배율이 fit-width(현행 대비 약 2배)다.
- 맞춤 버튼이 "배율을 조작한 상태"에서만 노출되고, 누르면 맞춤 배율로 정확히 복귀한다.
- 확대·축소 시 뷰포트 중앙 지점이 유지된다.
- 완료 결과물의 해상도가 배율과 무관하게 원본 natural 크기다.
- 기존 어노테이션 e2e(`annotation-overlay.spec.ts`)가 그대로 통과한다.
