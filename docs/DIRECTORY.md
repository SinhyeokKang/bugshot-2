# DIRECTORY.md

디렉터리 구조 + 파일별 역할. 테스트 파일은 대상과 같은 디렉터리의 `__tests__/*.test.ts(x)`에 위치.

```
src/
├── assets/icons/    # 확장 아이콘 리소스
├── background/      # service worker
│   ├── index.ts         # 메시지 라우터 + 전역 sidePanel 비활성화
│   ├── tab-bindings.ts  # 탭별 side panel on/off (활성화 셋 기반)
│   ├── jira-api.ts      # Jira REST 래퍼 (extractJiraDetail — 400 응답의 errors 객체를 사용자 문구로 매핑하는 순수 헬퍼. errors.assignee는 jira.error.assigneeNotAssignable 안내로 치환[유저 검색은 사이트 전역이나 assignable은 프로젝트 권한 스코프]) (Basic + Bearer, 401 시 refresh 재시도[기본값 — `jiraFetch(…, retryOn401=false)`로 끌 수 있고 agile 3경로가 그렇게 쓴다: scope 미비 401은 영구 조건이라 재시도가 회전형 refresh token만 태우고 그 OAuthError가 전역 '세션 만료' 안내를 오발화한다], getTransitions/transitionIssue로 상태 변경) + 스프린트: pickSprintField/mergeBoardSprints(순수 — createmeta 응답에서 gh-sprint 필드 판정[봉투 키는 `values`가 아니라 `fields`] / 보드별 결과 dedup·정렬, multiBoard는 결과에 남은 distinct boardName 수), getSprintFieldMeta(createmeta 상세, maxResults=200 무페이지네이션)·listSprints(board 목록 → kanban 제외 → 상위 5개 allSettled 팬아웃, 보드 단위·목록 단위 실패를 모두 빈 목록으로 수렴)·getSprint(404/403만 null, 429·5xx는 throw — 일시 실패를 '사라졌다'로 읽으면 sticky 값이 지워진다), createIssue의 sprintId 재해석 분기(패널이 fieldId를 payload에 안 싣는다 — 다이얼로그를 연 뒤 이슈타입이 바뀌면 stale. meta 조회 실패는 `.catch(()=>null)`로 삼켜 스프린트 없이 생성한다)
│   ├── oauth.ts         # Atlassian 3LO (launchWebAuthFlow + proxy 교환)
│   ├── oauth/           # config.ts(OAUTH_CONFIG 플랫폼 테이블 — clientId/proxyUrl lazy getter·needsProxy·cancelCodes·notConfigured 키 + isConfigured/assertConfigured/isCancellation, messages.ts `*.oauth.available` 판정 단일 출처) + errors.ts(OAuthError leaf — oauth.ts↔config.ts 순환 방지, oauth.ts가 re-export. `ConnectReason` 12값 enum과 태깅 헬퍼 3종도 여기: httpReason(status→4xx/5xx 버킷) + grantRejection/authorizeRejection(둘 다 `{cancelled, reason}` **쌍**을 반환해 두 축이 어긋날 수 없게 한다))
│   ├── github-api.ts    # GitHub REST 래퍼 (PAT/Bearer, 401 refresh hook 주입형, updateIssueState로 상태 변경)
│   ├── github-oauth.ts  # GitHub Web Flow (launchWebAuthFlow + proxy 교환) + refresh hook 자동 등록
│   ├── github-upload.ts # GitHub 파일 업로드 (MAIN world page injection, GitHub-Verified-Fetch 인증, batch uploadGithubFiles)
│   ├── linear-api.ts    # Linear GraphQL 래퍼 (API Key/Bearer, 401 refresh hook 주입형, getWorkflowStates/updateIssueState로 상태 변경)
│   ├── linear-oauth.ts  # Linear OAuth (PKCE, launchWebAuthFlow, proxy 불필요) + refresh hook 자동 등록
│   ├── notion-api.ts    # Notion REST 래퍼 (apiKey/Bearer, 401 → notion.oauthExpired, refresh 없음, updatePageStatus로 상태 변경)
│   ├── notion-oauth.ts  # Notion Web Flow (launchWebAuthFlow + proxy 교환, public integration — refresh 토큰 없음)
│   ├── gitlab-api.ts    # GitLab REST 래퍼 (PAT/Bearer, self-managed baseUrl, 401 refresh hook 주입형, uploadFile/createIssue/updateIssueDescription/updateIssueState)
│   ├── gitlab-oauth.ts  # GitLab OAuth (PKCE, launchWebAuthFlow, gitlab.com 고정, proxy 불필요) + refresh hook 자동 등록
│   ├── asana-api.ts     # Asana REST 래퍼 (PAT/Bearer, {data} 언랩, multipart attachment, 401 refresh hook 주입형, createTask/uploadAttachment(view_url)/updateTaskNotes/getTaskStatus/setTaskCompleted)
│   ├── asana-oauth.ts   # Asana OAuth (confidential — launchWebAuthFlow authorize + proxy token 교환) + refresh hook 자동 등록 (refresh_token 비회전)
│   ├── clickup-api.ts   # ClickUp REST 래퍼 (raw token `Authorization: <token>` Bearer 없음, refresh 없음, 401→재연결, clickupAuthHeader/flattenLists 순수 헬퍼, getTeams/getSpaces/getLists/getMembers/createTask(markdown_content)/uploadAttachment/updateTaskMarkdown/getTaskStatus/setTaskCompleted(list status 매핑))
│   ├── clickup-oauth.ts # ClickUp OAuth (confidential — launchWebAuthFlow authorize + proxy /clickup/token 교환) — 토큰 만료 없음 → refresh 함수 없음, isClickupCancellationCode/parseClickupCallbackParams
│   ├── slack-api.ts     # Slack Web API 래퍼 (Bearer user token, `ok:false` 패턴 → SlackError(body.platform), normalizeChannel/messageForSlackError 순수 헬퍼, getMyself(auth.test)/listChannels(users.conversations + users.list로 DM 이름 매핑)/listMembers/postMessage(thread_ts)/uploadFiles(getUploadURLExternal→POST(multipart)→completeUploadExternal 2-step)/getPermalink)
│   ├── slack-oauth.ts   # Slack OAuth v2 (user_scope, launchWebAuthFlow authorize + proxy /slack/token 교환) — user token=`authed_user.access_token`, 만료 없음 → refresh 함수 없음, isSlackCancellationCode/parseSlackCallbackParams
│   ├── bgRequestTypes.ts # BgRequest["type"] → true Record (BG_REQUEST_TYPE_MAP) — index.ts 인라인 Set에서 추출, union 누락을 typecheck로 강제
│   ├── injectSnapshotRows.ts # Jira(ADF) 제출 후처리 — buildIssueAdf가 만든 styleChanges table에 업로드된 before-${i}/after-${i} Snapshot 행을 element 인덱스로 splice (순수 함수, "As is"/"To be" 헤더로 table 식별)
│   ├── analytics.ts     # PostHog 익명 집계 — analyticsEnabled/buildCaptureBody/postCapture(순수·격리 fetch) + resolveInstallationId(순수)/getInstallationId(설치당 1회 UUID를 storage.local에 영속, 이벤트마다 공유 distinct_id) + captureEvent(키 게이팅 후 /capture/). store는 VITE_POSTHOG_KEY_PROD, 비-store는 VITE_POSTHOG_KEY를 사용하고 선택된 키가 없으면 no-op
│   ├── connect-tracking.ts # OAuth 시작 흐름 래퍼 — trackConnect(run 감싸 platform_connect success/cancelled/failed + 실패 시 reason 집계, 원본 에러 rethrow로 취소 토스트 억제 보존) + classifyConnectResult(OAuthError.cancelled 판정 순수 헬퍼) + classifyConnectReason(명시 태깅 우선 → cancelled/notConfigured/launchFailed 파생 → 숫자 status면 profile_fetch_failed → 네트워크 문구 → other. status 검사가 문구 매칭보다 앞이어야 상류 본문에 섞인 "load failed"가 오분류를 안 만든다). **태깅 누락은 그 플랫폼만 조용히 `other`로 떨어뜨리므로** `__tests__/connect-reason-coverage.test.ts`가 8개 oauth 파일의 원문을 스캔해 5개 레인(non-2xx 교환/갱신·200+본문 error·리다이렉트 `?error=`·`!redirect` 취소·프로필 조회)의 태깅을 전수 고정한다 — 사이트별 fetch 모킹 대신 호출부 누락만 겨냥한 그물
│   ├── platformErrors.ts # 플랫폼 API 에러 8종 → {status, body} 직렬화 (PLATFORM_ERROR_CTORS를 `satisfies Record<PlatformId, …>`로 묶어 플랫폼 추가 시 컴파일이 강제 — index.ts의 instanceof 8분기를 대체). OAuthError에는 null을 돌려줘 뒤 분기로 넘긴다 — index.ts의 플랫폼→OAuth→friendlyError 순서가 load-bearing
│   ├── capture-throttle.ts # captureVisibleTab 단일 직렬화 큐 (윈도우당 초당 2회 쿼터 회피 — 호출 간 최소 500ms 간격 + rate-limit 에러 한정 백오프 재시도). 리플레이 폴링·엘리먼트 스냅샷·스타일 before/after가 모두 이 큐 경유 (background captureVisibleTab 핸들러 필수)
│   ├── lib/             # adf-media(ADF media/mediaSingle 노드 빌더), adf-logs-link(업로드 후 logs.html em 노드에 link mark 주입하는 ADF 후처리 + LOGS_LINK_LABEL) — buildIssueAdf 계열 순수 헬퍼. readErrorBody(에러 응답 본문 JSON 우선 파싱 → 원문 → undefined, github/jira/gitlab/asana-api 공용). createRefreshRunner(github/gitlab/asana/linear 공용 토큰 refresh runner — pre-refresh(만료 60s 전)+401 1회 재시도, api 모듈 top-level 1회 발급 필수). rotatedAuth(pickRotatedAuth — refresh 락을 잡은 직후 저장분과 refresh token을 대조해, 앞선 요청이 이미 회전을 끝냈으면 소모된 토큰으로 재요청하지 않고 저장분을 쓴다. jira/github/gitlab/linear 4개 락 공용)
│   └── messages.ts      # 메시지 핸들러 디스패치 (jira.* / github.* / linear.* / notion.* / gitlab.* / asana.* / clickup.* / slack.* / analytics.capture / css.fetchSheets(cross-origin stylesheet 위임 fetch, SSRF 가드) namespace)
├── content/
│   ├── picker.ts          # DOM picker 메인 (메시지 라우터 + 모드 FSM + hover/select 이벤트)
│   ├── css-resolve.ts     # CSS 스타일 수집·토큰 resolve (resolveVarChain, collectSelection, collectTokens) + shorthand 전개·소유권 판정(expandShorthands/extractVarPropsFromMap/shouldOverwriteSpecified — var()가 끼면 CSSOM longhand가 비어 이 전개가 specified의 유일한 출처. ARCHITECTURE "CSSOM shorthand 한계 우회" 참조)
│   ├── css-source-cache.ts# raw CSS 텍스트 캐시 (CSSOM shorthand explode 우회, fetch + 경량 파서 + MutationObserver). cross-origin sheet는 직접 fetch 불가라 background css.fetchSheets에 위임(crossOriginRules·indexCrossOriginRules·getMatchingCrossOriginRules·getMatchingCrossOriginCustomPropRules)
│   ├── dom-describe.ts    # DOM 트리 직렬화 (buildSelector=finder 단발 경량 경로, buildInitialTree, buildChildrenResponse). pathSelector는 element-locator.ts로 이관해 역import — 트리 노드 selector는 요소 선택 경로와 **다른 빌더** 산출이다(아래 element-locator 참조)
│   ├── element-locator.ts # 요소 선택 경로 전용 selector 생성 — finder를 안정성 훅/기본 2회 실행해 (위치 유무, 단계, 길이)로 고른다. finder가 이미 penalty 오름차순 첫 유일 후보를 주므로 사람이 다시 매길 축은 이 셋뿐. TRUSTED_TEST_ATTRIBUTES 8종(data-testid/e2e/cy/qa/pw/test/test-id/automation-id)을 여는 게 존재 이유 — finder 기본 attr predicate의 wordLike 게이트가 그중 6개를 후보로도 안 만든다. 값은 isHandWrittenIdentifier로 좁혀 이메일·전화·주문번호·세션토큰이 selector를 타고 이슈 본문·플랫폼·LLM으로 나가는 걸 막고(id도 같은 게이트 — finder penalty 0이라 안 막으면 최우선 채택된다), 거부된 값은 compat 단계가 보전한다. 선택 요소 소유 class는 이름 기준 전역 거부(finder 훅에 element 인자가 없어 조상 동명 class도 함께 빠진다). 직전 선택 1건만 기억해 picker.selected ↔ selectionUpdated 일관성을 만들고, 재선택은 다시 계산한다(리스트 재배치 후 stale selector로 엉뚱한 요소를 편집·캡처하는 걸 막는다). 예산 소진 시 부분 결과 대신 pathSelector로 수렴. DOM Tree는 이 경로를 안 탄다 — buildSelector가 노드마다 자체 예산으로 호출돼 다단계를 얹으면 트리 열기가 초 단위가 된다
│   ├── capture-context.ts # element 캡처 컨텍스트 확장 판정(순수) — findContextAncestor(최근접 시맨틱 조상, form·fieldset은 개인정보 노출 면적 때문에 후보 제외)·passesContextGates(G1 뷰포트 완전 포함 / G2 요소 포함, 요소 0×0이면 생략 / G3 면적 ≤ CONTEXT_MAX_VIEWPORT_RATIO=0.4 — 클램프 없음)·resolveContextRect(after 재검증: 재조회 조상이 현재 요소를 DOM 포함하고 게이트를 다시 통과할 때만 채택). picker.ts가 BROWSER_BOUND_EXACT라 판정을 여기로 내려야 테스트로 고정된다
│   ├── frame-geometry.ts  # iframe↔부모 프레임 postMessage 핸드셰이크 (1-depth iframe picker 지원) — 자식 존재 registry 등록(announceFrameToParent/isRegisteredChildFrame, frameToken 위조 방어 + 도착순서 대비 pendingPresents 보류·재평가), 캡처용 top-frame 좌표 offset 응답(installFrameOffsetResponder consumeArm 게이팅 / requestFrameOffset — 부모 origin·isValidOffset 검증 + 타임아웃 폴백), top overlay 숨김 훅(onChildCapturePrep — 숨김이 top realm에서 커밋된 뒤 offset을 돌려준다. cross-origin iframe은 렌더러가 갈려 자식이 대신 기다릴 수 없다), composeTopRect/rectIntersectsViewport 순수 헬퍼
│   ├── overlay.ts         # Shadow DOM 오버레이 (아웃라인·배너·블로커·hover 방패·프리뷰). 인스펙터 카드의 다크 전환은 OS(prefers-color-scheme)가 아니라 `setOverlayTheme(handle, theme)`이 `labelEl`에 세팅하는 `data-theme` 속성을 따른다 — 사이드패널이 picker.start로 실어보낸 앱 theme이 출처이고, 미설정이면 라이트 기본 블록이 남는다. **`styles/__tests__/tokens.test.ts`가 `.picker-label[data-theme="dark"][data-mode="inspector"]` 문자열을 앵커로 라이트/다크 영역을 가르므로 셀렉터를 바꾸면 파서도 함께 고친다**(다크 블록이 라이트보다 뒤에 있어야 한다). blocker의 pointer-events는 blocker-state 컨트롤러가 파생하고 스크롤 양보(SCROLL_YIELD_MS=120)는 이유+타이머를 여기가 독점한다 — 회수는 마지막 휠로부터 SCROLL_INTENT_MS=60 밖일 때만(그 안의 이동은 스크롤의 일부)
│   ├── blocker-state.ts   # blocker pointer-events 소유권 컨트롤러(순수) — 투과 이유 집합(scroll-yield/handoff) + 진행 중 hit-test에서 값을 파생하고 적용까지 자기가 한다(apply 누락 구조적 차단). 이유가 겹칠 때 한쪽 해제가 남의 투과를 무단 취소하는 last-writer-wins를 없앤다 + isScrollIntent(휠 최근성 판정)
│   ├── hover-shield.ts    # hover 방패 소유권 컨트롤러(순수) — blocker가 물러나는 창(선택 커밋 / 캡처 준비)에 투명 전면 레이어를 세워 페이지가 CSS `:hover`를 되찾는 걸 막는다. 이유 집합(selection-commit/capture-prep) + hit-test 프로브에서 표시 여부를 파생하고, selection-commit만 만료 타이머를 이유와 함께 소유한다(캡처를 건너뛰는 경로에선 endCapture가 안 온다). blocker-state의 형제
│   ├── style-overlay.ts   # 요소 인라인 style/class 오버레이 적용·복원 상태기계 — 원본 스냅샷을 prop 단위로 보관했다가 되돌린다(picker.ts에서 분리, 순수 로직이라 단위 테스트 가능)
│   ├── area-select.ts     # 영역 드래그 선택 (dimming + 사이즈 라벨) + selectFullViewport(화면 캡처 — 드래그 완료와 같은 정리→onSelected 순서 공유). 확정은 오버레이를 걷은 프레임이 커밋된 뒤 발화(settleAfterPaint) — 대기 창에 취소가 오면 폐기(_cancelled), 재진입은 1회만 통과(_settling)
│   ├── after-paint.ts     # afterPaint — 오버레이를 숨긴 스타일 변경이 합성에 커밋될 때까지 대기(rAF×2, hidden·occluded 탭은 rAF가 안 와서 150ms 폴백으로 진행. 실패가 아니라 진행 신호다 — 길게 잡으면 그런 환경의 캡처마다 얹혀 상위 타임아웃을 깬다). 캡처 준비 응답이 같은 태스크에서 나가면 captureVisibleTab이 오버레이가 살아있는 직전 프레임을 찍는다 — picker.ts·area-select.ts·frame-geometry.ts 공용
│   ├── scroll-capture.ts  # 페이지 전체 캡처의 페이지 측 executor — 타일별 scrollTo(instant) + rAF×2 settle(hidden 탭 폴백), fixed + 이미 전부 노출된 뒤 붙은 sticky를 visibility:hidden !important로 임시 은폐(html·body 제외), MutationObserver로 후행 후보 추적, 세션 종료 시 스크롤·요소 원복
│   ├── annotation.ts      # 녹화 중 그리기 오버레이 (picker 엔트리 내부 모듈, top frame 한정) — shadow host(ANNOTATION_HOST_ID) + SVG 획 레이어 + blocker, pointer 드래그로 획당 단일 styled path(선택 도구의 색·두께·투명도, 흰 아웃라인 없음 — konva 벡터 동일), pointermove마다 EMA 스무딩, 그린 순서대로 꼬리부터 ~3초 rAF 트레일 페이드, 펜 ON 중 wheel yieldToScroll·touch-action:none, Esc→penOff 업스트림. showAnnotation/hideAnnotation/setAnnotationTool(tool·style). 고아 host 제거 후 재마운트
│   ├── annotation-draw.ts # pointsToPath(좌표 배열→SVG path d, 단일 포인트는 zero-length line)·smoothPoint(EMA 손떨림 보정)·dropExpired(선두 만료 점 트림)·PEN_SMOOTHING_ALPHA 순수 헬퍼
│   ├── recorders-entry.ts # MAIN world content_scripts entry (document_start, all_frames) — network/console/action 레코더 자기 호출
│   ├── recorder-bridge.ts # ISOLATED content_scripts entry (all_frames) — sentinel 수신·recorder.* data를 chrome.runtime으로 중계, cross-origin iframe 로그 커버리지 (picker.ts에서 추출)
│   ├── log-throttle.ts    # createTrailingThrottle (FLUSH_INTERVAL_MS=200, 폭주 중에도 interval당 1회 flush 보장) — 레코더 3종 스트리밍 **전용(content)**. 수신부 IDB 가드는 복제본 sidepanel/lib/trailing-throttle.ts 사용(recorders-entry 청크를 crxjs 동기 IIFE로 유지하려 의도적 분리 — sidepanel이 이 모듈을 import하면 공유 청크화로 pre-arm 무력화). 타이머 주입 가능(테스트)
│   ├── recorder-globals.ts# document_start 내장 스냅샷 — jsonParse/jsonStringify/objectEntries/isArray/URLCtor/URLSearchParamsCtor/CustomEventCtor/randomUUID(crypto에 bind)/addEventListener·removeEventListener·dispatchEvent(EventTarget.prototype에서 뗀 것)/navigationRef·performanceRef(네비게이션 유형 판정용 — 전역 **교체**만 막고 currentEntry 속성 재정의는 통한다. 위조 이득은 방향 오라벨뿐이라 수용). MAIN world는 페이지와 같은 realm이라 호출 시점 전역을 쓰면 페이지가 그걸 갈아끼워 마스킹을 무력화(JSON.parse를 throw로 → catch 폴백이 원문 통과)하거나 우리 리스너 등록·발화를 가로챌 수 있다. **sentinel 은닉은 목적이 아니다** — 브리지가 UUID를 평문 detail로 흘린다. **참조 고정까지만** 막는다(URL.prototype 메서드 재정의는 여전히 통함). recorders-entry 청크라 src/content/ 밖 런타임 import 0. 유닛 불가라 BROWSER_BOUND_EXACT 등재
│   ├── sentinel-registry.ts# createSentinelRegistry(cap=8) — sentinel 다중 등록(add 멱등·list 스냅샷·evicted drain). 단일 슬롯이던 시절 위조 setSentinel 한 방이 진짜 세션의 stop/sync/clear 리스너를 떼어내 수집이 무음으로 죽던 **파괴적 교체 하나**를 없앤다(위조 자체·위조 stop·캡 초과 evict는 수용된 잔여 위험 — ARCHITECTURE.md 참조). evicted()가 drain인 이유는 비우지 않으면 리스너 해제가 매 add마다 반복되고 목록이 무한히 자라기 때문. 순수 함수라 로직 스코프 유지
│   ├── recorder-prearm.ts # pre-arm 게이트 (sessionStorage PREARM_FLAG_KEY=__bugshot_recorder_active__) — isPreArmFlag/readPreArmFlag/setPreArmFlag. setSentinel 시 set, clear 안 함(탭 종료 시 자연 소멸). 레코더가 document_start에 읽어 active origin이면 sentinel 전부터 적재(capturing). sandboxed 접근 throw는 비활성으로 흡수
│   ├── post-to-runtime.ts # postToRuntime — chrome.runtime.sendMessage의 동기 throw(확장 reload/무효화)를 id 가드 + try로 흡수
│   ├── network-recorder.ts# MAIN world 네트워크 캡처 (fetch/XHR/sendBeacon 래핑, send 시점 phase="pending" entry push → 완료/에러 시 in-place 갱신, body omission에 size/limit/contentType context, 50MB 버퍼 메모리 총량 캡(초과 시 오래된 body evict — per-body 캡은 BODY_CAP 3MB) + 5000 entry FIFO. 적재 게이트는 capturing(pre-arm 플래그로 init) — active origin이면 sentinel 전부터 적재, sentinel 전 적재분은 entry.preArm=true 마킹. WebSocket: patchWebSocket이 window.WebSocket 생성자만 Proxy(construct) 후킹 + 인스턴스 send 직접 wrap → attachWsRecorder가 연결을 entry 1개(status 101/method "WS"/webSocket.frames)로 매핑, 텍스트 프레임만 적재·바이너리 드롭, 연결당 1000프레임 FIFO 캡(WS_FRAMES_CAPPED) — 전역 MEMORY_CAP 미합류)
│   ├── network-recorder-helpers.ts# classifyResponseBody / classifyBeaconBody / classifyWsFrameData(WebSocket 텍스트 프레임만 통과·바이너리 null 드롭·BODY_CAP 초과 truncated) / maskWsFrame(프레임 본문 민감키 마스킹 — maskBody 위임) 순수 헬퍼 + BODY_CAP (3MB) + createPatchedFetch (fetch wrap 팩토리 — new Request 복제본 전송으로 body 재소비 방지, 기록은 FetchRecordHook 위임) + eviction 계산부 (estimateBodySize / findOldestBodyIndex[문자열 본문을 가진 최오래 인덱스 — 이미 omitted된 항목을 다시 고르면 회수량 0으로 루프가 안 끝난다] / reclaimableSize[evict 시 차감할 request+response 합]. buffer 변형·memoryUsed 대입은 network-recorder 클로저에 남기고 선택·집계만 분리해 단위 테스트) + xhrFailureStatus/fetchFailureStatus/beaconStatus(BugShot이 만든 실패·큐 상태의 statusText와 statusKind를 한 함수에서 함께 정한다 — 둘이 어긋나면 isStatusHidden의 "상태 가려짐" 판정이 조용히 움직인다. 레코더 IIFE는 유닛으로 못 불러 여기가 그 계약의 유일한 그물)
│   ├── console-recorder.ts# MAIN world 콘솔 캡처 (log/info/debug + trace/assert/dir/table/group*/count*/time* 상시 wrap, error/warn은 arm 구간에만 wrap해 캡처 — arm 트리거는 setSentinel 또는 pre-arm(active origin이면 document_start부터 installEwWrap), chrome://extensions attribution 오염을 arm 창으로 한정, 멱등(ewState.installed), install 시점 prior를 스냅샷해 페이지 모니터링 wrapper 보존, sentinel 미도착 시 pagehide에서도 원복. 적재 게이트 capturing(pre-arm 플래그) vs dispatch 게이트 recording 분리, sentinel 전 적재분은 preArm 마킹. uncaught throw는 window.error/unhandledrejection으로 별도 캡처, 2000건 FIFO, clearBuffer는 counters/timers Map도 함께 리셋)
│   ├── console-recorder-helpers.ts# formatErrorEvent / formatRejectionReason / shouldCaptureAssertion + safeStringify·serializeArgs(페이지 값 throw-safe 직렬화 — getter/toString/Proxy throw를 [unserializable]로 흡수) + makeConsoleWrapper·installConsoleWrap·restoreConsoleWrap(error/warn arm-스코프 wrap 라이프사이클 — 멱등 설치·prior 복원) + capLabelMap(console.count/time 라벨 Map insertion-order FIFO 캡 — 상태 갱신을 capturing 게이트 밖에 둬야 arm 후 값이 DevTools와 맞아 무한 증가만 캡으로 bound) 순수 헬퍼
│   ├── action-recorder.ts # MAIN world 액션 캡처 (click/input/toggle/select/keypress/navigation/drag를 재현 단계로 기록, document_start부터 buffer, 1000건 FIFO, history.pushState/replaceState 래핑 + popstate/hashchange, 마스킹 2층 적용(값 경로 recordInput·recordSelect + 이름 경로 accessibleName·fieldLabel — 이름 경로를 안 막으면 click/drag/keypress의 target으로 저작물·PII가 도로 샌다. contentEditable은 값을 아예 미기록[리치 에디터 본문=사용자 저작물], recordToggle은 값이 checked/unchecked뿐이라 판정 대상 아님. 값은 VALUE_CAP=500 절단), 같은 selector 연속 input은 in-place dedup, sentinel-bound dispatch. drag=포인터 휴리스틱(source-only, 15px 임계+elementFromPoint 가드+pointercancel dedup) + 네이티브 HTML5 DnD(source+target, dragstart/drop). 적재 게이트 capturing(pre-arm 플래그) — sentinel 전 적재분 preArm 마킹, pre-arm으로 진입 load 적재 시 entryNavEmitted 가드로 setSentinel의 entryNavOnBind 보충 스킵해 중복 방지)
│   └── action-recorder-helpers.ts# shouldMaskField(1층 라벨 판정 — type=password·autocomplete + name/id/aria-label/label[for]·암묵 라벨·aria-labelledby/placeholder의 민감 키워드. 영문은 단어경계 \b + normalizeName[camel/snake/kebab 분해]로 부분일치 오탐 차단[pin⊂shipping], 한글은 부분일치) / isSensitiveValue(2층 값 판정 — 라벨에 단서 없는 경우[생성된 id·커스텀 폼]를 값 형태로: 이메일 정규식 또는 구분자 제거 후 9자리+ 순수 숫자열[전화·카드·주민·계좌]. `.`은 구분자 제외 — 소수·IP 오탐 방지) / maskValue / truncateName(접근가능 이름 trim·cap, 역할은 ActionEntry.role로 분리) / buildLightSelector / formatKeyCombo / entryNavOnBind / entryNavType(도착 문서의 PerformanceNavigationTiming.type → load|reload|traverse, 레거시 performance.navigation.type 폴백) / traverseDirection(히스토리 인덱스 델타 부호 — NavigationHistoryEntry.index의 -1을 음수 거부로 걸러야 back 오판이 안 난다) / popstateNavType(위 델타 + 도착 엔트리 id를 본 적 있는지 — 프래그먼트 네비게이션도 popstate를 쏘므로 이 게이트가 없으면 링크 클릭이 forward로 오라벨된다) / exceedsDragThreshold(제곱 거리>임계²)·DRAG_THRESHOLD_PX / matchesOwnHost(element id가 자기-UI host[picker·annotation]에 걸리는지 — isOwnUi 제외 판정) 순수 헬퍼
├── sidepanel/
│   ├── App.tsx          # Radix Tabs 4개 (디버그/이슈 목록/연동/설정) + TabNavContext Provider, 설정 sub-tab을 controlled로 보유
│   ├── main.tsx
│   ├── tab-nav.ts       # TabNavContext — 메인 탭(+설정 sub-tab) 전환 setter를 하위 컴포넌트에 노출 (App↔하위 순환 import 회피)
│   ├── capture.ts       # 요소 크롭 스냅샷 + 공용 cropImage/loadImage(줌 보정 — clampCropRect 경유, 영역·인라인·요소 캡처 단일 구현)
│   ├── scroll-capture.ts # 페이지 전체 캡처 오케스트레이터 — planScrollCapture 타일 계획 → picker.scrollCaptureTo + background captureVisibleTab 반복(관문 경유) → 타일 도착 즉시 canvas 스티치·비트맵 폐기(메모리 스파이크 방지), 탭 활성·abort 재확인, finally로 endScrollCapture 보장
│   ├── picker-control.ts
│   ├── recorder-control.ts# MAIN world 레코더 버퍼 clear sender (clearNetworkRecorder/clearConsoleRecorder/clearActionRecorder) — editor-store가 직접 호출하므로 picker-control과 분리해 순환 import 차단
│   ├── annotation-control.ts# 녹화 중 그리기 오버레이 제어 sender (showAnnotation/hideAnnotation/setAnnotationTool(tabId,tool,color,thickness) — overlayStrokeStyle로 strokeWidth/opacity 계산해 annotation.setTool 전송) — recorder-control과 동일한 tabs.sendMessage 래퍼
│   ├── video-capture.ts # 영상 녹화 시작 (startVideoCapture[탭]/startScreenCapture[화면 — getDisplayMedia 1080p·12fps, user activation 첫 await]) — 진입 화면 버튼 전용, video-recorder 순환 회피로 별도 모듈
│   ├── video-recorder.ts# MediaRecorder 녹화 세션 관리 (beginRecording 공통 본문[tabCapture/getDisplayMedia 공유] — beginTabRecording[탭 — 스트림 획득은 startTabStream]/startScreenRecording[화면, track ended→자동 stop])
│   ├── 30s-replay/      # 30s Replay 캡처 + **영상 구간 자르기(리플레이·탭/화면 녹화 공용)** — frame-buffer(직전 30초 순환 버퍼), mp4-encoder(WebCodecs VideoEncoder→mp4-muxer H.264 인코딩, MAX_FRAME_DURATION_MS export + createMp4Sink[muxer·encoder·flush/finalize 배선 팩토리 — encodeToMp4와 encode-range 공용]), trim-source(TrimSource 판별 유니언 — frames=리플레이 프레임축 / recording=탭·화면 녹화 벽시계축. 타입 전용·런타임 import 0으로 editor-store에 WebCodecs 그래프 유입 차단), encode-range(encodeVideoRange — MediaRecorder blob을 `<video>` 배속 재생 + requestVideoFrameCallback으로 재디코드해 선택 구간만 재인코딩. 브라우저 전용[BROWSER_BOUND_EXACT]), use-30s-replay(captureVisibleTab 폴링 훅 + capture() + resolveTrim. 트림 대기 상태는 store editor-store.replayTrim이 단일 출처 — 게이트+페이로드가 한 몸이고 drafting 전이와 같은 set()이어야 한다[레인 분리 회귀 방지]), replay-context(ReplayProvider로 replayEnabled/isReady/isEncoding/bufferedSeconds/capture/trimming 공유 — 훅 인스턴스가 App에만 있어 하위가 직접 못 읽는 값들. replayEnabled는 IssueTab 캡처 버튼 상태·툴팁용, trimming=replayTrim != null은 trim 오버레이 표시 중 DraftingPanel 마운트 가드 신호[흰화면 회귀 방지]), trim-math(frameOffsetsMs/secondsToFrameRange/isFullRange + replayLogTrimBounds/previewTrimBounds/isTrimmedOut — 초↔프레임 매핑 + 로그 trim 경계 헬퍼[apply-trim 실제 잘림과 트림 미리보기 muted가 공유 → "흐림=실제잘림" parity], 최소 2프레임 보장. **recording 축 카운터파트**: isFullRangeSec/recordingLogTrimBounds[벽시계 선형, 가드밴드 미적용 — 프레임 양자화가 없어서. 하한 없음은 -Infinity]/previewBoundsFor[TrimSource 판별 후 위임 — 두 축의 parity 단일 출처]/pickTrimBitrate[원본 실측×1.5를 0.8~4Mbps로 clamp — 고정값이면 저모션 녹화가 몇 배로 커진다]), apply-trim(applyReplayTrim — 선택 구간 프레임 slice→재인코딩+로그 재trim. 영상 메타=raw ts / 로그 경계=잘라낸 쪽은 정확한 프레임 wall-clock·가드 미적용, 안 자른 쪽만 capture 보존[앞=REPLAY_LOG_GUARD_MS, 끝=상한 없음] / applyRecordingTrim — encode-range로 구간 재인코딩 후 같은 로그 trim. 전체 구간이면 즉시 return[재인코딩 skip] / trimStoredLogs — 로그 3종 trim 공용, **동기**로 save Promise 배열 반환[await은 호출자가 replaceVideo 뒤에 — 그 사이 await이 끼면 "로그만 잘린" 중간 렌더가 샌다] / TrimLogsPersistError — trim본 IDB 저장 실패 신호. blob-db save는 reject가 아니라 false resolve라 값으로 판정), trim-markers(buildErrorMarkers — log-viewer buildMarkers 재사용해 console error·warn + network 에러 + action navigate 마커를 TimelineMarker(log-viewer 타입 그대로)로 매핑)
│   ├── hooks/           # useBoundTabId, useTabSupport(useTabUnsupported — 바인딩 탭의 미지원 여부 판정. tabs.get + url·status onUpdated 재조회, changeInfo.url은 미지원 전이에서 redact되므로 신뢰하지 않음. seq 가드로 늦은 옛 응답 무시), tab-support-context(TabSupportProvider/useUnsupportedTab — App이 1회 판정해 내려보내는 컨텍스트), useCodeCollapse(html 변경마다 root 내 pre에 코드블럭 접기 셸 재부착 + cleanup에서 unwrap — DocSectionBody·IssuePreviewView 프리뷰 공용),  useAI, useBackgroundRecorder, useEditorSessionSync, deriveLogsAttach(구 3플래그 스냅샷 → 단일 logsAttach OR 파생 — 전부 undefined면 기본 true, 세션 마이그레이션용), useIssueImages, usePickerMessages, useBufferThenSwitch(복수 element 버퍼 — 현재 요소의 after 스냅샷 캡처 후 버퍼에 push하고 다음 요소로 picker 재시작, busy 가드로 중복 클릭 차단), useDraftStyleElements(저장된 draft의 현재+버퍼 element 이미지를 dataURL로 로드 후 resolveDraftStyleElements로 병합 — DraftDetailDialog 렌더용, loadDraftStyleImages는 재제출에도 재사용), usePlatformFields(SubmitFieldsDialog 공용 platform fields state + open/draft 전환 시 idempotent reset), useLazyListOnOpen(콤보박스 open 시 1회 목록 fetch — CC 멤버 조회 콤보박스 공용), useThemeEffect, useAiLoadingStep(AI 로딩 오버레이 문구 step 타이머 — activeKey(표면)가 truthy인 동안 intervalMs마다 step 증가, 표면 전환/종료 시 0 리셋), useRecorderSyncInterval(Console/Network SubTab 공용 — active 동안 1.5s 간격 레코더 sync), useAiRun(AI 실행 1회분의 취소 레인 단일 출처 — run 객체·AbortController·editor-store aiCancel 슬롯 등록을 한 곳에서 소유하고 begin/isActive/end/readopt/detach/disposeCurrent를 노출. kind가 effect cleanup의 abort 여부를 가른다: oneshot(사용자 액션 발 — AiDraftDialog·AiStylingDialog)은 detach에서 abort, resumable(effect 자동 발화 — useReproPrefill)은 abort하지 않는다[cleanup이 언마운트와 게이트 왕복을 구분 못 해 재개될 요청을 죽인다]. 슬롯 해제는 슬롯이 아직 자기 canceller일 때만 — 다른 표면의 중단 버튼 clobber 방지. begin()은 로딩을 false로 내리지 않는다[StyleCssView가 하강 에지를 AI 턴 종료로 읽어 CodeMirror doc을 강행 재동기화]. provider 교체·언마운트는 disposeCurrent 공용, onDispose는 run당 1회. 3개 콜사이트에 흩어져 있던 손매칭 가드를 대체 — 불변식은 ARCHITECTURE.md "AI 취소"), useReproPrefill(video drafting 진입 시 stepsToReproduce가 비어 있고 AI(나노/BYOK) 가용하면 액션 로그를 AI로 정리해 자동 채움 — 세션 1회 persist 가드(reproPrefillDone), fire-input을 뺀 원시 플래그 deps + ref는 deps 좁히기·1회 래치 전용이고, in-flight 취소·클로버 방지는 useAiRun(kind: resumable) 위임 — 게이트 왕복·StrictMode 재마운트는 readopt로 직전 요청을 이어받고 cleanup detach는 abort하지 않는다, 실패 시 toastLlmError. AI 없으면 미발화)
│   ├── components/      # 공통 UI (Section(접이식 섹션 셸 — open/onOpenChange 공급 시 controlled, 미공급 시 내부 state. 접히면 children 언마운트)/AnnotationOverlay(react-konva 스크린샷 주석 오버레이 — 완료 시 webp flatten해 screenshotAnnotated 저장. 구현은 annotation/ 하위: AnnotationToolbar(3단 세로 아이콘 툴바 — 도구행[선택/그리기도구/삭제]·색·두께(텍스트 크기)행·undo/redo·완료행 + 캔버스 스크롤 뷰포트[키보드 포커스 가능]와 플로팅 줌 컨트롤·너비 맞춤 버튼 배치)·ShapeNode(도형별 react-konva 렌더)·shapes(도형 모델·좌표/transform 순수함수)·viewport(줌·팬 순수함수 — fitWidthScale[진입 배율]/fitAllScale[전체 조망]/zoomStops·stepZoom/ZoomLevel·resolveScale·normalizeZoom·refitZoom[줌 의도 ↔ 표시 배율]/centerAnchoredScroll[중앙 앵커]/panScroll/canPan/toolCursor)·ZoomControl(캔버스 우상단 플로팅 [-][n%▾][+] — ButtonGroup + shadcn Select)·history(undo/redo 스택)·presets(색·선 두께·텍스트 크기·형광펜·도구 메타·isStrokeTool·recordingColorCount[녹화 footer 폭→노출 색 개수 5/4/3, floor RECORDING_MIN_COLORS=3])·ToolbarGroups(ToolButton/ColorSwatches/ThicknessButtons/TextSizeButtons 공유 presentational 그룹 — AnnotationToolbar + 녹화 footer 공용, testid prefix 주입. ColorSwatches는 count로 좁은 폭에서 우측 색부터 접되 활성색은 항상 노출)·deferredExport(scheduleDeferredExport/useDeferredExport — rAF 1프레임 뒤 export를 예약하되 **언마운트 시 취소**한다. `handleDone`이 Transformer detach 후 다음 프레임에 `toDataURL`을 부르는데, 그 사이 오버레이가 닫히면(취소·[다음]) 예약된 콜백이 살아남아 이미 끝난 세션에 주석을 써 넣는다)·recording-pen(overlayStrokeStyle — tool·두께→strokeWidth/opacity, 녹화 오버레이 스타일 계산))/TiptapEditor/OrderedListEditor(개행 구분 문자열↔번호 매긴 Input 리스트 편집기 — 항목 추가/삭제·포커스 관리, 재현 과정 등 orderedList 섹션 편집. DraftingPanel·DraftEditDialog 공용, DraftingPanel 로컬 정의에서 추출)/DocSectionBody/IssuePreviewView(이슈 본문 프리뷰 공용 — 제목·재현 환경·섹션 + 마크다운 복사 버튼, media/logCards 슬롯, PreviewPanel·log-viewer Report 탭 공유)/DocTable/ConsoleLogContent(selectedId+onActiveChange 공급 시 행 클릭=선택[ring-2 ring-primary — 레벨 틴트·sync 하이라이트와 배경 경합 회피]+펼침 이중 동작, 미공급 시 표시 전용)/NetworkLogContent(네트워크 로그 뷰어(STATUS_KIND_LABEL_KEYS export — BugShot 생성 statusKind→i18n 라벨 키 단일 출처. Exclude<…,"networkError">라 union이 늘면 컴파일러가 강제하고, log-viewer 복제 사전 대칭은 log-viewer/__tests__/i18n.test.ts가 이 상수를 그대로 돌려 검사한다 — 키 스캐너가 t("리터럴")만 훑어 테이블 값은 못 보기 때문) — WS 연결은 상세 Messages 탭에서 send/receive/open/close 프레임 타임라인 + 방향 필터·dropped 배지)/LogInsertDialog(로그 1건을 골라 이슈 본문에 코드블럭으로 삽입 — Tabs[network/console, forceMount로 탭 왕복 시 내부 선택·검색·필터 보존] + NetworkLogContent/ConsoleLogContent(선택 모드) 재사용, 기존 logs.html '로그 첨부' 토글과 별개 기능)/LogPreviewDialog(탭형 단일 로그 미리보기 다이얼로그 — console/network/action 3탭 항상 노출[forceMount·0건 EmptyCase·0부터 배지], 삭제된 타입별 3 preview dialog 대체. logsAttach·syncBaseMs·onToggleAttach[첨부/해제] 지원)/OriginFilterBar(출처별 로그 필터 바 — Console·Network·Action 공용, origin 2개+ 일 때만 노출)/TreeChevronButton(트리 펼침 chevron 공용 — JsonTreeViewer·DomTreeDialog가 className까지 같은 raw <button>을 복제하고 있었고 aria-expanded는 한쪽에만 있었다. shadcn Button을 안 쓰는 건 size="icon"의 h-9가 h-4 슬롯에 안 들어가서고[DESIGN §10 예외], 라벨은 호출부 주입 — DOM 트리의 dom.expand/collapse 도메인 문구를 살린다)/ActionLogContent(액션 로그 뷰어 — 동사형 문장 렌더, 값/태그/URL을 inline 칩(shadcn `Kbd`, mono keycap)·링크로 강조. 값·태그·드래그·마스킹 칩이 `Kbd` 단일 출처)/InlineLink(inline 외부 링크 — target=_blank+noopener 공용, ActionLogContent·ConsoleLogContent·LinkifiedText 공유)/LinkifiedText(로그 본문 URL linkify 래퍼 — tokenizeLogText로 텍스트를 분할해 URL만 InlineLink로 렌더[클릭 시 행 토글 방지 stopPropagation], ConsoleLogContent args/stack 사용)/JsonTreeViewer(JSON 트리 뷰어 — highlightQuery 공급 시 매칭 leaf(문자열·숫자·불리언·null·키)를 파랑(blue) 하이라이트 + collectMatchExpandedPaths[매칭 노드 조상 path]로 접힌 아코디언 최초 1회 자동 펼침, 네트워크 바디·WS 프레임 공용. 순수 export collectMatchExpandedPaths는 트리 SEP 인코딩 공유 위해 콜로케이션)/HighlightedText(검색어 매칭 문구 `<mark>` 파랑(blue) 하이라이트 렌더 — splitHighlight 세그먼트 → 매칭만 mark, 빈/무매칭 시 원문 그대로 + HighlightQueryContext[JSON 트리 leaf에 쿼리 공급], NetworkLogContent 상세[URL·헤더·바디]·JsonTreeViewer 공용)/LogAttachmentCards(logs.html 단일 첨부 토글 카드 — 스위치 1개로 3종 통째 on/off, desc는 logCardTypeCounts로 타입별 건수·에러 표시, 스위치 영역 hover-suppress[IssueRow 패턴])/logCardTypeCounts(캡처된 타입만 console→network→action 순 카드 desc 조립 순수 헬퍼 — 에러 건수는 buildLogSummary 파생)/AttachmentSection(drafting 사용자 파일 첨부 — 추가 버튼·카운터·삭제 카드 리스트)/AttachmentList(읽기 전용 첨부 카드 목록 — 클릭 시 로컬 다시 다운로드, CATEGORY_ICON 보유, PreviewPanel·DraftDetailDialog 공용)/StyleChangesTable(diff table 3화면 공용 — SnapshotCell이 `annotated ?? raw`로 그리고, onAnnotate 핸들러가 **있을 때만** ImageActions를 렌더한다[읽기 전용 화면 보호]. hover/focus를 따로 세어 조건부 렌더 — opacity-0으로 숨기면 요소당 최대 4개의 유령 탭 정지점이 생긴다)/ImageActions(diff table 이미지 위 hover 액션 그룹 — ButtonGroup + TooltipIconButton 2개[제거·주석], onReset 유무가 곧 주석 유무라 라벨이 add/edit으로 갈린다. 표면 클래스는 그룹이 아니라 버튼에 준다[outline variant의 불투명 bg-background가 래퍼를 덮는다])/ColorSwatch(색/이미지 미리보기 스와치 — picker 인스펙터 .pl-swatch와 시각 통일, `shape="round"`는 라벨 색 dot용. TokenChip·StyleChangesTable·ValueCombobox·github/gitlab/linear LabelCombobox 공용)/CancelConfirmDialog/LoadingDialog(끊을 수 없는 장기 작업의 대기 화면 — AlertDialog 기반이라 바깥 클릭이 안 통하고 Escape까지 막아 닫는 컨트롤이 아예 없다. 원형 아이콘·제목·안내·진행률 바를 한 화면에 묶는다[EmptyShell·녹화 중 화면과 같은 골격]. percent 생략 시 바 미표시. 현재 사용처: 리플레이 트림 재인코딩)/AiLoadingText(AI 로딩 오버레이 문구 크로스페이드 — 이전 문구 위로 이탈·새 문구 아래서 진입, prev 정리 타이머 분리, App.tsx 오버레이 전용)/FieldRow(label+required 표기 공통 + htmlFor[라벨→입력 연결, connect 폼 10곳] + labelAction[라벨 우측 보조 링크 — 라벨과 "토큰 발급" `<a>`가 나란히 서는 7곳의 유일한 표현 수단. 컨테이너가 `grid gap-1.5`라 원본 `flex flex-col gap-1.5`와 렌더가 같다는 전제 위에 치환했다])/ConnectedBadge(플랫폼 연결 완료 녹색 배지 공용)/SubmitSuccessView(이슈 제출 완료 화면 공용 — 성공 아이콘+이슈 링크+후기·확인 버튼, IssueTab(작성)·IssueListTab(목록) 공유)/LogSeekChip(영상-로그 동기화 점프 칩 — onSeek 공급 시 [+MM:SS] 버튼, 미공급 시 기존 span)/CcMultiCombobox(CC 멘션 다중 선택 콤보박스 공용 셸 — 플랫폼별 CcCombobox/CcField가 멤버 목록·표시 규칙만 주입)/SingleLazyCombobox(단일 선택 lazy-fetch 콤보박스 공용 셸 — useLazyListOnOpen+ccOptions 사용, CcMultiCombobox의 single 카운터파트)/ccOptions(pinSelectedFirst/orderSelectedFirst·CcUserOption — 선택 유저 상단 고정 순수 헬퍼, CcMultiCombobox·SingleLazyCombobox 공유)/RecordingSettingsCard(녹화 설정 카드 — 녹화 모드 Tabs + 30초 리플레이 토글·권한 요청, 설정 탭 '녹화 설정' 섹션 전용)/TooltipIconButton(툴바 아이콘 버튼 공용 — outline h-8 w-8 + Radix 툴팁(Provider 내장, 상위 Provider 없는 오버레이·footer 대비) + 토글(data-active/aria-pressed/bg-muted) + ariaDisabled 잠금. 캡처 방식 툴바(IssueTab)·어노테이션 툴바(ToolbarGroups — 이미지·녹화 공용) 공유)/IntegrationsCta(연동 0개 유도 amber 배너 — AI 배너와 동일한 rounded-t-lg footer 접합 패턴, onNavigate 주입[다이얼로그는 닫기 합성]. IssueTab EmptyState·PreviewPanel·DraftDetailDialog 공용, 다이얼로그만 className으로 여백 상쇄))
│   ├── tabs/            # 탭별 진입점 + 편집 패널 (DebugTab(→IssueTab/ConsoleSubTab/NetworkSubTab)/IssueListTab(+IssueRow + issueListUtils 순수 헬퍼)/IntegrationsTab(+integrationsTabUtils 순수 헬퍼 — connected/add 서브탭 라우팅)/SettingsTab/StyleEditorPanel/DraftingPanel/DraftDetailDialog/DraftEditDialog(저장된 draft의 title/본문 섹션 단일 필드 인라인 편집 다이얼로그 — 열릴 때 target으로 seed, section은 lazy TiptapEditor 또는 OrderedListEditor·title은 Input, applyDraftFieldEdit로 patch 계산해 DraftDetailDialog가 patchIssue)/AiDraftDialog/IssueCreateModal/SubmitFieldsDialog(IssueCreateModal·DraftDetailDialog 공용 platform 선택 + 필드 입력 다이얼로그)/PreviewPanel/DomTreeDialog/IssueTypeCombobox/ProjectCombobox/ReplayTrimDialog(영상 구간 자르기 오버레이 — **30s Replay·탭/화면 녹화 공용**(source: TrimSource로 프레임축/벽시계축 분기. 이름은 리플레이 전용이던 시절의 잔재), full-screen, 아이콘 4탭[영상/콘솔/네트워크/액션 — CollapsingTabsList 공유, 로그 탭은 Console/Network/ActionLogContent를 isMuted 흐림 미리보기와 함께 인라인 마운트]/컨트롤러[재생+TrimTimeline]/액션바[undo·redo / 선택 길이 readout / 취소·확정], lazy)/TrimTimeline(trim 듀얼핸들 Slider + 비대화형 재생위치·에러마커 오버레이 레이어))
│   │   ├── styleEditor/   # AiStylingDialog, ValueCombobox, StyleChangesDialog(변경사항 보기 다이얼로그 — 요소별 카드 + 행/요소/전체 granular 초기화), StyleCssView(CSS 뷰 — specified를 `selector{}` 블록으로 prefill 렌더(collapseTrbl로 4면 longhand를 shorthand 병합 표시), computeOverrides로 오버라이드 diff만 store 커밋·applyStyles 라이브, cssDraftStatus로 raw/commit 불일치·invalid/중복을 표시하고 진행/버퍼 전환 차단, baseline 행 삭제는 canonical 원복, focusedRef 포커스 가드 + lastCommittedRef 재구성 문자열로 커서 점프 방지, **AI 스타일 적용(aiStylingLoading true→false 전이)은 포커스 무관 강행 재동기화**(docSync — 포커스 중 stale doc이 다음 타이핑에 AI 값을 덮어쓰는 것 방지), 요소 전환은 key remount)·CssCodeMirror(@uiw/react-codemirror + @codemirror/lang-css + @codemirror/autocomplete lazy 에디터 — 프리셋 없이(theme="none") 배경·텍스트·거터·선택색을 semantic 토큰으로 몰아 라이트/다크 통일, selector 파랑/property 앰버 HighlightStyle, flex로 패널 가득 채움·DOM Tree 폰트 통일(--mono-size 13px·행간 18px)·가로 스크롤. 값 자동완성 + **var(--토큰) 안에서 디자인 토큰 family 자동완성**(tokenSuggest 공유 — 편집 탭 ValueCombobox와 동일 데이터·규칙, 커서 걸친 토큰 이름 전체 교체, 콤보박스 shadcn Command 톤), 색상 값/토큰 좌측 인라인 swatch(var(--x)는 computedFacet resolved 색), 완성 토큰은 회색 칩 + hover 원시값 툴팁(rightHintText), 값이 `;`로 안 닫힌 잘못된 선언은 앰버+취소선, 들여쓰기는 line decoration(선택자 줄 제외), `{}` 표시 truncate, **1행 선택자 줄 편집 잠금**(selectorLock changeFilter — 1행만 protected range라 본문 삭제는 통과, 가려진 `{` 훼손 방지), 행 hover·활성 줄 배경 강조, Tab 포커스 이탈), cssBlock(serializeCssBlock/parseCssBlock/computeOverrides — specified 대비 diff, 삭제 prop은 initial 원복 / collapseTrbl·expandTrbl — TRBL(padding·margin·inset·border-*·radius) longhand↔shorthand 순수 변환, 표시만 병합하고 diff는 longhand 유지), inlineCssText(serializeInlineStyle/parseInlineStyle — inline style Record ↔ CSS 텍스트 순수 변환기, top-level `;`·개행만 분리[괄호·따옴표 내부 보존]·중복 last-wins·`--*` 케이스 보존·`!important` opaque 왕복), StylePropEditors와 헬퍼 (propMetadata, propValues(열거형 CSS 속성 값 단일 출처 — PROP_VALUES/selectOptions/valueHintsFor, 폼 SelectProp 옵션과 CSS 뷰 property-aware 값 자동완성이 공유), tokenUtils, tokenSuggest(토큰 제안 순수 로직 — LIKE 필터·category/family 그룹·matchRange, ValueCombobox·CssCodeMirror 공유), cssSwatch(색 값 판별 순수 헬퍼 — CSS-wide 제외 + CSS.supports 게이트), selectorLock(CSS 뷰 1행 protected range 순수 헬퍼 — selectorLineProtectedRange), docSync(shouldResyncDoc — store→doc 강행 재동기화 판정 순수 헬퍼: 평시 포커스 스킵, AI 적용 시 강행), cssDraftStatus(evaluateCssDraft/isCssDraftUnapplied/hasUnappliedCssDrafts — raw draft가 commit과 어긋나는지 판정. 마지막 조각만 미완성이면 타이핑 중으로 보고 경고하지 않는다), styleHooks, TokenChip, colorLiteral, hexUtils, valueFormat — finalizeValue 라이브·커밋 공용 값 정규화[color hex 확장·length px 부착]/rightHintText 트리거 우측 힌트)
│   │   ├── settings/      # AI 모델 설정 (LlmConnectDialog, LlmConnectForm) + SettingsFooter(가이드·문의·후기 공용 푸터 — 일반/이슈/모델 설정 탭 공유) — SettingsTab sub-tab content
│   │   ├── connect/       # 플랫폼별 연결 폼 (JiraConnectForm, GithubConnectForm, LinearConnectForm, NotionConnectForm, GitlabConnectForm, AsanaConnectForm, ClickupConnectForm, SlackConnectForm(OAuth 전용 — ConnectMethodDialog/PatDialog 없음)) + PlatformConnectFlow(6개 폼이 복제하던 연결 셸 — 가용 수단 조회·OAuth 실행·수단/토큰 다이얼로그 분기. **요청 2개는 `BgRequest & { type: `${NoInfer<P>}.…` }`로 platform에 묶는다** — 그냥 `BgRequest`면 *다른 플랫폼의 유효한* 타입이 통과해 남의 토큰이 이 계정 슬롯에 저장되고, `NoInfer`를 빼면 P가 넓어져 게이트가 다시 무력해진다. 계정 리터럴은 `buildAccount` prop으로 **호출부에 남긴다**[셸에서 조립하면 excess property check가 사라진다]. jira·slack은 flow가 달라 이 셸을 안 쓴다), ConnectMethodDialog(OAuth/토큰 연결 수단 선택 다이얼로그) + gitlabInstanceUrl(self-managed 인스턴스 URL 정규화) — IntegrationsTab 'add' 서브탭 content. 각 폼은 계정 defaults(연결 후 기본값)를 편집한다: 위치(프로젝트·저장소·팀·워크스페이스…) + **기본 담당자**(Jira·GitHub·GitLab·Linear·Asana·ClickUp 6개 — Notion·Slack은 담당자 개념 없음) + 라벨·이슈 타입. 담당자 콤보박스는 이슈 모달의 *Fields/AssigneeCombobox를 재사용하고, **상위 값을 바꾸면 하위 담당자·라벨 defaults를 비운다**(다른 repo·project의 멤버라 무효)
│   │   ├── jiraFields/    # Jira 메타 필드 컴포넌트 (ProjectField(제출 시점 프로젝트 — jira.listProjects 서버 검색, 연동 탭 ProjectCombobox와 별개[그쪽은 계정을 직접 갱신]. 트리거에 aria-label + data-testid="jira-project-combobox"), IssueTypeField, AssigneeField, PriorityField, EpicField(상위 에픽 단일 선택), CcField, RelatesField(연결 이슈 복수 선택 — CcMultiCombobox 래핑, jira.searchEpics 검색), JiraIssueFields 묶음 + FieldCombobox 공용 셸 + useDebouncedSearch/useJiraConfig 훅 + resolve-epic-parent(에픽 이슈 타입일 때 parent를 연결 이슈 relates[]에 append·중복 제거) + SprintField(스프린트 선택 — agile API에 이름 검색이 없어 목록을 한 번 받고 cmdk 기본 필터에 맡긴다[onSearch 미전달], multiBoard일 때만 항목에 보드명 2줄, `data-testid="jira-sprint-combobox"`) + useSprintFieldMeta(존재 판정 훅 + peekSprintFieldMeta[캐시만 읽는 동기 조회 — 제출 시점 분석 축이 훅을 구독하면 다이얼로그를 안 열어도·타 플랫폼 제출에도 createmeta가 나간다]. 모듈 스코프 세션 캐시 키 `siteId|projectKey|issueTypeId`, 실패는 캐시에 안 남기고 `answered`로 '안 물어봤다/없다고 답했다/조회 실패'를 가른다) + sprint-sticky(resolveStickySprint — 복원된 sprintId 재검증, 부재·비유효만 값을 비운다) + project-change(resolveProjectChange — 프로젝트 전환 시 정리 규칙 단일 출처: issueTypeId·assignee·parent·relates·sprint 8키를 비우고[스프린트는 보드에, 보드는 프로젝트에 묶인다] priority·cc는 patch에 넣지 않아 보존[사이트 전역 스코프]. 같은 프로젝트 재선택이면 projectKey만 반환)) — SubmitFieldsDialog의 Jira 탭에서 사용. **IssueTypeField(`data-testid="jira-issue-type-combobox"`, 열림 상태는 부모가 controlled로 쥔다 — 전환 직후·잠긴 채 진입 시 이 콤보를 대신 열어 알리는 게 유일한 단서다)·EpicField·RelatesField는 조회 스코프를 `useJiraConfig()`가 아니라 prop `projectKey`로 받는다**(useJiraConfig는 연동 완료 게이트로만 남음) — 계정 기본 프로젝트가 아닌 곳에선 계정 기본 이슈타입 자동 주입을 값·라벨·체크마크 세 경로에서 모두 막는다
│   │   ├── githubFields/  # GitHub 메타 필드 컴포넌트 (RepoCombobox, LabelCombobox, AssigneeCombobox, CcCombobox, GithubIssueFields 묶음) — IntegrationsTab/IssueCreateModal 양쪽에서 controlled로 재사용
│   │   ├── linearFields/  # Linear 메타 필드 컴포넌트 (TeamCombobox, ProjectCombobox, LabelCombobox, PrioritySelect, AssigneeCombobox, CcCombobox, LinearIssueFields 묶음) — IntegrationsTab/IssueCreateModal 양쪽에서 controlled로 재사용
│   │   ├── notionFields/  # Notion 메타 필드 컴포넌트 (DatabaseCombobox, StatusSelect, PropertiesFieldset, PropertySelectCombobox, CcCombobox, NotionIssueFields 묶음, reconcileNotionFields 헬퍼) — IntegrationsTab/IssueCreateModal 양쪽에서 controlled로 재사용
│   │   ├── gitlabFields/  # GitLab 메타 필드 컴포넌트 (ProjectCombobox, LabelCombobox, AssigneeCombobox, CcCombobox, GitlabIssueFields 묶음) — IntegrationsTab/IssueCreateModal 양쪽에서 controlled로 재사용
│   │   ├── asanaFields/   # Asana 메타 필드 컴포넌트 (WorkspaceCombobox, ProjectCombobox, AssigneeCombobox, CcCombobox, AsanaIssueFields 묶음 — workspace 종속으로 project/assignee gate) — IntegrationsTab/IssueCreateModal 양쪽에서 controlled로 재사용
│   │   ├── clickupFields/ # ClickUp 메타 필드 컴포넌트 (WorkspaceCombobox, SpaceCombobox, ListCombobox, AssigneeCombobox, CcCombobox, ClickupIssueFields + initialClickupFields 묶음 — Workspace→Space→List 3단계 종속, assignee/cc는 workspace 종속) — IntegrationsTab/IssueCreateModal 양쪽 controlled 재사용
│   │   ├── slackFields/   # Slack 메타 필드 컴포넌트 (ChannelCombobox(public/private/im/mpim 플랫 리스트), ChannelIcon(채널 종류별 앞 비주얼 — im 프로필이미지/mpim Users/공개 #/비공개 자물쇠), MentionCombobox(users.list 멀티셀렉트 — CcMultiCombobox 재사용), SlackIssueFields + initialSlackFields 묶음 — 채널 단일 + 멘션 선택) — IntegrationsTab/IssueCreateModal 양쪽 controlled 재사용
│   │   ├── statusBadges/  # 이슈 목록 상태 badge — SubmittedBadge 디스패처 + 플랫폼별 SubmittedBadge(fetch + read-only fallback, Slack은 상태 폴링 없는 정적 "전송됨" 배지) + BadgeFallback(error/deleted/loading 3종 셸 — error·deleted는 7벌이 바이트 동일했고 loading은 6벌[Jira는 로딩 중 `return null`이라 제외]. `STATUS_CATEGORY_COLORS.deleted`의 유일한 프로덕션 소비처) 8종(Jira/Github/Linear/Notion/Gitlab/Asana/Clickup/Slack) + 동명 StatusBadge(편집 가능 popover) 7종(Slack 제외 — 메시지엔 상태 없음) + PlatformChip(브랜드 아이콘 + 라벨, Slack은 lucide Slack 아이콘) + constants(STATUS_CATEGORY_COLORS, LINEAR_STATE_TYPE_COLORS, LINEAR_STATE_I18N) + utils(classifyBadgeError — 404→deleted 분류)
│   │   └── notionStatusColors.ts  # Notion status option color → STATUS_CATEGORY (new/indeterminate/done) 매핑
│   └── lib/             # buildIssueMarkdown, buildIssueAdf, buildMarkdownIssueBody(github/gitlab 공용 GFM 본문 빌더 — build{Github,Gitlab}IssueBody가 platform·videoEmbed 옵션 주입 thin wrapper로 위임), buildGithubIssueBody, buildLinearIssueBody, buildNotionIssueBody, buildGitlabIssueBody, buildAsanaIssueBody, buildClickupIssueBody(markdown 본문 — 이미지 inline, 영상은 `?view=open` 링크(ClickUp은 본문 영상 embed 미지원), 첨부 섹션 없음(영상·로그·사용자첨부는 네이티브 task 첨부), CC는 ccMarkdownLine), markdownToAsanaHtml(markdown → Asana html_notes subset — table→정렬 <pre>, image→caption, h3+→<strong>), submitAdapters(신규 제출·재제출 두 진입점이 공유하는 `submitToX` 인자 + `setLastSubmitFields` 레코드의 **순수 매핑 16개** — `xSubmitArgs`/`xLastSubmitFields`. **아무것도 실행하지 않는다**: 실행을 어댑터로 끌어오면 설정 기록이 `markSubmitted`·에디터 reset 앞으로 당겨져 호출 순서가 바뀐다. 반환 타입 16/16 명시가 필수이고 `media()` 헬퍼까지 박아둔다 — 스프레드 소스는 excess property check를 비껴가 optional 필드 오타가 무음이 된다. `requireMediaUpload`는 `MediaGuard`를 github·notion·gitlab 입력 타입에만 믹스인해 4번째 플랫폼이 물려받으면 컴파일 에러. tabs에서 가져오는 건 전부 type-only), submitToJira(NormalizedSubmitResult; jira.submitIssue 메시지 래퍼 — buildIssueAdf 본문+첨부 dimension 주석+inline 처리, 6번째 어댑터로 submitTo* 대칭), submitToGithub, submitToLinear, submitToNotion, submitToGitlab(NormalizedSubmitResult; GitLab은 이슈 생성 후 logs.html 역링크 재업로드+description PUT 보강), prepareUpload(업로드 파일 수집·업로드 실행·inline ref 치환·requireMediaUpload 가드·logsDropped 계산 공용 — submitToGithub/submitToGitlab 공유, someUploadMissing 포함 / toUploadEntry — 파일 입력에 contentType을 붙여 업로드 엔트리로. clickup·asana가 공유하고 **slack만 로컬판을 유지**한다(slack.uploadFiles 페이로드가 `{filename, dataUrl}`뿐이라 contentType이 미사용으로 경계를 넘고, map 결과라 excess property check도 안 잡는다) / toInlineUploadFiles — 인라인 이미지를 `{refId, filename, dataUrl}`로. refId를 함께 실어 호출부가 조회용 파일명을 두 번째로 조립하지 않게 한다. prepareUpload·clickup·slack 공유), submitToAsana(task 생성 먼저 → per-file 격리 첨부 업로드 → 캡처·본문 인라인 이미지 GID로 updateTaskNotes 임베드), submitToClickup(task 먼저 생성(markdown_content) → per-file 격리 첨부 업로드 → 첨부 URL로 본문 재구성해 updateTaskMarkdown 2차 갱신 — Asana식 create-first 2-write), issueBodyShared(sectionLabel[labelOverride 우선, 공백뿐이면 기본 라벨로 폴백]·listItems[줄 분할+trim+빈 줄 제거]·footerMarkdown·emitMarkdownLogSummary[logsHref **optional 유지** — 없으면 평문 `logs.html`, 있으면 링크. 필수로 바꾸면 href를 안 넘기는 호출부 본문에 `[logs.html](undefined)`가 찍힌다] — 본문 빌더 8종에 복제돼 있던 공용 헬퍼의 단일 출처. 출력 매체가 다른 쪽은 안 모은다: slack은 mrkdwn, notion은 NotionBlock[], adf는 노드, buildIssueMarkdown은 HTML판을 각자 유지. `MarkdownContext`를 통째로 받지 않고 구조적 부분집합 `LogSummaryContext`를 받는다 — leaf가 빌더를 되참조하면 순환이라. builderLocaleWrap의 EXEMPT[진입점이 아니라 감싸진 구간 안에서만 불린다]), buildSlackBody(스레드 본문 mrkdwn 빌더 — 환경·스타일 diff(텍스트 줄)·로그 요약, selector/환경값 escapeMrkdwn, 미디어 본문 미임베드), submitToSlack(부모 메시지(제목+멘션)→스레드 본문(splitSlackText로 4000자 한계 분할 후 순차 전송)→첨부 2-step 업로드→permalink 순서, Slack 전용 — 메시지 앱이라 trail이 다른 submitTo*와 갈림), splitSlackText(SLACK_TEXT_LIMIT=3800 — Slack chat.postMessage text 4000자 한계 분할 순수 함수. 라인 경계로 쪼개되 코드블럭 안에서 잘리면 앞 조각을 ```로 닫고 다음 조각을 ```lang으로 재개[Slack이 임의로 쪼개면 펜스가 깨져 로그가 평문화·엉뚱한 섹션이 코드블럭에 씌워짐 — 실사용 회귀], 한 줄이 한계보다 길면[미니파이 JSON] 개행 글루 없이 독립 조각으로 하드 분할해 원문 보존. submitToSlack 전용), markdownToMrkdwn(markdown→Slack mrkdwn 변환 + escapeMrkdwn — 볼드 `*`·이탤릭 `_`·링크 `<url|text>`·헤딩→볼드줄, 코드블록 보존), slackPromotionLink(parseSlackChannelId[permalink `/archives/<channel>/` 정규식 추출 — `/client/`·트레일링 없음·빈 문자열→null] + postSlackPromotionReply[Slack 보존 이슈 승격 성공 시 원 메시지 스레드에 트래커 URL 댓글 best-effort — channel 파싱 실패/예외 모두 삼켜 항상 resolve, `slack.postMessage` threadTs 재사용·신규 메시지 타입 없음]), buildCaptureFiles(captureMode → {video, images, logs} 정규화 — logs는 buildLogsHtml로 단일 logs.html 생성, network/console/action 중 하나라도 있으면 emit. action은 supportsActionLog 게이트 경유), buildLogsHtml(dist-log-viewer 템플릿에 데이터 주입 → self-contained HTML, async — 무거운 데이터(로그·미디어·report)는 gzip-base64 __BUGSHOT_DATA__ 태그, meta는 평문 __BUGSHOT_META__ 태그로 분리, har/*LogJson은 raw에서 파생이라 비직렬화), aiLogsManual(logs.html `<head>`에 비가시 `<script type="text/markdown">`로 박히는 AI 소비 매뉴얼 정적 텍스트 — `__BUGSHOT_DATA__` 위치·base64 gzip 디코드 레시피·필드 소비법, buildLogsHtml이 head에 주입), buildReportData(logs.html Report 탭용 — enabled 섹션 inline 이미지 dataURL resolve + 미리 빌드한 markdown/html 클립보드 페이로드, deriveContextEnvRows로 env 평탄화), buildMarkdownContext(캡처 모드별 MarkdownContext 조립 — element/screenshot/video/freeform 분기 단일화. mergeStyleElements가 버퍼 + 현재 요소를 selector 기준 dedup·재인덱싱해 element별 섹션으로 직렬화 — before-${i}/after-${i} 파일명 단일 출처), cssDeclaration(filterValidCssDeclarations — CSS.supports로 유효 선언만 남기는 순수 필터, CSS 뷰 커밋 전 게이트), hasStyleChange(element styling→drafting 진입 게이트 — inline/class/text diff 유무 판정 순수 헬퍼, no-diff 진입 차단), sectionDefaultOpen(스타일 섹션 초기 펼침 판정 — specified/computed·known-default 기준 순수 헬퍼, propMetadata 사용), styleChangeGroups(buildChangeGroups: 버퍼+현재 요소를 selector 기준 그룹화 / countChangeRows: shorthand collapse 반영 행 수 / removeDiffRow: 행 단위 원복 — StyleChangesDialog 전용 순수 헬퍼), classDiff(diffClassTokens: class 토큰 집합 diff로 추가·삭제 토큰에 changed 플래그 부여(원본 순서 보존) / segmentsToMarkdown: changed 토큰만 **볼드**로 감싸고 마크다운 셀 메타문자(|·개행) escape — element 모드 style diff의 변경 토큰 강조 단일 출처, StyleChangesTable·마크다운 5종 빌더 공용. ADF/Notion/HTML은 각 포맷 전용 헬퍼), markdownCell(escapeTableCell — 마크다운 표 셀 메타문자 |·개행 무력화, 6개 본문 빌더·classDiff 공용 단일 출처), resolveDraftStyleElements(저장된 draft의 bufferedElements + 현재 element를 라이브 mergeStyleElements와 동일 규칙으로 병합 — DraftDetailDialog 렌더·재제출 단일 출처), applyDraftFieldEdit(DraftEditDialog 편집 결과를 patchIssue용 부분 패치로 계산하는 순수 헬퍼 — title은 최상위 title+draft.title 동시 갱신[리스트/검색 정합], section은 draft.sections[id], draft 전체 스프레드로 patchIssue 얕은 병합 대응·updatedAt 주입·id 불변·원본 불변), bodyBlocks(이슈 본문 블록 순서 단일 출처 — issueSections를 {kind:"section"}|{kind:"meta"} 순열로. media 엔트리 자리가 곧 미디어·스타일diff·로그요약 클러스터 위치이고 enabled 무관 항상 포함·중복은 첫 자리만[오염 시 미디어 소실·중복 렌더 방어]. 8개 빌더·클립보드·DraftingPanel·DraftDetailDialog 공용 — 앵커 규칙 복제 제거), composePreviewLayout(이슈 프리뷰 섹션 사이 media/logCards 슬롯 순서 규칙 — sectionIds의 "media" 자리에 삽입, 없으면 말미), zipLogsHtml(Notion 전용 — Cloudflare WAF가 평문 HTML/로그를 차단해 DEFLATE-mode zip 1파일로 래핑, CompressionStream + 손작성 ZIP 컨테이너), captureLogSupport(supportsConsoleNetworkLog/supportsActionLog — 모드별 로그 지원 매트릭스 단일 진실 + resolveCapturedLog/EMPTY_CONSOLE_LOG·EMPTY_NETWORK_LOG·EMPTY_ACTION_LOG — logs.html용 정규화: 지원인데 0건이면 빈 객체, 미지원이면 null[null은 "미수집"만 뜻하게]), initialJiraFields(Jira 제출 필드 prefill 단일 출처 — project가 제출 목적지 필드로 승격돼 **projectKey를 반환한다**(직전 제출값은 한 프로젝트의 스냅샷이라 프로젝트까지 함께 복원해야 하위 필드와 정합이 맞는다). 남는 게이트는 **사이트 일치 하나**(3번째 인자 currentSiteId = jiraSiteId(auth); last.siteId나 currentSiteId가 없으면 검증 스킵 — 마이그레이션이 없어 기존 레코드 전원이 이 경로다). 담당자·이슈타입은 last 우선이고 **Connect 기본값 fallback은 복원 프로젝트가 계정 기본 프로젝트일 때만**(다른 프로젝트에 없는 이슈타입으로 제출해 400 나는 걸 막는다). siteId는 판별자일 뿐이라 미반환[EditorIssueFields에 없는 키가 issueFields로 새는 것 차단]. editor-store.confirmDraft와 DraftDetailDialog 두 제출 경로가 공유 — 한쪽만 고치면 기본 담당자가 절반 경로에서만 붙는다. 다른 플랫폼의 initial*Fields는 각 *IssueFields.tsx에 콜로케이션돼 있으나 Jira만 store가 직접 써서 lib으로 승격[store → sidepanel/tabs import 금지]), markdown-logs-link(injectLogsMarkdownLink — 마크다운 본문의 평문 logs.html을 [logs.html](url)로 치환하는 업로드 후 후처리, adf-logs-link의 markdown 형제. Linear description 패치 전용), markdownToAdf(markdown-it→ADF 변환), markdownToNotionBlocks(markdown-it→Notion 변환), renderMarkdown(markdown→HTML 프리뷰), compactImage(이미지 리사이즈·webp 변환), uploadMime(guessUploadMime — 업로드 파일 확장자→MIME 공용, 5개 submit 헬퍼 공유), findClosingToken(markdown-it 토큰 탐색), buildAiDraftPrompt, buildAiDraftRequest(AI Draft LLM 요청 조립 — 절삭 거친 systemPrompt를 받아 supportsImages에 따라 images만 결정), mergeAiDraftSections(mergeAiSectionsPreservingBlocks — AI 텍스트 덮어쓰기 시 기존 섹션 inline 이미지를 상단·코드블럭을 하단에 보존 + 예산 절삭으로 프롬프트에 안 실린 섹션은 사용자 원문 보존[판정 기준은 stripPreservedContent — 보존물 뺀 산문]), markdownBlocks(extractCodeBlocks/stripCodeBlocks/stripPreservedContent — 들여쓰기 0 fence 추출·제거 + 보존물[이미지·코드블럭] 뺀 산문 산출, selectDraftSections·merge·renderLogRefs 공유 단일 기준), renderLogRefs(MAX_LOG_REFS=3·renderLogRefBlocks/codeBlockMarkdown/appendLogBlocks — AI가 지목한 logRefs를 후보 역참조로 검증해 store 원본을 코드블럭 마크다운으로 렌더·description 끝에 append[동일 내부 텍스트 dedup — Tiptap 왕복 안전], 상한 초과 시 에러 ref(n*/c*)를 매칭 ref(m*)보다 앞으로 안정 정렬 후 상위 3개만 slice[검증된 에러 로그 생존·매칭부터 절삭, 구 전량 폐기 회귀 수정] + console.warn), buildAiStylingPrompt(isDeniedStyleProp 키 필터 + isDeniedStyleValue 외부 url() 값 필터), extractJson(LLM 응답에서 JSON 본문 추출 — 두 AI 프롬프트 빌더 공용), aiStylingPostProcess, ai-provider(BYOK 프로바이더 팩토리·프리셋·멀티턴 세션 + ProviderCapabilities 3축[promptStyle·supportsImages·contextBudgetChars] — byokCapabilities가 로컬 엔드포인트를 나노와 같은 compact 좌표로 판정), aiLanguage(AI 초안·재현 단계 출력 언어 — AI_LANGUAGE_OPTIONS 프리셋 9종[value=프롬프트에 박히는 영어 이름 / label=자기 언어 표기, auto는 미포함이라 UI가 i18n 라벨로 별도 행을 얹는다] + resolveAiLanguage[auto·undefined·오염값을 호출 시점 locale로 해석 — 저장 시점 스냅샷이면 UI 언어를 바꿔도 AI만 옛 언어로 남는다] + normalizeAiLanguage[대소문자 관용 없음 — 값이 프롬프트 지시문에 그대로 박히므로 임의 문자열을 통과시키지 않는다] + aiLanguageLabel. **settings-ui-store가 값으로 import하므로 순수 상수·함수만** — 이 스토어는 background 번들에 들어간다. LocaleMode는 `@/i18n/locales`에서 type-only import라 런타임 순환 없음. localeAiPreset(폴백 금지 `Record<LocaleMode, AiLanguagePreset>` — resolveAiLanguage의 auto 해석 단일 출처. 이전의 `locale === "ko" ? "Korean" : "English"` 3항은 새 로케일을 조용히 English로 답해 UI만 그 언어이고 초안은 영어가 된다)도 여기 산다. 소비처는 draftRich 하나이고 **compact은 의도적으로 로케일에 묶인 채** 남는다[nano의 outputLanguage 고정·영어 few-shot]), aiCancelSlot(getAiCancel — editor-store.aiCancel 라이브 리더. useAiRun이 "슬롯이 아직 내 canceller인가"를 판정하는 입력이고, 모듈 레벨 함수라 참조가 안정적이어서 훅 config에 그대로 실린다. 헬퍼가 store를 직접 import하지 않게 막는 간접층 — AI 3콜사이트 공용), llmErrorToast(LLM 에러 분기 토스트 공용 — contextOverflow/auth/empty/quota/overloaded + fallback, AI Draft·AI Styling·재현단계 자동채움 공유), generateReproPrefill(generateReproStepsWithAI — 액션 로그 요약을 AI draft 파이프라인의 stepsToReproduce 단일 섹션으로 좁혀 재사용, title 무시하고 steps만 추출, 성공 시 steps 문자열·빈/파싱실패 시 LlmEmptyResponseError throw·provider 에러 전파), reproSectionEnabled(isReproSectionEnabled — 본문 설정의 재현 과정 섹션 on/off 판정 단일 출처. DraftingPanel의 자동 채움 발화 게이트와 SettingsTab의 채우기 토글 활성이 같은 답을 보게 한다 — 하드코딩이 갈리면 토글은 켜졌는데 안 채워지는 식으로 조용히 어긋난다), video-mime(MediaRecorder mime 우선순위·확장자 매핑), video-thumbnail(generateThumbnail — 영상 blob 첫 프레임을 webp dataURL로. video-recorder에서 분리한 이유는 apply-trim[유닛 테스트 대상]이 video-recorder를 정적 import하면 picker-control·annotation-control까지 테스트 그래프에 딸려 오기 때문. 브라우저 전용[BROWSER_BOUND_EXACT], 실패는 세 호출부 모두 빈 문자열로 흡수), aiLoadingPhrases(AI 로딩 오버레이 문구 로테이션 순수 함수 — aiLoadingSurface[styling>draft>repro 우선순위 판정]/aiLoadingPhraseKey[표면당 5문구를 step%5 무한 루프]), trackViewport(getDisplayMedia track settings→{width,height} 해상도 — 화면 녹화 viewport 메타, 미제공 시 undefined), environmentRows(재현 환경 row 필터·모드별 readonly 파생 헬퍼 — filterEnvironmentRows가 {label,value}로 재조립해 apiHostRow의 source 표식을 모든 출력에서 벗기는 단일 초크포인트), apiHostRow(재현 환경 API Hosts 자동 행 — registrableDomain[tldts PSL, allowPrivateDomains로 vercel.app·github.io 같은 호스팅 도메인을 서로 다른 조직으로 가름] / deriveApiHostsRow[페이지와 동족인 다른 hostname을 요청 수 내림차순 나열, http(s)만·scheme·port·path·query 버림] / apiHostRowFor[supportsConsoleNetworkLog+logsAttach+networkLog 3게이트 단일 진입점 — 게이트를 컴포넌트가 아니라 여기 두는 이유는 DraftingPanel에 테스트 파일이 없어 element 누출이 green으로 통과하기 때문] / isApiHostsUndetermined[hydrate가 draft를 동기 복원하는데 networkLog는 IDB promise로 뒤따라 오는 구간을 '로그 없음'과 구분] / syncApiHostsRow[주입·갱신·제거·사용자 수정 보존 판정. 무변화면 입력 rows 참조를 그대로 반환해 호출부가 write를 생략 — setDraft가 전체 교체라 무조건 write하면 draft identity가 매번 바뀌어 루프] / stripApiHostsRows[미수정 행만 걷어내는 안쪽 술어 — source·label·value===lastDerived 셋이 전부 맞을 때만] / environmentForSubmit[제출 ctx 조립 경로의 게이트 — 모드·로그 첨부를 통과하면 rows 그대로, 아니면 strip. lastDerived의 undefined→null 흡수는 여기서만 하고 그게 구 draft를 no-op로 만든다. 라이브 buildEditorCapture와 저장 draft 재제출·표시 DraftDetailDialog가 공유해 화면과 본문이 갈리지 않는다]), osInfo(UserAgentData 기반 OS 이름·버전 파싱), buildConsoleLogJson, buildActionLogJson(액션 로그 → JSON 첨부), buildHar, buildLogSummary(buildNetworkLogSummary/buildConsoleLogSummary/buildActionLogSummary — AI 프롬프트·이슈 본문용 요약 + networkErrorCount[본문 빌더 8개가 인쇄하는 "에러 N건"의 단일 출처 — errors[]는 method+path+status로 dedup되고 5개로 cap된 표시용 샘플이라 개수로 쓰면 동일 요청 반복 실패가 1건으로 축소된다]), logToCodeBlock(serializeNetworkRequest/serializeConsoleEntry — 로그 1건 → 코드블럭 텍스트 {text, language?} 순수 직렬화. body descriptor 5종 라벨·16384자 truncate·pending/status0 방어·WS는 헤더 라인만, neutralizeFences로 라인 시작 백틱 런 무해화[tiptap-markdown이 3백틱 하드코딩이라 fence 조기 종료 → 8개 빌더 전부 오염]. 표시용 NetworkLogContent.formatBody와 요구가 달라 비공유), highlightJson(tokenizeJson/JSON_TOKEN_CLASS — 삽입 로그 코드블럭의 JSON 토큰화·색 클래스 단일 출처. TiptapEditor의 ProseMirror inline decoration과 renderMarkdown의 markdown-it highlight 훅이 공유해 에디터·프리뷰가 안 갈린다. ```json 펜스만 칠하고 raw 본문은 평문 유지), formatBytes, formatTimestamp, attachmentLimits(사용자 첨부 개수·합계 하드캡 takeWithinLimits + 플랫폼 단건 경고 checkAttachmentLimits 순수 헬퍼), attachmentDimensions(이미지·영상 첨부의 원본 픽셀 크기 측정 — Jira attachment input용), fileMeta(fileCategory/fileExtLabel — 파일 MIME·확장자 분류), downloadAttachment(IndexedDB 첨부 Blob 로컬 다시 받기 — owner+fallback owner), downloadCapture(미디어 섹션 다운로드 + blob 다운로드 트리거 — imageExtFromDataUrl[dataURL mime→확장자, image 아니면 webp 폴백]/downloadImageDataUrl[스크린샷]/downloadVideoBlob[recordingFilename]/triggerDownload[blob URL `<a download>`], Drafting·Preview 공용), buildEditorCapture(이슈 제출과 패널 로그 다운로드가 동일 logs.html을 만들도록 ctx·캡처입력 단일 출처 — buildEditorMarkdownContext[editor/settings store→MarkdownContext, 제출 다이얼로그 buildCtx 이관]/buildEditorLogsCaptureInput[buildCaptureFiles 입력 중 userAttachments 제외분]/downloadEditorLogsHtml[buildCaptureFiles로 영상·이미지 임베드+report 포함 logs.html 다운로드]. IssueCreateModal·Drafting·Preview 공용), resolveInlineImages(resolveSectionImages 포함 — enabled paragraph 섹션의 inline 이미지를 dataURL로 resolve, PreviewPanel·buildReportData 공용), inlineImageAnnotation(annotateInlineImage[최초 1회 원본을 inlineImageOrigins에 백업 후 표시 blob 교체·현재 blob이 null이면 백업 스킵]/resetInlineImage[원본 복원+origin 삭제] — 본문 삽입 이미지 어노테이션 blob 스왑. refId 고정이라 markdown·세션·prune·제출 경로 무변경 재사용, TiptapEditor ImageAnnotationNodeView 소비), diffAnnotation(diff table 주석 전용 순수 헬퍼 — SnapshotSlot 타입 + routeDiffAnnotation[현재 vs 버퍼 쓰기 라우팅. mergeStyleElements의 조건을 그대로 비춰 **현재 diff ≥ 1건 + 키 일치**일 때만 current로 보낸다 — 키만 보면 diff 0건일 때 렌더되는 버퍼 카드에 무음 no-op] + annotationSource[오버레이 배경 = 슬롯별 annotated ?? raw. 실패 모드가 ??누락이 아니라 슬롯 교차라 JSX 인라인으로는 못 고정]), liteSnapshot(toLiteSnapshot — 세션 쿼터 초과 2차 시도용 경량 스냅샷. top-level 이미지 4종[before/after × raw/annotated]과 buffered 항목 내부까지 명시 null화, captureContext는 남긴다[기준이 사라지면 resolveCaptureRect의 0×0 가드가 함께 풀린다]. snapshotFromState는 손나열 + getState 직독이라 옮기지 않고 훅에 남겼다), capture-error(isActiveTabPermissionError — captureVisibleTab/tabCapture의 activeTab 만료 에러 식별), log-merge(cross-page 로그 누적 — mergeLogItems id 기준 dedup·시간순 정렬·maxEntries 트림, topOrigin 주면 cross-origin 우선 evict로 top-page-origin 보존, trimByTime, rebuildNetworkLog/rebuildConsoleLog/rebuildActionLog, ACTION_MAX_ENTRIES, isLogFrozen), logOrigin(originKey/originHostLabel/originCounts/UNKNOWN_ORIGIN — pageUrl→origin 파생·출처별 개수 집계, opaque는 unknown 그룹, OriginFilterBar 공용), log-persist-guard(createLogPersistGuard — 수신부 IndexedDB write를 ~1s trailing throttle로 묶고 마지막 payload만 저장, save throw 시 pending 보존해 재시도, replay trim은 discard), trailing-throttle(createTrailingThrottle — sidepanel 전용 trailing throttle 복제본. content/log-throttle과 의도적 분리: log-persist-guard가 @/content/log-throttle을 import하면 recorders-entry 청크가 async loader가 되어 pre-arm 무력화), escapeHtml(HTML 이스케이프 4종 단일 출처 — buildIssueMarkdown·renderMarkdown·markdownToAsanaHtml 세 벌 중 Asana용만 `"`를 빠뜨려 있었다), tab-scope(isForeignTabMessage — chrome.runtime broadcast 중 다른 탭 메시지 드롭 판정. fail-closed: 내 tabId 미해소·해소 실패 구간도 드롭한다[열어두면 남의 탭 logClear 하나로 누적 로그가 통째 소실]), log-prearm-filter(shouldDropPreArmEntry — preArm 마커 엔트리는 logClear 경계[lastLogClearAt] 우회 보존, 그 외 과거 엔트리는 폐기), logRow(영상-로그 동기화 행 렌더 공용 — formatRelativeTime MM:SS, syncRowClass active 보더 슬롯), linkify(tokenizeLogText — 로그 자유텍스트에서 https?:// URL 토큰 분리, 후행 구두점·끝의 :line:col을 href에서 제거, LinkifiedText 전용 순수 토크나이저), actionInline(splitTemplate: i18n 동사 템플릿 {slot} 분할 / resolveClickTarget: 접근성 이름→태그 폴백 클릭 대상 뷰 / actionSearchText: 액션 엔트리→검색 매칭 텍스트[통합 타임라인 matchesTimelineItem 소비] — ActionLogContent inline 렌더 전용 순수 헬퍼), useScrollToEntry(스크롤-to-entry + 필터 리셋 공용 훅 — ConsoleLogContent/NetworkLogContent/ActionLogContent 공유), useDebouncedValue(범용 값 디바운스 훅), ccMention(CC 멘션 빌더 — ccMarkdownLine/ccAdfParagraph/ccAsanaHtml/injectAsanaCc + CC_SENTINEL, 7개 buildXXXIssueBody 공용), track-submit(submitEventProperties 순수 매핑 + trackSubmit/trackDisconnect fire-and-forget — 이슈 제출 성공/실패·플랫폼 연결 해제를 analytics.capture로 집계), listKeymap(shouldLiftListItem — 빈 list item 시작 Backspace로 리스트 종료할지 판정하는 순수 헬퍼, TiptapEditor keymap 전용), hrInputRule(shouldInsertHrAfterBreak — hardBreak(Shift+Enter) 뒤 `---`를 수평선으로 변환할지 판정하는 순수 헬퍼, StarterKit 기본 규칙 사각지대 보충·TiptapEditor 전용), scroll-capture-plan(planScrollCapture/tileDrawRect/tilePixelRect/stitchGeometry — 페이지 전체 캡처 타일 계획·스티치 좌표 순수 함수. 캡 MAX_SCROLL_TILES=20·MAX_CANVAS_HEIGHT_PX=32000·MAX_OUTPUT_PIXELS=4M[session 10MB 쿼터 lite 강등 방지]. 경계는 시작·끝 각각 반올림 — 높이를 따로 반올림하면 분수 배율에서 타일마다 ±1px 틈), crop-rect(clampCropRect — 크롭 rect를 캡처 이미지 경계 안으로 클램프, 줌≠100%의 투명 픽셀 방지. capture.ts:cropImage 단일 소비), capture-basis(resolveCaptureRect — 이번 캡처가 크롭할 rect 결정: 한 변이라도 0이면[요소가 사라짐] before 시점 rect를 재사용하되 viewport·scrollX·scrollY가 전부 같고 top frame일 때만, 하나라도 다르면 null[잘못된 영역 대신 이미지 없음]. context를 안 넘기는 스코프 밖 경로는 현행 유지 / resolveExpandRequest — 확장 요청 파생 단일 출처: context를 넘긴 경로는 before가 확정한 판정만 따르고 호출부 opt-in을 무시한다[호출부가 판정하면 기준을 정하는 문이 호출부마다 생긴다] / sameCaptureBasis — 두 기준이 같은 크롭 범위를 뜻하는지[확장 대상 조상만 비교, 확장 거부와 기준 없음은 같다]. useBufferThenSwitch·picker-control이 버퍼 커밋 직전 대조해 기준이 갈렸으면 after를 버린다. capture.ts가 BROWSER_BOUND_EXACT라 판정을 여기로 내려야 테스트로 고정된다), recordModeMeta(recordModeMeta: RecordingSource("tab"|"screen") → {icon, labelKey} 매핑 — 캡처 진입 화면 녹화 버튼 아이콘·라벨 단일 출처, IssueTab·테스트 공용 순수 헬퍼), codeCollapse(CODE_COLLAPSE_LINE_THRESHOLD=15 · countCodeLines/shouldCollapseCode — 코드블럭 접기 임계·줄 수 판정 순수 코어. markdown-it 후행 개행 1개를 벗겨 ProseMirror textContent와 같은 수를 내는 게 두 표면 일치의 핵심. 임계값은 여기가 단일 출처 → CSS엔 --code-collapse-lines로 주입), codeCollapseShell(createCodeCollapseShell — pre를 접기 wrapper+pill+행 번호 열로 감싸는 vanilla DOM 셸. 번호 열은 pre **밖**에 absolute로 얹혀 가로 스크롤에서 빠지고 접힘 높이에 함께 잘리며 복사·직렬화를 오염시키지 않는다(자릿수만 `--code-gutter-digits`로 CSS에 넘기고 폭 환산은 CSS가, 접힘 시엔 보이는 줄까지만 생성). 펼침 상태는 셸 내부 클로저(외부 노출은 readonly 판정뿐), unwrap()은 preview 훅 전용[NodeView는 PM이 wrapper째 걷어가므로 호출 금지]. TiptapEditor NodeView·useCodeCollapse 공용), blockActions(createBlockActions/createBlockIcon — 블럭 우상단 아이콘 버튼 그룹의 vanilla 팩토리. lucide-react가 iconNode를 공개 안 해서 React 밖 DOM(NodeView·프리뷰 후처리)에선 못 쓰므로 lucide 원본 path를 인라인한 두 번째 출처 — lucide 버전 올릴 때 ICON_PATHS 대조. SVG에 내재 width/height를 박는 게 핵심[viewBox만 주면 컨테이너를 꽉 채워 버튼을 터뜨린다]. codeCollapseShell의 extraActions 경유로 코드블럭 복사·삭제에 쓰이고, TiptapEditor ImageAnnotationNodeView가 본문 이미지 액션([초기화][주석][삭제])에 재사용 — setHidden으로 초기화 버튼 조건부 노출, rotateCcw/pencil 아이콘 추가. 블럭 무관 설계) 등 순수 유틸
│       └── prompts/  # AI 프롬프트 본문·컨텍스트 조립 — caps(PROMPT_CAPS 축별 캡 + MAX_TITLE_LENGTH 단일 출처), context(oneLine 개행 무력화·selectDraftSections 실린 섹션 단일 출처·selectStyles/selectRelevantTokens 캡 선별·buildStyleDeltaBlock 멀티턴 delta), logCandidates(selectLogCandidates/selectMatchedLogCandidates/candidateRefs/canRequestLogRefs/findCandidate — 프롬프트에 인쇄될 로그 후보 단일 출처[support 게이트→network dedup→스타일별 캡→ref 부여]·description 활성 게이트·ref 역참조. matched(m*)=유저·캡처 term과 exact-match되는 성공 2xx 응답 후보[rich+requests 배선 전용, tier/hit/recency 랭킹·cap 3]), queryTokens(tokenizeUserQuery — 유저·캡처 텍스트를 매칭 검색 term으로 쪼개는 3-tier[quoted/ident/word] 토크나이저·소스 길이 캡), responseDigest(digestResponseShape — 2xx 응답 본문의 shape 다이제스트[키·타입, 값 제외]·safeKey/dictionary collapse로 데이터성 키 redact), draftStyleElements(resolveAiDraftStyleElements — 스타일 편집한 요소 목록을 프롬프트용으로 정규화하는 단일 출처[legacy 단일 selector 폴백·전역 diff/이미지 캡 배분]. describeAiDraftElementImages는 실제 전송되는 이미지 인덱스를 계산해 프롬프트 문구와 첨부 순서를 맞추고, selectAiDraftTokens는 변경 요소가 정확히 하나이고 현재 selection과 같을 때만 디자인 토큰을 실어 복수 요소에서 토큰이 엉뚱한 요소로 귀속되는 걸 막는다), draftCompact/draftRich·stylingCompact/stylingRich(promptStyle별 본문 — Record 디스패치라 style 추가 시 컴파일 에러. draftRich만 matched 섹션 인쇄. styling 두 본문은 페이지 파생 값을 `<untrusted_page_context>` JSON 블록으로 격리), promptBudget(fitDraftContext 3단 절삭 사다리 + isPromptOverBudget 실측 게이트 — level≥1에 requests 삭제 편입)
├── store/               # Zustand 스토어 (editor/issues/settings/settings-ui), chrome-storage(Zustand↔chrome.storage 바인딩), blob-db(IndexedDB v8 — 이미지·비디오·네트워크/콘솔/액션 로그·에디터 인라인 이미지(inlineImages)·인라인 이미지 어노테이션 전 원본 백업(inlineImageOrigins)·사용자 첨부 파일 저장, 첨부 Blob은 pending:${tabId}→issueId rekey·orphan prune(inlineImages+inlineImageOrigins는 동일 globalRefs로 함께 정리), blobToDataUrl/dataUrlToBlob 유틸), issues-migrations(이슈 스토어 마이그레이션)
│                        # settings v11: accounts: { jira?, github?, linear?, notion?, gitlab?, asana?, clickup?, slack? } + lastSubmitFields per platform + global titlePrefix (v10: slack 추가 — 새 필드 optional이라 버전 마커만 bump / v11: jira relates 단일 relatesKey·relatesLabel → 복수 relates[{key,label}] shape 변경, migrateToV11이 직전 제출값 이관)
│                        # settings-ui v11: locale(v1부터 — 버전 분기 없이 rehydrate마다 normalizeLocale 재정규화, 다운그레이드 방어) + aiLanguage(v10 추가 — normalizeAiLanguage 게이트, 값이 프롬프트 지시문에 그대로 박히므로 관용 없음) + bodyLocale(v11 추가 — 이슈 본문 스캐폴딩 언어. "auto"|LocaleMode이고 normalizeBodyLocale 게이트가 오염값을 DEFAULT_LOCALE이 아니라 **auto로** 되돌린다[영어로 굳히면 화면 언어를 따르던 사용자의 제출 본문이 조용히 영어가 된다], migrate 라인은 mergePersistedSettings 재정규화와 기능적으로 중복이나 도입 시점 기록용으로 둔다) + LlmConfig { baseUrl, apiKey, modelId } + replayEnabled(30s Replay 토글) + recordingMode("tab"|"screen" 녹화 모드 선택, v6 추가·기본 "tab") + attachmentsEnabled + styleEditorView("form"|"code" CSS 코드 뷰/폼 뷰 토글, v7 추가·기본 "form") + autoReproPrefill(재현 단계 AI 자동채움 토글, v8 추가·기본 true) + issueSections 순서 배열에 media 엔트리 편입(v9 추가 — 본문 구성 드래그 재정렬, normalizeSections가 "정확히 1개" 보장·migrate와 rehydrate merge 양쪽에서 호출, reorderIssueSections는 arrayMove를 스토어에 인라인[background 번들에 dnd-kit 유입 차단]) 전부 chrome.storage.local 영속
│                        # issues v5: entry에 platform: PlatformId 필드 + notion 한정 메타 (notionPageId/notionDatabaseId 등) + slackPreserved(Slack 공유 시 데이터 보존·승격 대상, markSlackShared가 세팅)
├── log-viewer/          # Self-contained HTML 로그 뷰어 (별도 Vite 빌드 → dist-log-viewer/index.html 단일 파일). Light-only, Report/Console/Network/Action Log 탭(아이콘+배지 — Report는 이슈 본문 프리뷰+마크다운 복사, IssuePreviewView 재사용, 기본 선택에선 제외), 하단 footer(App.tsx 인라인 — 이슈 바로가기+탭별 다운로드 — har/console·actionLogJson은 raw 로그에서 다운로드 시점 즉석 생성). 영상 또는 스크린샷 임베드 시 ResizablePanelGroup으로 좌(미디어)/우(4탭: Report·Console·Network·Action) 분할, 영상 모드는 좌측을 다시 세로 분할(상단 VideoPlayer + 하단 TimelinePanel 병합 타임라인) + 영상↔로그 동기화(스크린샷은 정적이라 동기화 없음 / 영상 모드는 로그 행·mm:ss 칩·타임라인 행 클릭 시 seek, 마커 클릭 시 타입 탭 전환+seek). markers.ts(buildMarkers — 탭별 console/network/action 마커 생성 + variant 색 분류; 단 타임라인 렌더는 trim-markers.ts:buildErrorMarkers로 3타입 통합). components/VideoPlayer.tsx(커스텀 비디오 플레이어 — 재생/일시정지/다운로드/이슈 오버레이/마커 연동), components/ImageViewer.tsx(screenshot 모드 좌측 정적 이미지 뷰어 — VideoPlayer 컨테이너·배경 공유, 하단 컨트롤 없음), components/IssueTitleOverlay.tsx(VideoPlayer·ImageViewer 공용 상단 이슈 제목 오버레이 — 호버 시 dim+텍스트 노출), components/ProgressBar.tsx(타임라인 프로그레스 바 + 재생위치 + 시각 툴팁 — 마커 렌더는 TimelineMarkers에 위임), components/TimelineMarkers.tsx(variant별 teardrop 마커 핀 + portal 호버 툴팁 — ProgressBar와 sidepanel TrimTimeline 공용 추출), components/TimelinePanel.tsx(영상 아래 병합 타임라인 패널 — 단일선택 타입 필터 Tabs+개수 배지+검색 Input, playhead ref-relay 구독으로 현재 행 하이라이트, 행 클릭 시 seek+로그탭 전환 발화), components/TimelineRow.tsx(타임라인 1행 React.memo — LogSeekChip·종류 아이콘·내용, 네트워크는 method를 자연어 동사 문장으로 조립[경로는 파랑 밑줄이나 링크 이동 아님], sidepanel 렌더 헬퍼 다수 재사용). timeline.ts(findActiveIndex/toVideoSeconds/formatPlayerTime 순수 헬퍼), timeline-merge.ts(순수 모듈[node-testable] — buildTimeline[3종 로그 absTs 병합·안정정렬, KIND_ORDER action<network<console tiebreak]/matchesTimelineItem[타입 필터+검색]/timelineFillClass[행 면색을 log-colors에 위임]/netVerbKey·NET_VERB_KEYS[method→i18n 동사 키 닫힌 집합]). 경량 i18n — `DICTS`(폴백 금지, `Record<LocaleMode, …>`)에서 `detectLocale(navigator.language)`로 사전 선택. **사용자 UI 설정이 아니라 읽는 사람의 브라우저 언어를 따르는 게 의도** — logs.html은 확장을 설치하지 않은 팀원이 여는 산출물이라 리포터의 로케일을 박제하면 안 된다
├── i18n/                # 다국어 (ko/en 로케일, t()/useT() 훅). ko.ts/en.ts는 namespaces/{common,app,issue,editor,integrations,settings,logs,ai}.ts 8개 도메인 번들을 spread merge하는 진입점. index.ts는 `locales` 레지스트리를 export(테스트가 순회)하고 `withLocale`(이슈 본문 언어 전역 스왑 — try/finally 복원 + Promise 반환 거부[동기 전용], 인자는 정규화하지 않는다[trusted by construction]. 래핑 지점은 본문 빌더 진입점 9파일 11함수이고 누락은 builderLocaleWrap.test.ts 소스 스캔이 red로 잡는다. 상세는 ARCHITECTURE.md "본문 언어 전역 스왑")도 여기 산다. locales.ts(로케일 레지스트리 단일 출처 — LOCALES/LocaleMode/BASE_LOCALE[ko, 키 집합 정의]/DEFAULT_LOCALE[en, 폴백]/BCP47 + normalizeLocale[저장값 게이트, 대소문자 관용 없음 — 다운그레이드로 미지 로케일이 persist에 남으면 사전 조회가 undefined라 t()가 죽는다] + detectLocale[브라우저 태그 해석, 관용적 — 전역을 안 읽고 인자로 받아 순수 함수 유지] + matchLocaleTag[최장 매칭 우선 — 선언 순서로 고르면 `pt` 뒤에 `pt-BR`을 추가했을 때 긴 쪽이 영구 도달 불가] + LocaleTable/localeValue[**폴백 허용** 테이블 전용 — 기본 로케일 항목 필수, 나머지 선택] + BodyLocale/normalizeBodyLocale/resolveBodyLocale[이슈 본문 언어 축 — aiLanguage 3종 세트를 복제하되 도메인이 LocaleMode라 여기 둔다. background·log-viewer가 이 파일을 이미 공유하고 aiLanguage.ts는 사이드패널 전용이라 끌어갈 수 없다]. **런타임 import 0이 불변식** — log-viewer가 vite alias를 우회해 상대경로로 이 파일을 끌어가므로 store가 딸려오면 그 번들과 SW가 깨진다. locale-registry.test.ts가 소스 스캔으로 강제). bg-init.ts(service worker 로케일 초기화 — storage.local app-settings에서 locale 파생 + onChanged 동기화, normalizeLocale 경유)
                      # **폴백 금지 테이블 5개**(로케일 추가 시 컴파일러가 채우라고 해야 하는 것): i18n/index.ts의 `locales` · locales.ts의 `BCP47` · sidepanel/lib/aiLanguage.ts의 `LOCALE_AI_PRESET` · sidepanel/lib/localeLabels.ts의 `LOCALE_LABELS` · log-viewer/i18n.ts의 `DICTS`. 폴백을 허용하면 각각 t() 크래시 / 날짜 포맷 오류 / 무음 영어 AI 초안 / 셀렉터에 안 떠서 못 고름 / raw 키 노출로 샌다. 반대로 **폴백 허용**은 프롬프트 스캐폴딩 계열(draftRich의 SECTION_DESC_BASE·MODE_HINTS·EXPECTED_SPLIT_HINT, draftCompact의 SECTION_DESC)과 표시 스타일(issueListUtils의 `MONTH_STYLE`, lib/external-links의 USER_GUIDE_URLS) — 영어 뼈대 + "Write in X"가 설계이거나 누락이 "덜 예쁜 포맷"에 그치는 축이라 en 재사용이 정답
                      # **로케일을 추가할 때 채워야 하는 사전은 셋이다**: `src/i18n/namespaces/*.ts` 8개 · `src/log-viewer/i18n.ts` 복제본 · `public/_locales/<code>/messages.json`(manifest·chrome.i18n용 4키). 셋 다 그물이 있다 — 앞의 둘은 컴파일 + 대칭 테스트, 마지막은 `src/i18n/__tests__/manifest-locales.test.ts`
├── lib/                 # 공용 유틸 (bg-client(BgError·sendBg — 사이드패널·content→background RPC 단일 창구. `chrome.runtime.lastError`는 평범한 Error로, `ok:false`는 status·body를 실은 BgError로 reject한다. **reject 전에 `isOAuthRefreshFailed`면 `onOAuthExpired`를 전역 발화**하므로 호출부의 `.catch(()=>{})`로는 못 막는다[POSTMORTEM 2026-08-13] / isOAuthRefreshFailed·isOAuthCancelled·isOAuthNotConfigured — body 플래그 **전용** 판정, 메시지 텍스트 매칭 안 함·셋은 서로 배타 / getOAuthErrorPlatform — 8플랫폼 화이트리스트. types/messages.ts에 있던 것을 옮겼다 — 그 파일은 선언 전용이고 `src/types/`는 커버리지 분모에서 빠진다. **이 파일은 leaf가 아니다** — `@/i18n`을 런타임으로 물어 `settings-ui-store`→zustand persist까지 끌고 온다(이동 전 `types/messages.ts`가 갖던 모양 그대로라 순증은 0). 그래서 **store가 이걸 물면 아래 app-events가 끊어둔 순환이 되살아난다** — "`src/lib/`에 있으니 안전"으로 읽지 않는다), app-events(createEmitter<A>() 팩토리 + emitter 7종 onOAuthExpired·onPickerUnavailable·onPickerPermissionExpired·onPickerIframeUnsupported·onBlobSaveFailed·onStateSaveFailed·onSessionSaveExhausted — bg·store·picker가 사이드패널 UI에 올리는 단방향 알림 채널. store가 App.tsx를 import하지 않고 다이얼로그를 띄우는 수단이라 `src/lib/` leaf에 둔다), element-key(sameElementKey — selector+frameId(0=top) 복합키 동등성, frameId 미지정 구버전 스냅샷은 0 정규화 — dedup·프레임 라우팅 술어 단일 출처 / elementKey — `${frameId}:${selector}` 문자열 복합키, React key·라벨용), session-keys(포트명·`editor:` 세션 키·PENDING_PREFIX/pendingKey/isPendingKey[탭에 매인 임시 소유자 키 — 로그·영상·첨부 blob이 issueId를 얻기 전 쓰는 키, 리터럴이 15곳+에 흩어져 있던 것을 중앙화]·ISSUES_PERSIST_KEY[issues-store persist 키 — blob-db GC가 같은 키로 저장분을 읽는다]·APP_SETTINGS_PERSIST_KEY[settings-ui-store persist 키 — background의 i18n/bg-init이 같은 봉투에서 locale을 직독하므로, 리터럴을 두 벌 두면 이름이 갈리는 순간 SW 에러 문구만 타입 에러 없이 조용히 영어로 굳는다]·pageKeyOf·originOf·FROZEN_PHASES), adf-sentinels, url-support, with-timeout(withTimeout(p, timeoutMs, label) — Promise에 시한을 거는 공용판. encode-range·video-thumbnail이 시그니처가 다른 사본을 각각 갖고 있던 것을 3인자로 통일했다. **src/content/ 레코더에서 import 금지** — recorders-entry가 공유 청크를 갖는 순간 동기 IIFE가 깨져 pre-arm이 무력화된다), settings-storage(background가 zustand 없이 `bugshot-settings` envelope를 직접 읽고 쓰는 관문 — read 8종·OAuth write 5종이 **테이블 2벌**에서 파생된다. write의 `fields`는 **갱신 화이트리스트**이고 거기 없는 건 신원 필드다[gitlab `baseUrl`은 self-managed 인스턴스 주소라 갱신 응답으로 덮이면 그 계정이 죽는다] — `keyof A`가 오타는 막아도 목록이 넓어지는 건 못 막아 `__tests__`의 envelope 전량 대조가 그쪽 그물이다. 테이블에 데이터로 남긴 예외 둘: jira의 v1 `state.jiraConfig.auth` 폴백, github의 `keepIfAbsent`[refreshToken·expiresAt이 optional이라 단순 대입이면 갱신마다 지워져 무한 재로그인]), notion-page-id, key-obfuscation, pending-log-prune, capture-commands, element-label, navigation-clear(shouldClearLogs — reload/origin 변화 시 cross-page 로그 초기화 판정), network-log-path(networkLogPath — 요청 URL→pathname 표시용), network-status(isStatusHidden — phase error + status 0에서 `statusKind` 우선(`networkError`만 true), 없는 옛 저장 로그만 `statusText === "Network Error"` 센티널로 폴백(2단이 하위호환의 전부). abort/timeout/beacon은 응답이 없는 것이지 '가려진' 게 아니라 제외 / isNetworkError·isNetworkPending — 요청 실패(phase error·4xx+)·대기중 판정, NetworkLogContent 행 배경·통합 타임라인 면색 공용), network-search(requestMatchesQuery — 네트워크 요청 URL/본문/헤더 부분일치 검색 순수 헬퍼), highlight-text(splitHighlight — 텍스트를 검색어 기준 매칭/비매칭 세그먼트로 분할하는 순수 헬퍼[indexOf 스캔·대소문자 무시·정규식 미사용·비손실], HighlightedText 렌더 입력, 네트워크 로그 상세 검색 하이라이트 공용), loopback-host(isLoopbackHost·isCredentialSafeUrl — 자격증명(PAT·LLM API 키)이 나가는 엔드포인트에 평문 http를 허용할지. https이거나 loopback만 통과. ssrf-guard와 반대 방향[저쪽은 loopback을 차단]이라 판정을 공유하지 않는다), credential-url(assertCredentialSafeBase — 자격증명이 실려 나갈 base URL의 egress 관문. 판정은 loopback-host의 isCredentialSafeUrl에 위임하고 i18n 문구만 입힌다[jira.workspaceUrl·gitlab.instanceUrl 두 접두사 union이라 t() 캐스트 없이 컴파일 검사를 받는다]. jira-api resolveUrl의 apiKey 분기·gitlab-api gitlabFetch가 호출 — 폼 가드는 안내용이고 저장된 설정으로 도는 요청은 이 관문만 지난다), masked-header(isMaskedHeaderValue — 네트워크 레코더가 민감 헤더를 지운 `***[len:N]` 표식 판정. 생산자 content/network-recorder.ts는 recorders-entry 청크라 src/content/ 밖 import가 금지돼 리터럴을 복제하고, __tests__/masked-header.test.ts의 소스 대조가 두 벌을 묶는다), external-url(isSafeExternalUrl·safeExternalHref — `http(s)`만 통과시키는 링크 스킴 가드. CSP 없는 logs.html의 이슈 링크와 IssueTitleOverlay 공용), inline-ref(INLINE_REF_RE — `![alt](inline:refId)` 파싱 단일 출처. blob-db의 고아 GC와 resolveInlineImages의 해석이 같은 패턴을 써야 살아있는 이미지가 안 지워진다 / inlineRefUrl·inlineRefMarkdown — 그 짝이 되는 생성기. alt 기본값이 빈 문자열이라야 현행 생성물 `![](inline:x)`과 같다. editor-store·TiptapEditor·submitToAsana 3곳이 손으로 쓰던 리터럴을 여기로 모았다 / inlineUploadFilename(refId, ext="webp")·inlinePlaceholderId — 업로드 파일명·확장자 없는 참조 id의 단일 출처. 생성 측(업로드 엔트리)과 조회 측(업로드 결과 맵)이 문자열을 따로 조립하던 11곳을 모았다 — 갈리면 이미지가 본문에서 무음으로 사라지고, 특히 jira는 사이드패널이 만들고 background가 조회해 **realm이 갈려 타입이 못 잇는다**. ext를 넘기는 건 원본 포맷을 보존하는 asana뿐이고, placeholder id는 notion 전용이지만 파일명과 접두사를 공유해 같은 파일에 둔다), ssrf-guard(isFetchableSheetUrl — background가 page-controlled stylesheet href를 fetch하기 전 loopback·사설·link-local·IPv6 ULA·IPv4-mapped 호스트 차단), inject-issue-url(평문 __BUGSHOT_META__ 태그만 문자열 치환 — 압축 blob 미접근), gzip-base64(CompressionStream 기반 gzip→base64 무손실 왕복 — logs.html 무거운 데이터 압축, 청크 btoa로 SW 블로킹 회피), external-links(USER_GUIDE_URLS — locale별 사용자 가이드 URL(bug-shot.com/{locale}/docs) / STORE_REVIEW_URL — 크롬 웹스토어 후기 페이지 URL, 설정 푸터·이슈 제출 완료 화면의 후기 버튼이 사용), named-colors(NAMED_COLORS — CSS Color L4 named color 집합 + transparent, categorizeToken[content]·isRenderableColorLiteral[sidepanel] 공용 단일 출처), log-colors(LogTone·TONE_TEXT[`text-<c>-600 dark:text-<c>-400`]·TONE_BG[행 배경 틴트 base `bg-<c>-100 dark:bg-<c>-950/50`]·TONE_BG_HOVER[hover 단계 `hover:bg-<c>-200/70 dark:hover:bg-<c>-950/70`]·TONE_BG_STRONG[선택 행·행 안 코드블럭처럼 이미 틴트된 면 위에 다시 떠야 하는 표면 `bg-<c>-200 dark:bg-<c>-950/70`. **neutral만 TONE_BG(빈 문자열)와 비대칭인 `bg-muted`** — 면이 반드시 있어야 하는 자리라 빈 값을 물려받으면 콘솔 log/debug 코드블럭 배경이 사라진다]·TONE_DOT[칠해진 점 `bg-<c>-500 dark:bg-<c>-400` — 네트워크 상세 상태 dot]·CONSOLE_LEVEL_TONE/NETWORK_METHOD_TONE + consoleLevelTextClass/networkMethodTextClass/consoleLevelBgClass/consoleLevelBgStrongClass — 로그 레벨/메서드/톤 텍스트·행 배경색 단일 출처. 사이드패널 로그 탭·다이얼로그(Console/Network/ActionLogContent 아이콘·행 배경)와 log-viewer markers.ts 툴팁 + 통합 타임라인 패널(timeline-merge.timelineFillClass·TimelineRow) 공유. 핀 색·content-type 아이콘·syntax highlight는 스코프 밖), jira-sprint(isActiveSprint — active/future 화이트리스트[미지 상태가 유효로 새면 끝난 스프린트로 제출된다]. sprint-sticky가 유일한 런타임 소비처이고 leaf로 뗀 이유는 번들 경계: 사이드패널이 background/jira-api를 import하면 OAuth·설정 저장소가 패널 그래프에 딸려온다 — buildIssueAdf↔adf-logs-link와 같은 형태), dismiss-guard(hasOpenDismissLayer — Dialog dim 클릭이 열린 Popover만 닫고 다이얼로그는 살려두게 하는 소유 판정. Radix DismissableLayer가 modal Dialog 자신을 pointer-events 기준선으로 삼아[isPointerEventsEnabled = index >= highestIndex] 위에 쌓인 non-modal 레이어와 Dialog 양쪽에 outside 이벤트를 뿌리는 걸 dialog.tsx가 되받아 막는다. 판정은 포커스 위치가 아니라 PopoverContent가 심는 `data-dismiss-layer` 마커 — 소비처가 못 덮게 스프레드 뒤에 박혀 있다. Dialog **아래** 깔린 레이어는 Radix가 인라인 pointer-events를 꺼두므로 세지 않는다[그 레이어는 자기 outside 이벤트조차 못 받아 스스로 닫히지 못해, 세면 dim이 다이얼로그 수명 내내 막힌다]. AlertDialog[Radix가 이미 preventDefault]·Select[disableOutsidePointerEvents]는 대상 아님), utils)
├── components/ui/       # shadcn 컴포넌트 + collapsing-tabs.tsx(CollapsingTabsList/TabLabel — 폭 부족 시 탭 라벨을 아이콘만 남기고 일괄 접는 측정 기반 래퍼. 메인/서브/플랫폼/로그뷰어 탭 공용, 필터 탭 제외)
├── components/icons/    # 커스텀 브랜드 아이콘 (SlackIcon — @icons-pack/react-simple-icons 미제공 Slack 로고 인라인 SVG)
├── styles/              # globals.css (Tailwind/shadcn 디자인 토큰·다크모드 CSS 변수·커스텀 스크롤바·Pretendard + Geist Mono import — @import는 반드시 @tailwind보다 위, 아래로 내려가면 postcss-import가 경고만 내고 빌드는 통과해 폰트가 조용히 폴백된다). **base가 테마별로 다르다(라이트 slate / 다크 neutral) — 의도된 비대칭이니 "일관성" 명목으로 밀지 말 것.** **토큰 표와 mono 블록(`font-variant-ligatures: none`)이 log-viewer/styles.css에 복제돼 있어 둘을 함께 고쳐야 한다**(log-viewer는 별도 빌드라 globals.css를 못 받는데 App.tsx가 사이드패널 컴포넌트를 import해 mono 표면이 실재한다) — __tests__/tokens.test.ts가 두 표 일치·mono 블록 일치·라이트 채도>0/다크 채도=0·destructive 대비 하한·mono 폴백 존재를 고정. 디자인 시스템·UI 컨벤션 전반은 DESIGN.md(docs/)
├── sidepanel/lib/resolveDark.ts   # resolveDark(theme, prefersDark) — 앱 theme 설정 + OS 선호를 최종 다크 여부로 접는 순수 함수. useThemeEffect(사이드패널 .dark 토글)와 picker-control(picker.start에 실어 보내는 theme)이 **같은 판정을 써야** 사이드패널과 페이지 인스펙터 카드가 안 갈린다 — 판정이 두 벌이면 그게 이 함수를 만든 원인 버그다. (CssCodeMirror에 세 번째 복제본이 남아 있다 — 미이행)
├── sidepanel/lib/localeLabels.ts  # 언어 셀렉터 라벨 `LOCALE_LABELS`(폴백 금지 — 라벨이 없으면 그 로케일이 셀렉터에 안 떠서 사용자가 못 고른다. 배열로 두면 사전을 다 채우고도 무음으로 빠진다). 값은 자기 언어 표기. 컴포넌트가 아니라 lib에 둔 이유는 SettingsTab에 두면 테스트가 렌더를 거쳐야 재기 때문
├── test/               # 테스트 셋업 — locale-parity.ts(로케일 사전 대칭 검사 단일 출처 — findParityViolations[기준 로케일 대비 키 대칭·빈 값·placeholder 토큰]/findUncovered[LOCALES에 등록만 하고 사전·메타가 없는 로케일 — 폴백 금지 테이블의 런타임 강제]/findExtraneous/placeholderTokens. 메인 i18n과 log-viewer 복제 사전이 **같은 검사기를 공유**하고, 검사기 자체는 __tests__/locale-parity.test.ts가 일부러 깨뜨린 3번째 로케일 fixture로 검증한다 — 대칭 테스트는 항상 초록이라 검사기가 고장 나도 초록이기 때문[vacuous green]), setup-dom.ts(`*.test.tsx`(jsdom) 전용: testing-library cleanup + ResizeObserver·PointerCapture·scrollIntoView 폴리필. vitest.config.ts의 environmentMatchGlobs가 확장자로 환경을 가른다 — `*.test.ts`는 node라 무관), ai-session.ts(makeSession — AISession을 캐스트 없이 타입 그대로 만족시키는 fake 세션이라 prompt 옵션에서 signal이 빠지면 typecheck가 깨진다[signal을 무시하는 mock이 취소 회귀를 green으로 통과시킨 전례] + deferred — 호출을 붙잡아 뒀다 테스트가 원할 때 settle시켜 취소 시점을 제어. AiDraftDialog·AiStylingDialog 테스트 공용), withLocaleScan.ts(본문 언어 래핑 게이트 두 벌[sidepanel/lib/__tests__/builderLocaleWrap · background/__tests__/bodyLocaleBackground]이 공유하는 소스 스캐너 — exportedSegments[최상위 export 선언 단위 분할. 비-export 헬퍼의 t()는 감싸진 진입점 안에서만 불리므로 제외하고, default·let·class·generator까지 마크를 만드는 건 미매칭이 false green이 되기 때문] + stripWithLocaleCalls[withLocale 호출을 괄호 매칭으로 도려낸 나머지 — "t와 withLocale이 한 함수에 같이 있다"만 보면 `const h = t(…); return withLocale(l, () => inner(h))`가 통과해 h만 화면 언어로 굳는다]. 복제하면 한쪽만 강화된다)
└── types/               # platform.ts (PlatformId/Accounts/LastSubmitFieldsByPlatform), github.ts, jira.ts, linear.ts, notion.ts, gitlab.ts (GitlabAuth/GitlabPatAuth/GitlabOAuthAuth + 이슈 페이로드·상태 타입), asana.ts (AsanaAuth/AsanaPatAuth/AsanaOAuthAuth + workspace/project/user/task 페이로드·상태 타입), clickup.ts (ClickupAuth/ClickupPatAuth/ClickupOAuthAuth + workspace/space/list/user/task 페이로드·상태 타입 — 토큰 만료 없음이라 OAuth auth에 expiresAt/refreshToken 없음), slack.ts (SlackAuth=SlackOAuthAuth(OAuth 전용·BYOK 없음·만료 없음) + SlackAccount(teamId/teamName/defaults) + channel/user/message 페이로드 타입 + SlackOAuthResult), environment.ts (EnvironmentRow), console.ts, network.ts, action.ts (ActionEntry/ActionLog/ActionLogSummary), attachment.ts (UserAttachmentMeta — 사용자 첨부 파일 메타), messages.ts (BgRequest/BgResponse/BgInternalMessage **선언 전용** — `sendBg`·`BgError`·판독 4개·emitter 7종은 `lib/bg-client.ts`·`lib/app-events.ts`로 나갔다. `src/types/`는 `coverage-report.mjs`가 "실행 코드 없음"을 근거로 로직 분모에서 통째 제외하므로 여기에 로직을 두면 그 근거가 거짓이 된다 — 예외는 platform.ts의 상수 3개뿐), picker.ts, log-viewer.ts (LogViewerData/LogViewerReport — 로그 뷰어 데이터 + Report 탭 본문 인터페이스, video 동기화·screenshot 정적·report 임베드 포함), user-agent-data.d.ts (NavigatorUAData 타입 보강) 등
vite.log-viewer.config.ts# 로그 뷰어 전용 Vite 빌드 설정 (viteSingleFile → dist-log-viewer/index.html 단일 파일, @/i18n alias redirect)
tsconfig.e2e.json        # e2e/ 전용 tsconfig — 루트 references 편입으로 pnpm typecheck가 spec·fixture 타입을 검사 (Playwright는 transpile만 함)
.nvmrc                   # Node 메이저 단일 출처 — CI 두 job이 `node-version-file: .nvmrc`로 읽는다. 로컬을 여기 맞춰두면 "내 노드에선 되는데 CI에선" 축이 사라진다(판정 주체가 CI다)
.env.ci                  # **커밋되는 유일한 .env** (gitignore에 `!.env.ci` 예외). CI e2e 빌드용 더미 값 — 워크플로가 .env.local로 복사해 쓴다. OAuth client ID가 비면 isConfigured()가 false가 되어 연동 탭 UI가 로컬과 갈리므로 그 판정만 통과시키는 가짜 문자열이고, 실 크레덴셜이 아니다. PostHog 키는 비워 집계 no-op 유지. 키 집합은 .env.example과 동기
e2e/                     # Playwright e2e 스위트 (@playwright/test, BUGSHOT_E2E_BUILD=1 산출물 dist-e2e/ 로드. 차단 게이트는 CI의 e2e-gate 단독 — 로컬 실행은 미리 보기용). **커버리지·수동 잔여는 e2e/COVERAGE.md, 함정은 e2e/GOTCHAS.md, 개요·실행·헬퍼는 e2e/README.md**
├── README.md            # 개요 · 실행법 · project 구성 · 헬퍼/fixture 빠른 참조 (커버리지·함정은 아래 분리 문서) — /e2e-write가 함께 갱신
├── COVERAGE.md          # 커버리지 맵(spec별 시나리오) · 수동 잔여(자동화 못 한 것 + 이유)
├── GOTCHAS.md           # 실전 함정 누적 — 새 spec 쓰기 전 필독
├── playwright.config.ts # workers:1 · retries 로컬 0/CI 1 · forbidOnly CI 한정 · headed · trace retain-on-failure · reporter CI에 github 추가 · 2 projects — sidepanel(확장 구동, e2e/*.spec.ts) / logview(확장 없이 dist-log-viewer standalone, e2e/logview/, viewport 1280×800). logview에 dependencies를 두지 않는 이유: 실제 의존이 아닌데 --shard 사용 시 의존 project가 샤드마다 전량 실행돼 샤딩 효과를 지운다
├── fixtures/
│   ├── extension.ts     # worker-scoped ext fixture (ephemeral 포트 정적 서버·persistent context·extension id·teardown) + 헬퍼 (enterDebug/pickElement/typeStyleValue/… — README 참조)
│   └── pages/           # 로컬 fixture 페이지 — basic.html · second.html(cross-page 세션 폐기) · console-error.html(정적 인라인 스크립트의 async throw — uncaught error의 stack·filename에 page URL이 실려 console linkify 판정) · capture-context.html(element 캡처 컨텍스트 확장 게이트 G1/G2/G3를 전부 만족하는 #modal + 시맨틱 조상 없는 폴백 #plain-btn) · shorthand-var.html(var()가 낀 shorthand의 specified 복원 — `*{border:0}` 리셋이 컴포넌트 규칙보다 먼저 와야 spec이 유효) · stable-locator.html(selector 생성 우선순위 — 대상마다 형제를 둬야 finder가 조상 앵커까지 올라간다) · iframe.html(picker iframe 가드·iframe 로그) · cross-origin.html(localhost iframe — origin 필터) · prearm.html(pre-arm 로드 초반 캡처) · websocket.html(WebSocket 프레임 캡처 — __openWs/__closeWs/__wsCheck, ws echo는 extension.ts http upgrade 핸들) · scroll-capture.html(페이지 전체 캡처 — 150vh + fixed/sticky 헤더 중복 제거, 타일 2장 고정으로 captureVisibleTab quota 최소화) · iframe-nested.html/iframe-child.html(1-depth 등록·중첩/sandbox 거부) · cross-origin-style.html+cross-origin-style.css(cross-origin stylesheet 원문 fetch) · api-hosts.html(재현 환경 API Hosts 자동 행 — __fetchApis(port)로 api/auth 서브도메인에 요청 3건, arm 후 spec이 호출) · dom-tree-deep.html(8단 중첩 + 긴 클래스명 — DOM 트리 라벨이 패널 폭을 반드시 넘게 해 가로 스크롤 전제를 성립시킨다) · nav-history.html(네비게이션 유형 판별 — pushState 버튼 2개 + 해시 링크 3개. `<a href="#x">`가 popstate까지 쏘는 걸 태우는 게 요점) · specificity.html(캐스케이드 승자 판정 — 문서순을 이기는 높은 spec / hex 이스케이프 클래스 `2xl:accent` / 조건 거짓 @container. 값 축이 전부 rem인 이유는 GOTCHAS 참조)
├── logview/             # 확장 없는 standalone 로그 뷰어 스위트(project: logview) — fixtures.ts(dist-log-viewer/index.html에 합성 로그를 평문 JSON으로 setContent 주입 + makeAction/Console/Network/Report 빌더 + clipboard stub + generateTinyVideoDataUrl[in-page MediaRecorder로 finite-duration 영상 즉석 생성 — 마커·seek 검증용]) · log-viewer.spec.ts(i18n 라벨 raw-key 회귀·필터·검색·origin 필터·Report 탭·다운로드 버튼, ko/en) · log-viewer-sync.spec.ts(영상 모드 — 마커 3타입 통합·마커 클릭 탭전환·로그 행/칩 클릭 seek) · timeline-panel.spec.ts(병합 타임라인 패널 — 시간순 병합·필터 개수배지·타입 단일선택·검색[console args·network 본문]·행 클릭 seek+탭전환·playhead active 하이라이트)
└── *.spec.ts            # activetab-broad-permission · style-edit-flow · style-code-view · style-zindex · style-position-section · style-changes-dialog · style-changes-stacked · style-field-fixes · style-token-hint · style-bugfix-regression · style-cross-origin-section · style-specificity(캐스케이드 승자 판정 — 실 CSSOM 위에서 specificity·이스케이프·@container opaque 고정) · style-shorthand-var(var()가 낀 shorthand의 specified 복원 — CSSOM longhand가 전부 빈 문자열이라 수동 전개가 유일한 출처, 이 spec이 유일한 그물) · stable-locator(요소 selector 생성 우선순위·PII 값 거부) · border-per-side · linked-quad-merge · buffered-reselect-edit · body-composition-reorder(본문 구성 순서 재정렬·영속·복원·draft 반영) · issue-body-locale(이슈 본문 언어 축 — 화면 언어·AI 출력 언어와 독립 전환) · dom-tree-nav · dom-tree-overflow(DOM 트리 가로 스크롤 — 전제 단언 scrollWidth>clientWidth·라벨 무클리핑·행 폭 = 스크롤 폭) · capture · capture-context(element 캡처 컨텍스트 확장 — 크롭 종횡비로 판정, 픽셀 폭 환산 금지) · capture-modes-layout · annotation-overlay · download-buttons · draft-resume · freeform-draft · log-capture · logs-cross-page · logs-error-warn · logs-iframe · logs-origin-filter · logs-prearm · console-linkify · network-body-search · websocket-log · onboarding · picker-guard · session · settings-sections · attachments · clickup-submit-gating · slack-submit-gating · slack-issue-promotion · slack-promote-media-guard · ai-draft · ai-draft-log-refs(AI 지목 로그 코드블럭 삽입 — 왕복 dedup·수동 블럭 보존) · ai-draft-matching-log(유저·캡처 term과 매칭된 성공 2xx 응답 후보(m*)가 프롬프트에 실리는 경로) · ai-local-provider · ai-styling · capture-methods(화면·페이지 전체 캡처 툴바) · draft-field-edit · network-search-highlight · picker-iframe · recording-annotation(녹화 오버레이 — 펜·박스·형광펜 setTool + 드래그 판정) · annotation-zoom · action-log-scope(액션 로그 지원 모드 — screenshot drafting 카드·토글·기록, element 부재) · action-log-nav-type(네비게이션 유형 판별 — data-nav-type 속성으로 판정[문구는 로케일 비결정]. 레코더 본문의 유일한 그물이라 E3=reload·E5=프래그먼트 오라벨 금지·E7=유령 진입 항목 금지가 각각 유닛이 못 잡는 뮤테이션을 담당) · unsupported-url · log-insert · repro-prefill · style-class-section · style-code-value-hints · style-table-section · code-block-collapse · inline-image-annotation(본문 삽입 이미지 어노테이션 — hover 액션 노출·초기화 숨김·오버레이 진입·삭제) · hover-shield(hover 방패 소유권 — 만료 타이머가 낀 축이라 클릭 성공 대신 shadow 아웃라인 rect로 판정) · api-hosts-env-row(재현 환경 API Hosts 자동 행 — *.bugshot.test 서브도메인으로 동족 origin 생성) · diff-image-annotation(element diff before/after 이미지 주석 — src 변화 여부·버튼 개수로만 판정) · jira-project-switch · jira-project-sticky · jira-sprint-field(Jira 스프린트 선택 — 판정 유무에 따른 행 노출·이슈타입 전환 시 행 소멸과 복원값 제거·재제출 진입점. 스파이는 addInitScript로 심는다[뱃지 조회가 패널 부팅 즉시 나가 401 → 전역 세션만료 오버레이]) (커버리지 맵은 COVERAGE.md)
oauth-proxy/             # Cloudflare Worker — Atlassian /token + GitHub /github/{token,refresh} + Notion /notion/token + Asana /asana/{token,refresh} + ClickUp /clickup/token(refresh 없음) + Slack /slack/token(oauth.v2.access, refresh 없음) 교환 (client_secret 서버 보관, Linear·GitLab은 PKCE라 proxy 불필요)
docs/                    # 프로젝트 레퍼런스 문서 (루트엔 CLAUDE.md·README.{md,ko.md}와 GitHub 규약 파일 LICENSE·SECURITY.md·CONTRIBUTING.md만 유지)
├── DIRECTORY.md     # 이 문서 — 디렉터리 구조·파일별 역할
├── ARCHITECTURE.md  # 설계 상세 (탭 스코프·인증·어댑터·토큰 체인·CSSOM 캐시·마이그레이션 등)
├── DESIGN.md        # 디자인 시스템·UI 컨벤션 (토큰·레이아웃·합성 컴포넌트·상태 표현)
├── PERMISSION.md    # Chrome 권한 전체 레퍼런스 (activeTab·OAuth 토큰 흐름 등)
├── CI.md            # GitHub Actions 구성·게이트·함정 (job 4개·샤딩·xvfb depth 24·nightly notify). CI 수정·실패 진단 시 먼저 읽는다
├── POSTMORTEM.md    # 회귀·버그 사후분석 회고 누적 (/postmortem)
├── privacy.ko.md   # 개인정보처리방침 ko 원본 (bug-shot.com/ko/privacy 서빙 소스)
├── privacy.en.md   # 개인정보처리방침 en 번역 (ko와 항상 동기화)
├── _config.yml      # GitHub Pages(jekyll-theme-minimal) 서빙 설정 — `exclude: [features]`라 기획 문서는 공개에서 빠진다
└── features/        # /feature 산출물 — 기능별 디렉터리에 prd.md·design.md·tasks.md. /implement·/ship bypass가 계획 원본으로 읽는다
    └── DROPPED.md   # 안 하기로 한 기획의 사유 기록. 기획을 접으면 디렉터리를 그냥 지우지 말고 여기 항목을 남긴다(왜 안 하는지 + 무엇이 바뀌면 다시 볼 만한지). /feature 0단계가 새 기획 착수 전 이 파일을 grep해 과거 판단을 소환한다
guide/                   # 사용자 가이드 소스 (ko/en). bugshot-web docs-portal이 빌드타임에 fetch → bug-shot.com/{locale}/docs로 서빙. guide/{ko,en}/ 각각 SUMMARY.md + README.md + quick-start.md + faq.md + assets/ + 6개 섹션(integrations·settings·element·screenshot·video·logs) 25페이지. 작성 규칙은 guide/AUTHORING.md
scripts/                 # 저장소 유지보수 스크립트 (빌드 파이프라인 밖)
├── sync-agents.mjs   # Codex 미러 생성기 — CLAUDE.md→AGENTS.md(.agents/PREAMBLE.md 프리펜드) + .claude/commands/*.md→.agents/skills/source-command-*/SKILL.md. 본문 무치환 복제, 고아 미러 삭제, `--check`로 드리프트만 검출(exit 1). `pnpm sync:agents` / PostToolUse 훅 / `/push` 게이트가 호출
├── check-prearm-chunk.mjs  # pre-arm 청크 형태 검사 — 빌드 산출물의 manifest에서 recorders-entry 항목을 찾아 world=MAIN·run_at=document_start·loader 미강등·IIFE 시작·잔여 static import 0을 대조(exit 1). recorders-entry에 외부 static import가 유입되면 crxjs가 async loader로 되돌아가 pre-arm 버퍼링이 조용히 죽는데 typecheck·유닛이 못 잡는다(행동 검증은 e2e/logs-prearm.spec.ts지만 CI에 없음). `pnpm check:prearm` / CI가 호출
├── postmortem-report.mjs  # 회고 집계기 — docs/POSTMORTEM.md 항목의 영역·계열·그물 태그를 파싱해 축별 랭킹 + 영역×계열 반복 함정 교차 + 자동 그물 밖(시각·수동) 비율을 리포트. vocab 단일 출처(AREAS·PATTERNS·NETS)이자 `--check`로 태그 누락·오타·헤딩 유실을 차단(exit 1). `pnpm postmortem:report`/`postmortem:check` / `/postmortem` 스킬이 호출. 그물 `scripts/__tests__/postmortem-report.test.mjs`
└── coverage-report.mjs  # 커버리지 리포트·트렌드 비교기 — coverage/report/coverage-summary.json(vitest v8 — vitest.config.ts가 baseline.json 보호를 위해 reportsDirectory를 report/로 격리) 읽어 로직 스코프(isBrowserBound() 단일 출처로 브라우저/UI 코드 제외) vs 전체 두 지표 계산 + git-tracked coverage/baseline.json 대비 이전→지금 비교·회귀 래칫·개선 후보 랭킹. `--update`로 베이스라인 갱신. `pnpm coverage:report`/`coverage:update` / `/coverage` 스킬이 호출
coverage/                # 커버리지 산출물 — 리포트 본체는 .gitignore, baseline.json(트렌드 기준선)만 git-tracked
.github/                 # GitHub Actions + 외부 기여자용 이슈·PR 템플릿
├── workflows/ci.yml  # dev push·main PR·nightly(08:00 KST = cron 23:00 UTC, schedule은 기본 브랜치=main에서만 발화)·workflow_dispatch. permissions: contents:read(fork PR 대비). job 4개 — verify(typecheck → sync:agents:check → test → build → check:prearm, 결정적) / e2e(전 스위트를 --shard=N/4 매트릭스로 러너 4대, headed 강제라 xvfb-run 경유·가상 스크린 깊이 24 필수[캡처 spec이 픽셀 색 판정], fail-fast:false, 실패 샤드는 report·trace artifact 업로드, 더미 .env.ci를 .env.local로 복사해 secret 없이 빌드) / e2e-gate(샤드 결과 집계 — 브랜치 프로텍션 required check가 가리키는 단일 이름) / notify(schedule·workflow_dispatch 한정 Slack Incoming Webhook 알림 — 성공/실패 항상 전송, SLACK_WEBHOOK_URL secret 없으면 warning 후 skip). verify와 e2e는 병렬
├── workflows/trigger-web-deploy.yml  # main의 privacy/guide 변경 시 bugshot-web 배포 훅 호출
├── ISSUE_TEMPLATE/   # 이슈 폼 — bug_report.yml(버전·Chrome/OS·영역·플랫폼·확장 콘솔 에러·문제 페이지 성격) + feature_request.yml(문제 우선 서술 + 캡처 백엔드 불가 스코프 체크) + config.yml(가이드·보안 신고·privacy 전송 표 링크)
└── pull_request_template.md  # PR 체크리스트 — main 기준 브랜치 안내 + typecheck/test/build + privacy ko/en 동시 갱신 조건
.agents/                 # Codex 런타임 미러 (전부 생성물 — PREAMBLE.md만 수기)
├── PREAMBLE.md       # AGENTS.md 상단에 붙는 Codex 런타임 차이 (스킬 매핑·미제공 스킬·훅 부재·커밋 트레일러)
└── skills/           # .claude/commands/ 미러 17개 (push·merge·deploy·sync 제외)
guide/AUTHORING.md       # 가이드 작성 매뉴얼 (docs-portal 서빙 밖, 레포 전용 — ko/en 상위). 운영 방식·IA·톤·사실 대조 소스·UI 라벨 규칙·footer·검증 = 가이드 작업의 단일 출처. /guide·/push가 가이드 갱신 시 필독
```
